# ANIMATION.md

## 고정 규칙

- Day 4~9 애니메이션 코드 금지(GSAP 포함) — 재미 장치의 "로직"만 구현, "연출"은 Day 10~12
- 모든 애니메이션은 cleanup 필수 (unmount 시 인스턴스 kill)
- `prefers-reduced-motion` 대응 필수
- 서버 렌더링 보존: 애니메이션이 콘텐츠 표시를 막으면 안 됨
- 애니메이션 속성은 `transform`·`opacity`만 (레이아웃 스래싱 금지)

## 의존성 — 착수는 CSS + rAF, 라이브러리는 열어둔다 (2026-08-27 Day 10)

**Day 11 구현을 새 라이브러리 없이 시작한다.** 필요한 모션 4종 중 CSS로 불가능한 건
숫자 카운트업 하나뿐이고 그건 rAF 루프 한 개로 끝나므로, 시작점을 가볍게 잡는다.
**GSAP을 기각한 것이 아니다** — SCR-002 §13의 "연출(GSAP)" 표기는 유효하고,
애니메이션 설계는 Day 11~12 구현·검수에서 수정될 수 있다 (사용자 결정 2026-08-27).

- 착수 도구: `tw-animate-css`(이미 globals.css에 임포트됨) + CSS + rAF
- 열어둠: gsap(gzip 후 ~23KB) · framer-motion — 아래 조건에 걸리면 도입을 검토한다

### 도입 검토 조건 (Day 11~12 · 하나라도 해당하면 그 자리에서 재검토)

1. CSS 키프레임으로 등급 릴 정지의 손맛이 안 나온다 (사용자 감성 판정에서 컷)
2. 시퀀스 오케스트레이션(중단·스킵 되감기·구간 재생)이 rAF 수기 관리로 흐트러진다
3. 60fps 검수(chrome-devtools 루프)에서 CSS 구현이 프레임을 떨어뜨린다

> ✅ **Day 11 결과: 3개 전부 미발동 — 도입 근거 없음** (2026-08-28)
> ① 육안 판정 통과. 단 **원안 그대로는 컷이었다** — 릴 800ms·easeOutQuad가 "너무 빨리
> 지나간다"로 걸려 1100ms·easeOutCubic으로 조정한 뒤 통과했다. 부족했던 것은 라이브러리가
> 아니라 **감속 곡선**이었고, 그건 rAF 상수 두 개로 끝났다.
> ② 스킵·Esc·자연 종료·reduced-motion 4경로가 `finish()` 하나로 수렴하고 정리 대상은 rAF
> 핸들 1개다. 오케스트레이션이 흐트러질 표면이 없다.
> ③ 프로덕션 216프레임 중앙값 16.7ms · 17ms 초과 1건 · 드롭 0.
>
> **재론 금지는 걸지 않는다.** Day 12 `review-animations`에서 새 근거가 나오면 다시 본다 —
> 검증 전에 문을 닫으면 판단이 막힌다는 것이 2026-08-27의 교훈이다.

**도입 시 지키는 선**: LCP 2.5초 예산은 협상 대상이 아니다. 연출은 `/report`에서만
쓰므로 dynamic import로 초기 번들과 분리한다 (공유 카드의 `html-to-image`와 같은 방식).

## 토큰

### 이징 — 커스텀 2개만

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--ease-out-quart` | `cubic-bezier(0.25, 1, 0.5, 1)` | 350ms 이상 이동의 착지. JS `easeOutQuart`와 같은 곡선이라 CSS/JS가 안 어긋난다 |
| `--ease-out-back` | `cubic-bezier(0.34, 1.56, 0.64, 1)` | 오버슈트 — 등급 릴 정지(클라이맥스) 전용 |
| `ease-out` (Tailwind 내장) | — | 250ms 이하 전부. 짧은 구간은 내장 곡선과 구분이 안 되므로 토큰을 만들지 않는다 |

기각: spring·elastic·bounce / `ease-in-*` 계열(진입 전용 화면이라 퇴장 곡선 불필요) / `ease-in-out`(pulse가 내장으로 이미 씀)

### duration — CSS 토큰 0개

ms 값은 전부 `src/lib/reveal.ts`의 `PLAN` 상수 하나에 둔다.
rAF와 CSS가 **같은 숫자를 둘 다 알아야** 하므로, `delay-[600ms]` 클래스와 JS 상수로 이중화하면
튜닝할 때 반드시 어긋난다. 타이밍은 인라인 스타일로 주입, 성격(fade/slide/ease)만 클래스로.

문서용 스케일(신규 모션 설계 시 기준값): **150 즉각 · 250 짧게 · 400 보통 · 700 길게**

## 공개 연출 타임라인 예산 (SCR-002 REQ-RES-002)

### 풀 버전 — `entry=cli`, 등급 있음

| 구간 | 시작 | 길이 | 끝 | easing | 이유 |
|---|---|---|---|---|---|
| 빌드업 "채점 중…" | 0 | 600 | 600 | pulse(내장) | 기대감의 최소 체류. 600ms 미만이면 "채점했다"는 인지가 안 생긴다. **크기는 display 스케일(`text-6xl md:text-7xl`) 고정** — 아래 §LCP 참조 |
| 지표 행 진입 ×6 | 600+90·i | 240 | ~1530 | `ease-out` | 스태거 90ms — 120ms면 늘어지고 60ms면 동시에 보인다 |
| 숫자 카운트업 ×6 | 600+90·i | 700 | 1750 | easeOutQuart(JS) | 초반 44%에 90% 도달 → 마지막 자릿수에서 뜸들이는 착지감 |
| 비트(행 디밍 1→0.35) | 1750 | 100 | 1850 | `ease-out` | 시선을 지표에서 등급으로 넘기는 호흡. 없으면 클라이맥스가 카운트업에 묻힌다 |
| 등급 릴 회전(16스텝) | 1850 | 1100 | 2950 | easeOutCubic(JS) | 첫 스텝 23ms → 마지막 직전 113ms → **마지막 등급 체류 437ms**. **릴 영역은 1850에 200ms 페이드인** — 그전에 초기 등급이 보이면 클라이맥스를 미리 흘린다(회전과 겹쳐 총합 불변) |
| **릴 정지 pop** scale 1→1.08 | 2950 | 380 | **3330** | `--ease-out-back` | 오버슈트가 "철컥" 정지감. 색을 안 쓰는 DESIGN.md 제약을 크기로 대체 |
| 카드 전환 | 클릭 후 | 350 | — | `--ease-out-quart` | ↩︎ 개정 2026-08-30: 자동 전환 폐지. 3330 = **자연 종료·대기 진입 + EVT-RES-002 발화**, `ready` 전이는 결과 보기(EL-RES-005) 클릭이 한다. 대기 중 화면은 정지 상태(등급·디밍 행) + 버튼 교체(건너뛰기 → 결과 보기, 200ms 페이드) |

**검산**: `600 + (5×90 + 700) + 100 + 1100 + 380 = 3330ms` — 명세 상한 4000ms 이하, 하한 3000ms 이상. 여유 670ms.
행이 5줄(하이라이트 없는 재진입)이면 3240ms로 자동 감산 — 총합을 하드코딩하지 않는 이유다.

> **대기 개정은 전 모드 공통** (2026-08-30): 축약판·부분 모드도 자연 종료 후 결과 보기 대기.
> reduced-motion만 예외 — 스테이지 자체가 미마운트라 버튼을 둘 곳이 없고, 기존대로 즉시 카드다.

> ↩︎ **개정 2026-08-28 (육안 판정)**: 릴 800ms·easeOutQuad에서 **1100ms·easeOutCubic**으로.
> 원안은 마지막 등급이 **200ms**만 머물러 "등급이 정해지는 과정이 너무 빨리 지나간다"는 판정을 받았다.
> 세제곱 감속은 뒤쪽 스텝을 길게 잡아 **마지막 체류 437ms**(2.2배)를 만든다 — 슬롯의 손맛은
> 회전 속도가 아니라 **"곧 멈춘다"는 예고**에서 나온다. 축약판(`link` 700ms)은 AC-5의 1.5초
> 명세에 묶여 있어 길이는 그대로 두고 감속 곡선만 공유한다.

### 축약판 — `entry=link` (AC-5)

카운트업 전면 생략. 수신자는 남의 결과를 보는 것이라 기대감 구간의 가치가 0에 수렴한다.

| 구간 | 길이 | 누적 |
|---|---|---|
| 스테이지 진입 + 빌드업 | 250 | 250 |
| 등급 릴 회전 | 700 | 950 |
| pop | 300 | **1250** |
| 카드 전환 | 250 | 1500 |

**검산**: `250 + 700 + 300 = 1250ms` 연출 + 250 = 1500ms — 명세값 정확 일치.

### 축약판 — 부분 모드 `prompts < 10` (REQ-RES-003 AC-1)

`judge()`가 호출되지 않으므로 릴·pop 구간이 **데이터로** 0이 된다. 분기 코드가 아니다.

| 행 수 N | 연출 종료 | 카드 표시 |
|---|---|---|
| 6 | 1500 | 1800 |
| 4 | 1340 | 1640 |
| 1 | 1100 | 1400 |

`빌드업 500 + 80·(N−1) + 카운트업 600` + 카드 300. 상한이 아니라 하한이 문제 — 1.4초 아래로 내려가지 않는다.

**`link` + 부분 모드 겹침** = 500ms(사실상 페이드). 네 번째 모드를 만들지 않는다.

### reduced-motion (AC-4) — 페이드 1종

- **스테이지를 마운트하지 않는다.** decode 성공 → 즉시 `ready` + `reveal_completed{skipped:false}`
- 카드 래퍼에 `fade-in` **200ms** `ease-out` 1종만 (opacity만, translate 없음)
- 감지: `matchMedia("(prefers-reduced-motion: reduce)").matches` 1회. change 리스너 불필요 — 3초 사이에 OS 설정을 바꾸는 사용자는 없다
- ⚠️ globals.css에 `animation-duration: 0.01ms !important` **전역 킬 블록을 넣지 않는다.** 그 대해머는 이 대체 페이드까지 죽인다. 크로스페이드는 reduce에서 권장되는 대체안이라 살려야 한다

## 공유 시트 전환 (Day 12 구현 — 채택 표의 "공유 시트 열림·닫힘" 항목)

키프레임이 아니라 **트랜지션** — 빠른 재열림에서 현재 상태부터 이어간다(재시작 없음).
네이티브 `<dialog>`의 display·top-layer는 `transition-behavior: allow-discrete` + `@starting-style`로
붙잡는다. **미지원 브라우저는 자동으로 현행(즉시 표시)으로 강하** — JS 분기 0.

| 구간 | 열기 | 닫기 | 근거 |
|---|---|---|---|
| 모바일 slide-up (`translateY(100%)` → 0) | 400ms `--ease-out-quart` | 250ms `ease-out` | 350ms 이상 이동의 착지는 quart. 닫기가 짧은 **비대칭 타이밍** — 조작 응답은 빠르게 |
| md+ scale·fade (`scale(0.97)` → 1) | 250ms `ease-out` | 200ms `ease-out` | 중앙 모달은 화면 밖 이동이 없다. 250ms 이하는 내장 곡선 (토큰 안 만든다) |
| 백드롭 opacity | 열기·닫기 본체와 동기 | 〃 | — |
| reduced-motion | 페이드 200ms (transform 강제 none) | 〃 | 이동·크기만 죽이고 페이드는 남긴다 — 전역 킬 블록 금지 규칙과 동일 |

실측(2026-08-30): 열림 중 120ms 시점 translateY 148px 진행(모바일)·scale 0.9763(md+),
닫기 후 display가 전환 끝까지 유지 → 250ms 뒤 none (allow-discrete 동작 확인).

## 대시보드 앵커 스크롤 (Day 12)

기각 표의 "네이티브 `scroll-behavior`로 충분" 판정의 실구현 — `html { scroll-behavior: smooth }`
한 줄, `prefers-reduced-motion: no-preference` 가드. JS 스크롤 금지 유지.

## 모션 채택·기각 판정 (find-animation-opportunities 게이트, 2026-08-27)

### 채택

| 대상 | 빈도 | 목적 |
|---|---|---|
| 공개 연출 전체 | **Rare** — CLI 실행당 1회, 안내가 "내일 또" | State indication + Delight |
| 등급 릴 정지 | Rare | **State indication** — DESIGN.md가 "등급은 색이 아니라 크기·굵기·연출로"를 확정해, 모션이 정보 위계를 떠맡는다. 장식이 아니다 |
| 공유 시트 열림·닫힘 | Occasional | 공간 연속성 (모바일 바텀시트 = slide-up, md+ = scale/fade) |

3~4초는 UI 300ms 예산을 넘지만 그 예산은 '조작에 대한 응답'에 걸린다. 이건 콘텐츠 공개 시퀀스다.
**초과의 조건은 스킵이 항상 1클릭·1키로 가능할 것** — EL-RES-002·Esc.

### 기각

| 대상 | 기각 사유 |
|---|---|
| 대시보드 위젯 등장·막대 grow | **게이트 4(기능)** — 사용자가 읽는 정보 밀집 UI. 데이터가 스스로 움직이면 판독을 방해한다 |
| 히트맵 168셀 스태거 | 위와 동일 + 셀 수가 과다 |
| 랜딩 히어로 장식 모션 | 게이트 2 — 목적을 Feedback·State 중 무엇으로도 못 부른다 |
| 대시보드 앵커 스무스 스크롤 | 네이티브 `scroll-behavior`로 충분. JS 불필요 |

## 구현 규칙 (Day 11~12 착수 시)

- **연출은 카드 바깥 스테이지에서.** `stat-card.tsx`는 share-sheet의 `toBlob` PNG 캡처 대상이라 서브트리에 opacity/transform이 남으면 저장 이미지에 중간 프레임이 박힌다. 또한 서버 컴포넌트다 — 무수정 유지
- **`position: fixed` 오버레이 미사용.** 스테이지를 h1 아래 문서 흐름(`min-h-[60svh]`)에 두면 z-index·스크롤 락·포커스 트랩·backdrop 4개가 전부 불필요해진다
- **카운트업은 rAF + ref의 `textContent` 직접 갱신.** 프레임마다 React 렌더 금지(60fps). 표기는 `src/lib/format.ts`의 `num`·`duration` 재사용 — 카드와 표기가 갈리면 안 된다
- **등급 릴은 JS 인덱스 순환.** CSS `steps()`는 등간격이라 감속이 안 되고, 감속이 슬롯 느낌의 핵심이다. 마지막 스텝이 반드시 판정 등급에 착지해야 한다(음수 모듈로 주의) — 테스트로 고정할 것
- **릴 폭 고정 필수.** 등급이 `S·A+·A·B+·B·C`로 1~2자 혼재라 폭이 흔들린다. `tabular-nums`는 숫자 전용이므로 컨테이너에 고정 폭을 준다
- **타이머 핸들은 rAF 하나만.** `setTimeout(finish, total)`을 따로 두지 않고 루프가 `t >= total`을 판정 — 정리 대상이 하나면 새는 경우도 하나뿐
- **스킵·자연 종료는 같은 `finish(skipped)` 경로.** 중복 발화 가드 필수 — `reveal_completed`는 깔때기 지표다
- **행 진입 애니메이션에 `animation-fill-mode: both` 필수.** `animate-in`만 붙이면 fill-mode가 `none`이라 delay 동안 초기 상태가 적용되지 않아 **"0개 / 0개 / 0"이 0ms부터 보인다.** 카운트업 연출이 통째로 무의미해진다 (2026-08-28 실측)
- **`reelStep`의 하한 clamp 필수.** 릴 구간 전에도 rAF는 돌고 있어 `elapsed`가 음수로 들어온다. 새면 음수 스텝 → `GRADE_REEL[음수] = undefined` → 릴이 빈칸으로 보인다 — 테스트로 고정
- rAF는 백그라운드 탭에서 멈춘다 → 탭을 옮기면 연출이 일시정지했다 이어진다. **연출은 봐야 의미가 있으므로 의도된 동작**

## LCP — 연출이 예산을 밀지 않게 하는 조건 (2026-08-28 실측, 이슈 #3)

LCP는 "지금까지 그려진 것 중 가장 큰 콘텐츠 요소"를 추적한다. 텍스트의 경우 크기는
요소 박스가 아니라 **글자가 실제 차지하는 영역**(`PerformanceEntry.size`)이다.

| 요소 | size | 비고 |
|---|---|---|
| h1 "오늘의 성적표" | 14,616 | 초기 LCP |
| 카드 h2 "에러의 늪 탐험가" | 17,516 | 연출 종료 후 등장 → **LCP를 가져간다** |
| 빌드업 `text-sm` | 808 | 후보 미달 |
| 빌드업 `text-4xl md:text-5xl` | 10,279 | 여전히 h2보다 작다 |
| **빌드업 `text-6xl md:text-7xl`** | **22,188** | h2 초과 → LCP 고정 |

빌드업이 `text-sm`이던 동안 `entry=cli`의 LCP는 **3484ms**였다(프로덕션 빌드).
카드가 연출 종료(3030ms) 후 마운트되므로 **LCP가 연출 길이에 종속**됐던 것이다.
빌드업을 display 스케일로 올린 뒤 **52ms**로 떨어졌다.

**규칙: 스테이지에 카드 h2보다 큰 텍스트가 하나는 있어야 한다.** 연출 길이를 늘리거나
카드 타이포를 키우면 이 조건을 다시 검산할 것.

측정값 (프로덕션 `next start`, 수정 후):

| 경로 | LCP | 60fps |
|---|---|---|
| `entry=cli` (3030ms) | 52ms | 중앙값 16.7ms · 17ms 초과 1/216 |
| `entry=link` (1250ms) | 48ms | — |
| 부분 모드 | 48ms | — |
| reduced-motion | 248ms | 스테이지 미마운트 |
