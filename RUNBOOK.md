# RUNBOOK — 장애 대비

> PLAN Day 15~16 "장애 대비 3종"(롤백 / 백업 정책 / 행동 3줄).
> 런칭(9/8) 이후 장애가 나면 **이 문서만 보고** 대응한다. 원인 분석은 복구 다음이다.

---

## 1. 장애 시 행동 3줄

1. **확인** — `scored.kr` 직접 접속 + [Sentry 이슈](https://sentry.io) 신규 확인. 재현되면 장애로 확정한다
2. **되돌린다** — 직전 배포로 롤백(§2). **고치려 들지 말고 먼저 되돌린다** — 롤백은 30초, 수정은 미지수다
3. **알린다** — 런칭 글(GeekNews·디스콰이엇·X)에 댓글 한 줄로 상태를 남긴다. 복구 후 같은 자리에 완료를 남긴다

> 장애 판정선: **랜딩·`/report` 중 하나라도 흰 화면이거나 5xx**. 그 외(문구 오류·레이아웃 깨짐)는 장애가 아니라 다음 배포로 고친다.

> ⚠️ **알림을 믿기 전에**: 배포 이벤트의 Sentry environment는 `production`이 아니라 **`vercel-production`**이다. 알림 규칙의 environment 필터가 `production`이면 실오류가 들어와도 메일이 오지 않는다 — 이슈는 쌓이는데 알림만 조용한 상태라 장애를 늦게 안다. 규칙은 `All Environments`로 둔다 (2026-09-02 실측으로 규명).

---

## 2. 롤백 절차

### 웹 (Vercel) — 30초, 재빌드 없음

```bash
vercel ls                      # Status=Ready·Environment=Production 인 직전 배포 URL 확인
vercel rollback <직전배포URL> --yes
vercel rollback status         # 완료 확인

# ▼ 장애가 해소되면 반드시 실행 — 안 하면 이후 배포가 조용히 안 올라간다
vercel promote <최신배포URL>   # 도메인을 최신으로 옮기고 자동 할당을 다시 켠다
```

빌드를 다시 하지 않고 별칭만 옮기므로 수십 초면 끝난다. 배포 이력은 13건 보존 중(2026-09-01 확인).

🚨 **롤백은 자동 도메인 할당을 끈다 — 반드시 `promote`로 되돌려라.** 롤백 상태에서는 main에 푸시해도 `scored.kr`이 바뀌지 않는다. **빌드는 성공하고 배포 상태도 `● Ready`로 뜨는데 도메인만 옛 배포에 고정**되므로, 성공 로그만 보면 절대 알아챌 수 없다. 2026-09-02 리허설 후 실제로 이 상태에 빠졌고 **`Ready` 배포 5건이 6시간 동안 라이브에 못 올라갔다**(당시 변경이 전부 문서라 실피해는 0). Vercel 대시보드에 `To undo the rollback promote to production or re-enable auto-assigning custom domains` 배너가 뜨는 것이 유일한 신호다. **점검법**: `vercel inspect https://scored.kr`의 배포 id가 `vercel ls --prod` 맨 위와 같은지 본다.

⚠️ **환경변수가 원인이면 롤백은 답이 아니다.** `NEXT_PUBLIC_*`은 빌드 시점에 번들로 구워진다(Day 14 실측 — DSN이 청크에 박힌 것을 확인). 값을 잘못 바꿔 생긴 장애는 **env를 고치고 재배포**해야 풀린다.

### CLI (npm) — 되돌리기가 아니라 앞으로 밀기

npm은 **발행된 버전을 덮어쓸 수 없고**, 72시간이 지나면 unpublish도 막힌다. 그래서 롤백 = 새 패치 발행이다.

```bash
npm deprecate @jong-yeon/scored@<불량버전> "결함 있음. 최신 버전을 쓰세요"
cd cli && npm test && npm run build && npm publish   # 직전 정상 코드로 패치 버전
```

`npx @jong-yeon/scored`는 항상 `latest`를 받으므로 **패치를 발행하는 순간 신규 사용자에게는 롤백이 끝난다.**

### 롤백으로 못 고치는 것

| 대상 | 이유 | 대응 |
|---|---|---|
| 환경변수 오설정 | 빌드에 구워짐 | env 수정 → 재배포 |
| 도메인·DNS | 배포와 별개 | Vercel 도메인 설정 |
| GA4·Sentry 장애 | 외부 서비스 | 대기. 둘 다 없어도 제품은 동작한다 |
| **Hobby 한도 초과 정지** | 플랜 문제 | **Pro 업그레이드 또는 대시보드에서 수동 재개**(↓) |

⚠️ **`503 DEPLOYMENT_PAUSED`가 보이면 롤백하지 마라 — 플랜 문제다.** Hobby는 한도를 넘으면 배포가 일시정지되고 방문자에게 이 코드를 뱉는다. **자동으로 안 풀린다** — 대시보드에서 프로젝트를 직접 재개해야 하고, 원칙은 30일 대기다. 롤백은 아무 효과가 없다(어느 배포로 가도 정지 상태). 즉시 복구가 필요하면 **Pro 업그레이드가 유일한 수단**이다. 여유는 크다 — 월 100GB에 첫 방문 1회 약 300KB(2026-09-02 실측: HTML + 자산 13개) → **월 30만 방문 상당**이라 런칭 규모에서 닿지 않는다. (2026-09-02 [Fair Use Guidelines](https://vercel.com/docs/limits/fair-use-guidelines) 확인)

---

## 3. 백업 정책 — **해당 없음 (N/A)**

백업할 상태가 없다. 근거:

- **서버 저장 0** — DB·인증·서버 상태 전무 (ARCHITECTURE.md). 동적 OG `/api/og`도 무DB 엣지 함수다
- **사용자 데이터는 서버에 도달하지 않는다** — 페이로드는 URL 해시에만 존재하고 해시는 전송되지 않는다 (BR-004)
- **localStorage 미사용** — 브라우저에도 잔존 데이터가 없다
- 영속 자산은 **소스 코드(GitHub)와 npm 패키지(레지스트리 불변)** 둘뿐이고, 양쪽 다 이미 외부에 복제되어 있다

→ **복구 = 재배포.** 별도 백업 잡을 두지 않는다.

### 다만, 복구 불가 자산 2건

백업 대상은 아니지만 잃으면 되돌릴 수 없는 것:

| 자산 | 왜 복구 불가 | 잃었을 때 |
|---|---|---|
| npm 발행 버전 | 덮어쓰기 불가 | 새 패치 버전으로만 해결 (§2) |
| `SENTRY_AUTH_TOKEN` | Vercel Sensitive라 되읽기 불가 | Sentry에서 재발급 후 재등록 |

---

## 4. 롤백 리허설

- [x] **실시 완료 (2026-09-02)** — 4단계 전부 정상. 롤백은 실제로 작동한다.

절차: 직전 배포로 롤백 → `scored.kr` 200 확인 → 현재 배포로 다시 롤백 → 200 재확인.

| 단계 | 대상 | 결과 |
|---|---|---|
| baseline | `scored-7pc2ygcid` (`dpl_5cYrdXTLeTPCqRWACv2hZFEiMNWn`, 커밋 `cefeedb`) | 200 |
| 롤백 | `scored-c3w8q08zh` (커밋 `9f82f5a`) | 200 |
| 롤포워드 | `scored-7pc2ygcid` | 200 |
| 복귀 확인 | `vercel inspect https://scored.kr` → `dpl_5cYrdXTLeTPCqRWACv2hZFEiMNWn` | 원래 배포로 복귀 |

두 배포의 차이가 **문서 커밋 1개뿐**이라 화면 변화 0인 상태에서 절차만 검증했다 — 리허설은 화면이 바뀌지 않는 창에서 하는 게 맞다. 실제 장애 시엔 §2의 명령이 그대로 쓰인다.
