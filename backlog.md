# backlog.md

> Day 2 스코프 확정 이후 모든 추가 아이디어는 여기로. 판정일(런칭+14일) 전 부활 금지.

## 기능

- 주간/월간 리포트 — "지난달보다 삽질 N% 감소" 성장 추적 (재방문 엔진, v2 최유력). **근거 보강 2026-09-03 (Day 13 검증 세션)**: 제작자 본인이 전 플로우 완주 후 자문 ③ *"다시 올 이유가 화면에 있나"* 에 **"없다"** 고 답했고, 필요한 것으로 **기록 축적·조회**를 지목했다. 판정일 재방문율이 낮게 나오면 **원인 가설 1순위**로 이 항목부터 검증한다(원인 재탐색 불요). 복귀: `PLAN.md` 출시 후 백로그 표의 "계정·기록 동기화 | 재방문율 확인 후"와 같은 게이트
- 연말 결산(Wrapped) 이벤트 모드
- 프롬프트 코칭 — 재시도 많은 구간 분석 → 개선 제안 (AI 결합·유료화 후보)
- 삽질 아카이브 → TIL 자동 추출
- 채용 자산화 — "AI 협업 능력 증명서" 리포트
- 타 도구 지원 — Codex·Cursor·Gemini CLI 포맷 파서 추가
- 로컬 MCP 서버 (`scored mcp`) — Claude Code 안에서 "오늘 성적표" 툴 호출 (AX 축; stdio JSON-RPC 수작업 구현으로 의존성 0 유지, CLI "옵션 없음" 스펙 변경이라 make-prd 절차 필요. 대안: Claude Code 스킬 배포가 더 저비용 — 판정 후 비교 선택. 복귀: 판정 통과 후)
- 대화 내용 원문 뷰어 — 세션 브라우징 대시보드 (URL 해시 용량·프라이버시로 v1 제외, 로컬 뷰 방식 재설계 필요)

## 개선

- **CLI 출력 서식** — 현재 평문 5줄(`cli/src/copy.ts` CPY-CLI-001~005). 색·정렬로 첫인상을 개선할 수 있다(2026-09-03 Day 13 검증 세션 사용자 제기). **제약**: 의존성 0 유지 규칙상 `chalk`·`ora` 불가 → ANSI 이스케이프 직접 + TTY 감지 필수(파이프·CI에서 코드 노출). `main.test.ts:54`(URL `startsWith`)·`:61`(버전 `assert.equal`) 두 줄은 색 금지. **비용**: `0.1.2` 재발행 시 DoD "npm publish + 수동 완주"가 검증 버전과 어긋나 재검증 필요. 복귀: 런칭 후, 또는 버퍼 9/6~9/7
- 문의·피드백 채널 — 현재 정성 피드백 회수구 0개(`mailto`·문의 링크 0건, GitHub 링크 2곳은 "코드가 곧 증명" 신뢰 장치라 문의 창구 아님). 에러 화면에도 없어 사용자가 장애를 알릴 수단이 없다. 후보: GitHub Issues 링크(타겟이 개발자·의존성 0·서사 일관, 최유력) / `mailto:` / 구글 폼. 새 EL-·CPY- 추가라 make-prd 절차 필요. ⚠️ **PLAN Day 15~16은 이 항목을 "피드백 채널 심기" 필수로 잡고 있었다** — 2026-08-31 사용자 결정으로 backlog행. 외부 눈 0명 결정과 겹쳐 **판정일(9/22) 근거가 GA4 정량 지표뿐**이라는 점은 감수 사항 (sentry 연동은 별개 항목으로 잔존 — 기계 관측 경로는 살아 있음)

## 컷된 화면

## 컷된 재미 장치

- VS 카드 — 친구 성적표 1:1 비교 (재미 장치 1개 규칙으로 v1 제외)
- 유형 도감·수집 / 랭킹 (Day 1부터 backlog 유지). **2026-09-03 Day 13 검증 세션에서 재제기 → backlog 유지 확정.** 랭킹은 서버에 기록을 축적해야 성립하므로 **무업로드 원칙(CLAUDE.md·14 §보안)과 정면 충돌**하고, 재미 장치 1개 규칙에도 걸린다. 축적이 필요하면 **localStorage 경로**가 먼저다(PLAN Day 2 원문: *"기록 보존이 필요하면 v1은 localStorage, 계정은 backlog"*) — CLI가 이미 `~/.scored/`에 7일 롤링으로 남기고 있어 로컬 축적의 절반은 존재한다. 복귀: 판정 통과 후, 인터뷰 데이터로 우선순위 결정

## 발동 조건부 항목

- 브라우저 드래그&드롭 진입 부활 — 발동 조건: npx 진입 깔때기 손실로 생성 완료율(결과 도달률) 부진 시
- `llms.txt` — Lighthouse 신규 **Agentic Browsing** 카테고리의 유일한 실패 항목(전 화면 67점). AI 에이전트가 사이트 구조를 읽게 하는 규약이고 타겟이 AI 코딩 사용자라 서사는 맞지만, PLAN·PRD 범위 밖이며 v1 스코프 동결(00 §버전 규약 — v1.x는 치명 버그만) 대상이다. 접근성·SEO·Best Practices는 이미 100. 판정 통과 후 재검토 (2026-09-02 Day 17 사전 감사에서 발견)
