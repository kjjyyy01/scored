# 작업 히스토리

## 2026-08-13 — Day 2 MVP 기획서 초안 (grill 대기)

- 무엇을: MVP 기획서 초안(docs/MVP.md) — SCR 5개 + 범위 판정 3종 + JSONL 파싱 스펙
- 어떻게: 로컬 `~/.claude/projects` 실데이터 263개 파일 샘플링으로 레코드 구조(type 분포·usage·tool_use·is_error) 검증 후 추출 지표 8종 확정. SCR은 랜딩/공개연출/성적표/공유카드/처리안내 5개로 압축, Day 9 컷 우선순위 명시
- 왜: 더미 스펙이 아닌 실데이터 기반 파싱 스펙 — 포맷 리스크(Day 1 인계)를 관대한 파싱·지표별 독립 실패로 대응
- 결과: 범위 판정 — 백엔드 無(예외: @vercel/og 엣지 함수 1건, 컷 1순위) / 콘텐츠 제작 Day 7 배정 / 외부 API 無 / 비로그인 자동 충족. 잔여: /grill-me 스코프 방어 → 기획서 확정
- **[grill 완료]** Day 2 grill 3라운드(Q1~Q11) 종료 — 전 문답 원문·선택 이유를 docs/grill-log.md에 기록 (Day 1 도전장·살까말까·물줬어?·바이브 성적표 grill 원문도 세션 트랜스크립트에서 복원해 수록)
- **[v3 확정 — Day 2 종료]** grill 반영: 유형 8→6종 / 발췌 웹 승인 방식(replaceState+공유 토글) / 킬 크라이테리아 데스크톱 모수 개정(도달률 20%·공유 10%·표본 150) / Day 9 컷 순위에 대시보드 화면 전체 추가 / SCR-1에 "명령어 나에게 보내기" / 레포 public(kjjyyy01/vibe-scorecard) + git 이력 시크릿 스캔 통과 / npm 이름 2종 미등록 확인. 잔여: 도메인 결제(사용자, 즉시). EOD 완료(Notion 과정요약 1건·Obsidian TIL 2건, 8/13)
- **[v2 개정]** 사용자 검토 2건 반영: ① 대시보드 추가(토큰 추이·활동 히트맵·도구 분포 — 실용 축, SCR 5→6) ② 진입 방식을 드래그&드롭에서 `npx vibe-scorecard` CLI로 전환 — CLI가 `~/.claude` 자동 탐색·로컬 분석 후 집계 JSON을 URL 해시로 전달(서버 전송 0 유지, 공유 링크 공짜 획득). 원문 대화는 URL 미탑재(용량·프라이버시), 발췌 하이라이트만 grill 확인 대상. 미채택 확장(주간 리포트·VS 카드·타 도구 지원 등)은 backlog.md 기록

## 2026-08-11 — Day 0 사전 세팅

- 무엇을: 시작일 역산, 필수 문서 5종(CLAUDE/DESIGN/ARCHITECTURE/ANIMATION/TODO) + backlog.md 작성
- 어떻게: PLAN.md Day 0 체크리스트 기준. 미정 항목은 "(Day 1 확정 후 기입)"으로 남기고 고정 규칙만 기입
- 왜: 아이디어 미정 상태에서 가상 서비스 상정 금지 — 문서는 뼈대만, 값은 Day 1 판정 후
- 결과: Day 0 = 8/12(수), Day 20 런칭 = 9/1(화). 계정·결제·플러그인 토글 항목은 사용자 작업으로 분류 (TODO.md 참조)
- 추가: /setup-matt-pocock-skills 완료 — 이슈 트래커 GitHub Issues 선택, docs/agents/(issue-tracker.md·domain.md) 생성, CLAUDE.md에 Agent skills 블록 추가. triage 스킬 미설치로 라벨 설정 생략
- 추가: Next.js 스캐폴드(create-next-app 기본값, 빌드·정적 프리렌더 확인) + git init + GitHub private 리포 push. 플러그인 정리는 기완료 상태 확인. Obsidian dev-notes/ + TIL 템플릿 생성. 잔여: Vercel 로그인·GA4·도메인·비용 상한·Notion DB (사용자)

## 2026-08-12 — Day 1 아이디어 발굴 (진행 중) — 탈락 기록

- 무엇을: 브레인스토밍 방식으로 아이디어 발산·수렴 진행. 후보 2개를 검증까지 끌고 갔다가 1개 탈락, 1개 보류
- 어떻게: 백지 발산 대신 문답으로 좁힘 (일상 마찰 영역 → 공유 대상 → 핵심 행동 → 재미 장치 순). 검증은 PLAN.md 기준(직접 경쟁 3개·차별점 1줄·리뷰 불만 1개) + /grill-me 스트레스 테스트
- 탈락 ①: **도전장** (친구 1:N 일회성 미션 + 사진 인증 + 전적) — 시장 검증은 통과(챌린저스·Betcha·ChallengeM8 존재 = 수요 증명, "무설치 링크" 차별점 유효)했으나 grill에서 붕괴. 사유: 스트라이크 1 = "내가 주 3회 이상 쓰는가" 본인 부정, 스트라이크 2 = 보완안(내기 프레임)의 원본 행동(최근 1달 내 친구와 내기)이 부재. 교훈: **행동 증거 없는 아이디어는 프레임 보완으로 살아나지 않는다**
- 보류 ②: **살까말까** (충동구매 쿨다운 + 아낀 돈 누적) — 행동 증거 통과(주 3회+ 실재), 시장 검증 통과(Why Buy 등 카테고리 존재하나 국내 웹 빈 자리, 강자 부재), 스코어 19/20. grill 3문항(재방문 구조/유입 재판정/축 유지) 답변 대기 중 사용자가 재시작 선언
- 재시작 사유: "확신이 안 선다"(C 유형) — 확신은 Day 1 산출물이 아니라 Day 13 유저 테스트·판정일의 산출물임을 확인하고도 재시작 선택. 합의 규칙: **다음 라운드 승자는 느낌이 아니라 증거·스코어로 확정**
- 부수 사건: Day 1 기록 전체 삭제(claude-mem 포함)를 요청했다가 실행 직전 철회 — 탈락 과정도 구직 자산이자 차순위 교체 대비 자료로 보존하기로 결정 (PLAN.md Day 1 마일스톤 기록 요구사항과 일치)
- 결과: Day 1 미완 — 최종 확정 없음. 차순위 풀: 살까말까(19점), 뭐 입지, 먹은 것 기록, 오늘의 미션 배틀·주간 리그(도전장 변형, 동일 사유로 사실상 사망). Day 1은 최대 2일 규칙상 8/13이 마지막 날, 8/14는 Day 2 강제 진입

## 2026-08-12 — Day 1 최종 확정: 물줬어?

- 무엇을: 서비스 최종 확정 — **물줬어?** (링크로 초대한 둘이 함께 키우는 디지털 화분. 각자 하루 1회 물 주면 성장, 둘 다 방치하면 시듦)
- 어떻게: 선정 기준 자체를 전환 — 시장 증거 우선(살까말까 등 생존자 3개 전부 기각됨)에서 **본인 끌림 우선**으로. 결(친구·커플과 공유하는 감성 공간)을 잡은 뒤 카탈로그 20개 제시 → 15번 디지털 화분에 끌림 → 검증 통과 → 축소 grill → 확정
- 왜: 3주 솔로 스프린트에서 "만들면서 신나는가"는 생존 조건. 증거 우선 후보들은 커밋 직전마다 기각되는 패턴 4회 반복 — 킬 크라이테리아가 하방을 막으니 끌림에 베팅
- 검증: 수요 증명 — Widgetable(강자·한국어)·Pookie·Petiverse·FurTwo 등 커플 펫/식물 위젯 카테고리 성장 중. 불만 — 광고(먹이 1회에 광고 6~8개)·과금이 감성을 죽임. 차별점 — "설치 없이 링크 하나, 광고 없이 화분 하나"
- 산출물 5: ⓪한국어 ①킬: 2주 후 화분 7일 생존율 20% 미만 또는 초대 수락률 50% 미만 시 피벗(표본 화분 50개 미달 시 보류) ②축: 재미 손맛 — 속도 승, 물주기·성장·시듦 순간만 연출 승 ③유입: 공유·입소문(초대 링크=제품 시작점, 공유 트리거 SCR 1) ④플랫폼: 웹 확정(오늘 앱 전환 논의 2회 발생 → 최종 봉인, 앱+위젯은 판정 통과 후)
- 알려진 리스크(정직 기록): ① 위젯 없는 웹의 물주기 리텐션 — 시듦 긴장감+상호 리마인드로 완화, Day 13 관찰 1순위 ② 본인의 화분 파트너 미확보 — Day 11 테스터 섭외 최우선 항목 ③ 둘 공유 상태 = 백엔드 필수 유력 → Day 2 판정
- 결과: 서비스명 "물줬어?" 확정. 도메인 후보 muljwo.com·mulju.kr 미등록 확인(구매는 사용자). Day 1 종료 — Day 2(8/13) MVP 기획서 진입
- **[당일 저녁 번복]** 확정 직후 "확 끌리지 않는다 + 나라면 안 쓸 것 같다"로 기각 — 아래 최종 확정 참조

## 2026-08-12 — Day 1 진짜 최종 확정: 바이브 성적표

- 무엇을: **바이브 성적표** — `~/.claude` 세션 데이터를 브라우저에 드래그하면 로컬 분석으로 재미 성적표 생성 (개발자 유형, "미안하다" 카운트, 최장 삽질, 새벽 코딩 지수) + 공유 카드. 무업로드·비로그인·무백엔드
- 어떻게: 물줬어? 기각 후 강제 결정 필터 2종(후회·만드는 재미)을 파이널리스트 3개에 적용 → **전부 무신호** = 기존 풀 전체에 끌림 부재 확정. 그 직후 사용자가 claude-mem DB 탐색 경험에서 **자발적으로** "내 프롬프트 데이터로 뭔가 만들자" 제안 — 오늘 유일하게 증거(본인이 매일 쓰는 도구)와 끌림(자발 제안)을 동시 통과. 형태 3안 중 A(성적표) 선택
- 왜: 하루 종일 제시된 카탈로그 32개는 전부 커밋 직전 기각 — 끌림은 메뉴에서 고르는 게 아니라 만지작거리다 발견된다는 실증. 데이터를 직접 뒤지던 경험이 씨앗이 됨
- 검증: 수요 증명 — ccusage(토큰·비용 CLI 강자), Claude Code Wrapped·Year in Code·Code Card(영어권 연말 결산 카드들). 빈 자리 — ① 한국어(말투 유머는 언어 종속이라 영어 도구가 못 따라옴) ② 프롬프트 "내용" 재미 분석(경쟁은 숫자만) ③ 연말 이벤트가 아닌 상시
- 산출물 5: ⓪한국어 ①킬: 2주 후 방문자 중 성적표 생성 완료율 20% 미만 또는 생성자 중 공유·저장률 10% 미만 시 피벗(방문 300 미달 보류) ②축: 재미 손맛 — 속도 승, 성적표 공개 순간만 연출 승 ③유입: 공유·입소문(공유 카드=유입 엔진, GeekNews·디스콰이엇=타겟 서식지) ④플랫폼: 웹(파일 드래그+로컬 처리라 필연)
- 알려진 리스크: ① 파일 드래그 마찰 → 킬 지표 1번으로 측정 ② Claude Code 데이터 포맷 변경 가능성 ③ 니치 시장(대신 타겟 밀도 최고) ④ 서비스명·도메인에 "Claude" 금지(상표)
- 결과: 서비스명 "바이브 성적표", 도메인 vibescore.kr 미등록 확인(결제는 사용자). Day 1 종료(6회 방향 전환 끝) — Day 2(8/13): SCR 5~7 + JSONL 파싱 스펙

## 2026-08-14 — Day 3 PRD + GA4 이벤트 스펙 (초안 완료, 승인 대기)

- 무엇을: MVP.md v3를 make-prd 규격 PRD로 변환 — docs/prd/ 20문서 (SSOT 14: 공통규약·용어·정보구조·데이터모델·해시규약·API·도메인규칙·에러·카피·보안·분석·DoD·범위외·OQ / 화면 6: SCR-001~006 각 16섹션·G/W/T 수용기준·테스트 케이스)
- 어떻게: make-prd 스킬(spec-common v1.0) 준수 — 값은 SSOT에만, 화면 문서는 ID 참조만. F- ID는 MVP.md에 없어 04_정보구조에서 F-001~007 부여 승계(MVP.md 동결 유지). GA4 스펙은 pm-data-analytics 부재로 15_분석및추적.md에 직접 작성: 커스텀 EVT 9종(npx 복사·결과 도달·연출 완료·공유 3종 등) + 자동 수집 갈음 4종(재방문=client_id, 데스크톱 한정=device_category) + 킬 크라이테리아 계산식 매핑
- 왜: PLAN Day 3 요구(화면 스펙 + EVT 스펙 동시 확정, 재미·공유·재방문 이벤트 필수). 판정 지점(BR)을 코드보다 먼저 문서로 고정해 Day 4~9 구현이 스펙만 보고 진행되게 함
- 핵심 결정: ① 유형·등급 판정은 웹(BR-001 — CLI 재배포 없이 문구·규칙 수정) ② 발췌 승인 흐름 BR-004(주소창 즉시 제거+명시 토글) ③ GA4 파라미터에 성적 데이터 금지(14 §5) ④ /r는 단일 라우트 상태 머신(연출→성적표⇄대시보드)
- 결과: 정합성 검사 통과(ID 무결성·P0↔테스트·값 복제 없음. 예외: 화면↔API — 무백엔드 해당 없음). 화면 문서 status=Draft — 사용자 승인 후 Approved 전환이 구현 게이트. OQ 5건 등록(유형·등급=Day 7, 페이로드 실측=Day 4~5, 도메인·GA4 ID=사용자)
- **[추가 2026-08-15] 도메인 이름 통일**: Day 1 추천 vibescore.kr → **vibe-scorecard.kr**로 변경. 사유: Day 2에 진입이 `npx vibe-scorecard`로 확정되며 npm·레포명(vibe-scorecard)과 도메인(vibescore)이 갈라짐 — 신뢰 장치(이름 일치)를 위해 통일. B안(`vibescore` 전면 통일)은 서비스명 직역 유지 이유로 기각. MVP.md URL·PRD 06·OQ-003·TODO 반영. 결제는 사용자(Day 4 전)
- **[추가 2026-08-15] 도메인 최종 변경 `scored.kr`**: GitHub 레포명이 `vibe-scorecard` → `scored`로 바뀌어 위 통일 근거가 깨짐 → 도메인을 `scored.kr`로 최종 확정·구매 완료. remote·claude-mem DB 프로젝트명도 `scored`로 정리. 잔여: Vercel 프로젝트 연결·DNS, npm 패키지명(`scored` 미등록) 재결정
- **[추가 2026-08-15] PRD 피드백 반영 + 갭 보강**: ① 00에 버전 규약(v1/v1.x/v2) ② 라우트 `/r`→`/report` (공유 URL 의미 전달) ③ shadcn/ui 채택 — DESIGN.md에 화면 명칭↔컴포넌트 매핑표 ④ 갭 1·4·5 보강: `CLI-001_분석기.md` 신설(F-007 입력 계약·집계 규칙·중복 제거·타임존·Windows PowerShell 오픈·REQ-CLI-001/002·TC 10건), OQ-006(P0 콘텐츠 작성 시점), 19 DoD에 전역 에러 바운더리(ERR-APP-001)·CLI 테스트 추가, ERR-CLI-003·CPY-CLI-001~003·CPY-ERR-008/009 등록. 도메인 `scored.kr` DNS·SSL 연결 확인(200). 잔여 결정: npm 패키지명(`scored` 미등록), BR-001 부작용(공유 링크 판정 사후 변경·OG immutable 캐시), DESIGN.md 토큰 4항목·다크모드
- **[추가 2026-08-15] 운영 교훈 → PLAN 운용 원칙 4 신설**: 위 PRD 수정을 make-prd 없이 수동 편집한 사례 → "구간별 지정 스킬은 작성뿐 아니라 수정·재검토에도 필수" 명문화. 이후 make-prd 규격(§1 프론트매터·§2 ID 정규식·§8 정합성)으로 재검증 — ID 미정의 참조 0건, CLI-001은 규격 §1 확장(`PRD-CLI-001`)으로 00에 명시, P0 REQ/AC 4종 보강, 값 복제 2건(BR-005 패턴·05 크기 상한) SSOT로 이관
- **[추가 2026-08-15] 남은 결정 2건 처리**: ① npm 패키지명 **`scored`** 확정(`npx scored`) — CPY-LAND-002·CPY-ERR-001/002·10·19·04·SCR-001·CLI-001·MVP 치환(문구 교체 — 운용 원칙 4 예외 범위), 루트 package.json은 `scored-web`(미발행, cli/ 패키지와 이름 충돌 방지) ② DESIGN.md 토큰 기입 — sequential-thinking 5단계로 결정: Pretendard Variable(next/font/local, swap)+Geist Mono, 타이포 역할 5개, 스페이싱 허용 단계 8개·컨테이너 max-w-3xl/5xl, shadcn base **stone** + `--primary` 1색(오렌지 후보, Day 4 실물 확정), 등급 색 없음, 다크모드 시스템 추종만. 잔여 결정: BR-001 부작용(공유 링크 판정 사후 변경) 처리 방식
- **[추가 2026-08-16] BR-001 부작용 결정 = 수용+운영 규칙**: 웹 판정이라 공유 링크의 등급이 사후 변경될 수 있는 문제 → ⓑ(판정 버전 고정)는 옛 규칙 영구 보관 비용으로 기각. ⓐ 채택: 08 BR-001 비고(규칙 변경은 v2 이후, v1.x는 문구만; 필요 시 `v` 상향+ERR-HASH-002 재생성 안내), CPY-COM-003 고지 신설, SCR-006 EL-HOW-005 추가. OG 캐시는 07을 `immutable`→`s-maxage` 1일+SWR로 수정(크롤러가 호출하므로 TanStack Query 등 클라이언트 캐시는 무관). 남은 결정 0건 — 잔여는 SCR-004 프롬프트 구성요소 범위 확인·화면 문서 승인
- **[추가 2026-08-16] PLAN 일정 조정 1회 (make-plan Step 7 확장)**: Day 3가 8/14→8/16으로 2일 지연(PRD 전체 피드백·갭 보강·결정 3건·도메인 전환으로 범위 확대). 프로토콜 기본인 지렛대 0(버퍼 소모) 대신 사용자 결정으로 캘린더 +2일 확장 — 실작업일 21 불변, Day N≥4 = 8/13+N, **Day 20 런칭 = 9/2(수)**(화~목 자연 충족, 원래 요일 보정용 Day 21 하루가 버퍼로 회복), 판정일 9/16. 배분·스코프·킬 크라이테리아 불변. PLAN 상단에 일정 매핑·조정 이력, TODO 헤더, 00 버전 규약 기간 갱신. 정합성: 고정 하한 유지·backlog 존재·실패 조건 12항·런칭일 수요일 ✓
- **[추가 2026-08-16] PRD 핵심 검토 8항 중 5항 반영 (3·4·5·7·8)**: ③ 하이라이트 = 명대사 발췌 → **최다 사용 문장 ≤3(반복 횟수)·단어 ≤10** 빈도 집계 — 05 `highlights.sentences/words`, BR-005, CLI-001 §4, SCR-003 EL-RPT-004, CPY-SHARE-003, 03 용어, MVP 지표 표 (용어 "발췌"→"하이라이트" 전면 치환) ④ 킬 크라이테리아 오염 방지 — CLI URL `?from=cli`, EVT-RES-001 `entry: cli|link`, 도달률 분자 `entry=cli`만, BR-004가 쿼리도 제거, SCR-005 링크는 쿼리 없음 ⑤⑦ **대시보드(SCR-004)·동적 OG 컷 제외** → P0 승격, 컷 순위 재편(하이라이트→SCR-006 별도 화면→연출 축소), 19 DoD 갱신 ⑧ 브랜드 **scored** (CPY-COM-002, 00 헤더, CLAUDE.md). 부수: REQ-RES-003 AC 4종 보강. 미반영: ① 유형·등급 기준(같이 브레인스토밍) ② 성적표 = 하루 결과(스키마 영향 → grill 후 반영) ⑥ 재미 장치 grill
- **[추가 2026-08-16~17] grill 3라운드(항목 1·2·6) → 제품 재정의 반영 (make-plan: MVP / make-prd: PRD)**: ① **하루 단위** — 대상일 = 마지막 활동일, 하루 경계 로컬 05:00, `prompts<10` 부분 모드(BR-002·BR-011), 7일 컨텍스트(`week.*`), 진행 중 배지 ② **스탯 카드 + 등급 뱃지**(GitHub Readme Stats 은유): 카드 지표 6줄(프롬프트·세션·토큰·활동 시간·최다 도구·최다 문장) + 오늘의 유형 + 등급, 카드=공유 이미지=OG(07 파라미터 재정의, 최다 문장은 원문이라 OG 제외) ③ 등급 = 3지표 고정 임계 커브 6단계 활동량 척도(BR-009, 임계 Day 7 내 로그→Day 13 보정), 오늘의 유형 6종+폴백 초안 확정·매일 변동이 의도(BR-010) ④ `/report` 한 스크롤 페이지 — 탭 제거, EVT-DASH-001=섹션 노출 ⑤ **대화 원문 = 로컬 HTML 리포트**(CLI-001 REQ-CLI-003, `~/.scored/{day}.html`, 세션별 프롬프트+답변, 0600) — 실측: 하루 대화 45k~110k자(URL 예산 8k)라 URL 불가, https→localhost는 Safari/iOS 차단·Chrome 142+ 권한 팝업(1차 출처 확인)이라 로컬 파일로 확정, 20 "원문 표시 영구 제외"를 "공유 가능한 형태만"으로 정밀화 ⑥ 재미 장치 유지 — 연출 = 채점 중→지표 카운트업→등급 릴→카드(3~4초, entry=link 1.5초) ⑦ 재방문 = 카드 문구+CLI 종료 메시지+alias 안내(훅·크론 v2) ⑧ 대시보드 프롬프트 섹션 = 길이 분포·한 줄 비율·자주 쓴 문장 3·단어 10 + 로컬 리포트 안내. 05 스키마 전면 개정(`day/inProgress/tokens{in,out}/activeMinutes/hourly/week/lenBuckets`, `period/activeDays/tokensDaily/heatmap` 삭제), CLI-001 §2~5·TC 7건 추가, ERR-CLI-004·CPY 7건, OQ-001/002 축소·OQ-007 신설. 정합성: ID 미정의 0, P0 REQ AC 4종 전수. CLI 배정 1.5일
- **[추가 2026-08-17] Day 3 종료 — PRD 승인**: SCR-001~006 + CLI-001 `status: Approved`, `version 1.1.0`(하루 단위 개정 반영). PLAN 체크리스트 Day 3 체크, TODO 갱신. 승인일 8/17 = 조정 이력상 Day 4 시작일 → **일정 변동 없음**(추가 조정 불필요, 누적 조정 1회 유지). EOD Notion·Obsidian 기록은 새 세션에서 진행
- **[추가 2026-08-18] Day 3 EOD 기록**: ① Obsidian TIL 5건(`dev-notes/`: HTTPS→localhost fetch Safari 차단 / 판정 규칙 웹 배치 시 페이로드 `v` 버전 / 퍼널 지표 진입 경로 태깅 / 재미 지표 = 빈도 집계+고정 임계 등급 / 레포·npm·도메인 이름 세트) ② `docs/grill-log.md`에 Day 3 grill 원문 수록 — 검토 8항 답, Round 1~3(Q1~Q22), Q19 후속 문답(npx 범위·8시간 실측·Safari 팩트체크→(b) 확정), 합의문 전문, 결과 요약표 (2026-08-17 세션 트랜스크립트 복원) ③ Notion 개발 로그: 과정요약 1건 + 트러블슈팅 3건(Q19 원문 뷰어 / make-prd 우회 정합성 / 일정 조정 1회) 등록 완료 (MCP OAuth 재인증 필요했음 — 토큰 만료 시 `authenticate` → 브라우저 승인)

## 2026-08-18 — Day 4 (계획 8/17, 1일 지연 착수) 세팅 + CLI(F-007) 집계 코어·인코딩·main

- 무엇을: ① 웹 세팅 — shadcn/ui init(`base-nova`·Base UI, baseColor stone, theme orange), Pretendard `next/font/local`(pretendard npm 가변 1파일·swap)+Geist Mono, `lang="ko"`, 다크모드를 `.dark` 클래스→`prefers-color-scheme` 시스템 추종으로 전환 ② `cli/` 독립 npm 패키지 `scored`(의존성 0, `node:test`, TS erasable 소스를 Node가 무빌드 실행·`tsc`로 dist 빌드, 루트 tsconfig에서 제외) — `types.ts`(05 페이로드) · `time.ts`(05:00 경계·IANA tz 주입) · `analyze.ts`(관대한 파싱·필터·message.id 중복 제거·stats/week/fun/highlights) · `files.ts`(readline 스트리밍) · `encode.ts`(deflate-raw→base64url·8,000자 상한 재인코딩) · `open.ts`(darwin/linux/win32 PowerShell) · `copy.ts`(CPY-ID 키) · `main.ts`(DI·종료 코드 반환) · `index.ts`(bin) ③ OQ-004 실측 ④ 문서: ARCHITECTURE(구조 기입)·DESIGN(primary 임시값·대비 소견)·05·21·TODO(Day 4 섹션)·CLAUDE(CLI 명령어)
- 어떻게: sequential-thinking 5단계로 CLI 아키텍처 판단(패키지 배치·무빌드 TS·1패스 압축 보관·tz DI·심 4개) → mattpocock `/tdd` 수직 슬라이스 14사이클(테스트→최소 구현) — CLI-001 §9 TC를 심 합의로 간주, `test/analyze.test.ts`(TC-01~06·11~14 + §3 fun·models)·`encode.test.ts`(TC-07 DecompressionStream 라운드트립·TC-08)·`main.test.ts`(TC-09·10·ERR-CLI-002·--help/--version, 임시 홈 디렉터리·open 모킹) **20 tests pass**. shadcn init은 새 CLI(프리셋 코드) 규격 — `shadcn/preset` `encodePreset`로 stone/orange 코드 생성(chartColor≠stone이면 400)
- 왜: 05 페이로드가 CLI·웹 공용 계약이라 CLI를 먼저 고정(PLAN Day 4 권장). 의존성 0·무업로드는 CLI부터 성립(CLI-001 §1). Pretendard 2MB 단일 파일은 DESIGN 결정대로 — Day 17 LCP 미달 시 dynamic-subset CSS로 교체(`ponytail:` 주석). 후보 primary `oklch(0.70 0.19 45)`는 흰 글자 대비 ≈2.7:1이라 shadcn orange 대비 검증 쌍을 임시 채택 — 확정은 카드 첫 렌더일에 사용자
- 결과: `node cli/dist/index.js` E2E — 실로그 300파일/932MB 2.5s·RSS 280MB, `2026-08-18 세션 4개 · 프롬프트 16개` → URL 1,138자 → macOS 탭 오픈 확인. **OQ-004 완료**: 인코딩 1,104자(하이라이트 제거 880자, 최악 근사 ≈1.1K) → 8,000자 상한 확정. OQ-003 잔여(Vercel·DNS) 완료 확인(HTTPS 200). `next build` 통과. 잔여: REQ-CLI-003 로컬 리포트(Day 5, OQ-007 결정 후) · npm publish(`npm login` 필요, LICENSE 정책 결정) · make-prd 일괄(`<synthetic>` 제외·`week.tokens`=in+out·CPY-CLI-004 임시 분리) · 일정 1일 지연 처리(사용자) · `--primary` 확정(사용자)

## 2026-08-19 — PLAN 일정 재조정 2회차 (make-plan Step 7)

- 무엇을: Day 4 착수 1일 지연(8/17 계획 → 8/18 실착수)을 **캘린더 +1일 시프트**로 재매핑 — `Day N≥4 = 8/14+N` → Day 4 = 8/18(화), **Day 5 = 8/19(수)**, Day 9 = 8/23(일), Day 14 = 8/28(금), **Day 20 런칭 = 9/3(목)**, Day 21 = 9/4(금), 판정일 9/17(목)
- 어떻게: make-plan Step 7(재조정) 진입 — 현재 산정값 확인(실작업일 21, 누적 조정 1회, 버퍼 잔량 Day 13~14 + Day 21 1일) → 확장 프로토콜 적용, 지렛대 0~5 **미적용**(실작업일·페이즈 배분·스코프 불변) → PLAN 상단 일정 매핑·조정 이력 갱신, TODO 헤더·Day 4 헤더, PRD 00 버전 규약 기간(9/3·9/3~9/17) 동기화. 정합성: 고정 하한 유지 · 킬 크라이테리아 표본 조건 유지 · backlog.md 존재 · 실패 조건 12항 · **런칭 요일 9/3(목) 화~목 규칙 충족** ✓
- 왜: 사용자 결정 — 처음 "버퍼 흡수"에서 "재매핑"으로 변경. 지연 1일을 버퍼에서 빼면 Day 13~14 게릴라 테스트·Day 21 대응 여력이 줄어드는데, 이 프로젝트의 버퍼는 첫 유저 접촉 실패 시 유일한 복구 수단이라 캘린더를 늘려 **버퍼를 보존**하는 쪽이 리스크가 낮다. 확장분은 Day 4 지연에 전액 소비 → 배분·스코프·킬 크라이테리아 불변(파킨슨 방지)
- 결과: 누적 조정 **2회**(3회째는 조정 전 전제 재검토 3문항 필수 — PLAN 프로토콜). 실작업일 21일·버퍼 잔량 불변. 남은 실작업 16일, 런칭까지 15일. MVP-기획서·PRD 제품 정의는 불변(지렛대 4·SCR 변경 없음)

## 2026-08-19 — PLAN 일정 재조정 3회차 (make-plan Step 7, 전제 재검토 동반)

- 무엇을: AI Summit Seoul & Expo 2026 참가로 **8/19~8/20 작업 공백** 발생 → Day 5 이후 전 구간 **캘린더 +2일 시프트**(`Day N≥5 = 8/16+N`) — Day 5 = 8/21(금), Day 9 = 8/25(화), Day 12 = 8/28(목), Day 14 = 8/30(일), Day 19 = 9/4(금), **9/5~9/7 = 버퍼**, **Day 20 런칭 = 9/8(화)**, Day 21 = 9/9(수), 판정일 9/22(화)
- 어떻게: make-plan Step 7 진입 → 누적 3회째라 **전제 재검토 3문항 선행**(① 외부 일정 요인·진척 미달 아님 → SCR 6·재미 장치 1 스코프 유지 ② 킬 크라이테리아 조기 발동 불필요, 기준·표본 조건 불변 ③ 구조적 변경 = "확정 외부 일정 선반영" 규칙을 PLAN 상시 규칙에 신설) → 확장 프로토콜 적용, 지렛대 0~5 **미적용**(실작업일 21·페이즈 배분 불변). **런칭 요일 검증**: +2 시프트만 하면 Day 20 = 9/5(토)로 화~목 위반, 프로토콜의 ±1일(9/4 금·9/6 일)로도 복구 불가 → 런칭을 9/8(화)로 이동하고 9/5~9/7 3일은 **버퍼 증액**(확장 우선순위 3)으로 귀속. 동기화: PLAN 일정 매핑·조정 이력·상시 규칙·구간 헤더(Day 4~14 / Day 10~12 / Day 13~14 / Week 3 / Day 20·21 행), TODO 헤더, PRD `00_공통규약` 버전 규약 기간(9/8, 9/8~9/22)
- 왜: 서밋은 대체 불가한 외부 일정이고, 버퍼 소모(지렛대 0)로 흡수하면 버퍼 잔량이 0이 되어 남은 15일을 무보험으로 달리게 된다(이 프로젝트의 버퍼는 Day 13 첫 유저 접촉 실패 시 유일한 복구 수단). 캘린더 연장으로 흡수해 버퍼를 보존하는 쪽이 리스크가 낮다. 시프트 2일은 공백에 전액 소비되고 요일 보정 3일은 버퍼 귀속이므로 배분·스코프·킬 크라이테리아는 불변(파킨슨·backlog 역류 방지)
- 결과: 누적 조정 **3회**, 캘린더 24→29일(실작업일 21 불변). 버퍼 잔량 = Day 13~14 내 버퍼 + 9/5~9/7 3일 + Day 21 1일(증액). 정합성: 고정 하한 유지 · 킬 크라이테리아 표본 조건 유지 · backlog.md 존재 · 실패 조건 12항 · **런칭 요일 9/8(화) 화~목 규칙 충족** ✓. MVP.md·PRD 제품 정의 불변(지렛대 4·SCR 변경 없음). 반복 방지책으로 확정 외부 일정은 확인 즉시 일정 매핑에 선반영

## 2026-08-20 — PLAN 일정 재조정 4회차 (make-plan Step 7, 3차 전제 정정)

- 무엇을: AI Summit Seoul & Expo 2026 참가가 **2일 → 8/19(수) 하루**로 확정 → 8/20이 작업일로 회복. Day 5 이후 시프트를 +2일 → **+1일**로 축소(`Day N≥5 = 8/15+N`) — Day 5 = 8/20(목), Day 9 = 8/24(월), Day 12 = 8/27(목), Day 14 = 8/29(토), Day 19 = 9/3(목), **9/4~9/7 = 버퍼**, Day 20 런칭 = 9/8(화) 불변, 판정일 9/22(화) 불변
- 어떻게: make-plan Step 7 진입 → 회복 1일을 확장 프로토콜 우선순위 3(**버퍼 증액**)으로 귀속, 지렛대 미적용. **런칭 요일 검증**: 단순 -1 시프트 시 Day 20 = 9/4(금)로 화~목 위반, 9/3(목)까지 당기려면 Day 15~19 출시준비를 1일 압축(지렛대 1)해야 해서 사용자 결정으로 미적용 → 런칭 9/8(화) 유지. 동기화: PLAN 일정 매핑·조정 이력·구간 헤더 4곳(Day 4~14 / Day 4~9 / Day 10~12 / Day 13~14 / Week 3), TODO 헤더. PRD `00_공통규약`은 런칭·판정일 불변이라 수정 없음
- 왜: 전제(서밋 2일)가 1일로 정정된 되돌림이라 새 전제 재검토 없이 3차(2026-08-19) 답을 유지 — 새 지연이 아니며 진척 미달도 아니다. 회복분을 스코프·폴리싱으로 흘리지 않고 버퍼로 넣은 이유는 파킨슨 법칙 방지(늘어난 시간이 일을 부풀리는 것) + Day 13 첫 유저 접촉 실패 대비 여력 확보
- 결과: 누적 조정 **4회**(전부 외부 일정·범위 확정 사유, 진척 미달 0건). 캘린더 29일·실작업일 21일 불변. 버퍼 잔량 = Day 13~14 내 버퍼 + **9/4~9/7 4일** + Day 21 1일. 3차 조정에서 신설한 "확정 외부 일정 선반영" 규칙의 첫 적용 사례. MVP.md·PRD 제품 정의 불변

## 2026-08-20 — Day 5 CLI 잔여(REQ-CLI-003 로컬 리포트) + 집계 결함 수정 + npm 라이선스 확정

- 무엇을: ① **[결함] 프롬프트·세션 수 부풀림 발견·수정** — 세션 재개 시 Claude Code가 직전 대화 전체를 새 `.jsonl`로 복제하고 이어 쓴다는 사실을 실데이터에서 확인. 기존 스펙은 assistant `message.id`만 중복 제거하고 세션을 "파일 수"로 정의해, 프롬프트가 46% 부풀고(8/19 실측 28→15) 세션이 2배(파일 4개=실제 대화 2개)로 잡혔다 ② **OQ-007 확정**(사용자 결정 3문항) ③ **make-prd 일괄 반영 6건** — CLI-001 v1.1.0→v1.2.0, 11·05·14·21 ④ **REQ-CLI-003 구현** `cli/src/report.ts` 신설 + main 통합 ⑤ **npm 라이선스 확정**(사용자) — CLI만 MIT, 웹은 현행 유지 ⑥ `npm login` 완료(`jong-yeon`)
- 어떻게: OQ-007 선택지를 근거 없이 묻지 않으려고 실데이터를 먼저 계측했고 — 그 과정에서 결함이 드러났다(도구 결과 분량 측정 → 파일별 uuid 비교 → 100% 포함 관계 발견). 수정은 근본 지점 한 곳: `analyze.ts`에 `dedupe()`(uuid 1회) + `liveSessions()`(프롬프트 uuid 집합이 다른 파일에 완전 포함되면 재개 복제본으로 제외)를 두고 `report.ts`가 재사용. 세션 판정은 **중복 제거 전** 파일별 집합으로 해야 포함 관계가 살아남는다(제거 후엔 사라진다) — 구현 중 한 번 틀렸다가 테스트로 잡음. 스펙 수정은 PLAN 운용 원칙 4번대로 `make-prd`로, 구현은 `/tdd`로 RED→GREEN(신규 9 케이스). 리포트는 로그를 두 번 읽는다(대상일이 집계 후에야 정해져서) — 실측 2.4s+2.4s
- 왜: 세션·프롬프트는 스탯 카드 6줄 중 2줄이고 등급 BR-009의 3지표 중 하나라, 그대로 두면 **모든 사용자의 등급이 부풀려진 채 런칭**된다. 재미 서비스라도 숫자가 틀리면 신뢰가 무너지고 이건 되돌리기 어려운 축(공유된 링크에 등급이 박힌다). OQ-007 ③은 내 권고(무제한 축적)와 달리 사용자가 7일 롤링 자동 삭제를 택했다 — CLI가 사용자 디스크에서 파일을 지우게 되므로 삭제 대상을 `^\d{4}-\d{2}-\d{2}\.html$` 정확 매치 일반 파일로만 한정하고 디렉터리·심볼릭 링크를 제외했다. 라이선스는 `npx scored` 실행이 제품의 전부인데 기존 문구가 "어떠한 사용 권한도 부여되지 않습니다"라 자기모순이었고, CLI는 사용자 머신에서 사용자 데이터를 읽으므로 검증 가능성이 곧 신뢰 장치다
- 결과: **31 tests pass**(기존 20 + 신규 11), `tsc` 빌드·ESLint 통과. E2E(실데이터 읽기·임시 홈): exit 0, 4.8초, 5세션·23프롬프트, URL 1,221자(상한 8,000), 리포트 26.7KB·0600, 7일 밖 리포트 삭제·사용자 파일(`keep-me.txt`) 보존 확인. 프로젝트 간 uuid 교집합 0으로 과잉 병합 없음도 확인. `npm pack --dry-run` 20파일 9.0kB(LICENSE 포함). 잔여: `cli/README.md` 없음(npm 페이지 백지 — publish 전 필요) · `--primary` 확정(사용자, Day 4 이월) · npm publish · 웹 화면 착수(Day 6~9)

## 2026-08-20 (2) — Day 5 후반: `/report` 화면 착수 + `--primary` 확정

- 무엇을: ① `src/lib/payload.ts` — 06 해시 디코딩(base64url→deflate-raw→JSON), BR-003 버전 판정, BR-004 주소창 정리, BR-002 부분 모드. TC-RES-001-01~04·003-01 ② `/report` 라우트 + SCR-003 스탯 카드(지표 6줄) + SCR-002 진입 로직 ③ **`--primary` 딥 그린 확정**(사용자) ④ 브라우저 전용 버그 2건 수정
- 어떻게: 웹도 테스트 의존성 0 유지 — `node --test src/lib/*.test.mts`(Node 타입 스트리핑). 05 페이로드 타입은 `cli/src/types.ts`에서 `import type`으로 공유해 SSOT를 한 곳에 뒀다(`allowImportingTsExtensions`). 색 결정은 dev 전용 `?primary=` 오버라이드를 넣어 실물 카드 위에 후보 3종을 얹고 라이트·다크 6컷을 비교했다. 헤드리스 Chrome이 뷰포트를 최소 500px로 강제해 390px 스크린샷이 잘려 나오는 문제가 있었는데, **이걸 가로 오버플로 버그로 두 번 오진했다** — `innerWidth`를 실제로 찍어보고서야 도구 아티팩트임을 확인했고, 이후 iframe으로 정확한 폭을 만들어 검증했다
- 왜: 유닛 테스트만으로는 못 잡는 버그가 둘 있었다. ① 손상 해시에서 `DecompressionStream` 쓰기 쪽 promise가 미처리 거부로 터짐 ② **해시를 두 번 읽던 문제** — BR-004가 주소창을 즉시 비우는데 StrictMode가 이펙트를 두 번 돌려 두 번째 읽기가 빈 값이 되고, 정상 링크가 ERR-HASH-001로 떨어졌다(첫 렌더에서 실제 재현). 해시는 모듈 스코프에서 1회만 캡처하도록 고쳤다. `--primary`는 사용자가 고른 `oklch(0.55 0.15 150)`이 foreground 토큰과 4.27:1로 SC 1.4.3 미달이라 명도만 −0.05 조정했다 — 형광 오렌지를 탈락시킨 것과 같은 기준을 사용자 선택안에도 그대로 적용
- 결과: 5 tests pass, `next build` `/report` 정적 프리렌더, ESLint clean. 390/768px 라이트·다크 실렌더 확인(지표 6줄 정상, 오버플로 없음). DESIGN.md `--primary` 임시 딱지 제거 — 라이트 `oklch(0.5 0.15 150)` 5.22:1 / 다크 `oklch(0.48 0.14 150)` 5.73:1. **새 이슈**: 토큰 지표가 33,194,453으로 찍힌다 — 05·CLI-001 §3대로 `cache_read`까지 합산한 결과라 스펙 위반은 아니지만 캐시 읽기가 값을 지배하고, BR-009 등급 3지표 중 하나라 등급 인플레 소지. Day 7 임계값 작업에서 분리 여부 판단 필요. 잔여: SCR-004 대시보드·SCR-005 공유·SCR-001 랜딩(아직 Next 스캐폴드), 유형·등급 판정 모듈(Day 7)

## 2026-08-20 (3) — 토큰 지표에서 cache_read 제외 (등급 오염 차단)

- 무엇을: `stats.tokens.in` 정의에서 `cache_read_input_tokens`를 뺐다. PRD(CLI-001 §3·05 데이터모델·08 BR-009 전제 note) + `cli/src/analyze.ts` + TC-CLI-001-17 신설·TC-02 기대값 갱신
- 어떻게: `/report` 카드에 토큰이 33,194,453으로 찍히는 걸 보고 구성을 분해했다 — 6일(8/15~8/20) 실측에서 `cache_read`가 전체의 **91~97%**. 스펙 개정은 PLAN 운용 원칙 4번대로 make-prd 경유, 구현은 `/tdd` RED→GREEN
- 왜: `cache_read`는 "같은 컨텍스트를 몇 번 다시 읽었나"이고 대화가 길어지면 턴 수의 제곱에 가깝게 불어난다 — 사용자의 작업량이 아니라 세션 길이·컨텍스트 크기를 재는 값이다. 날짜 간 편차가 **153배**(8/16 699k ~ 8/18 107.2M)로 벌어져, BR-009 등급 3지표 중 토큰 축이 "긴 세션 1회 = 최고 등급"으로 무너진다. 짧게 여러 번 효율적으로 일한 사람이 낮은 등급을 받는 구조였다. Day 7에 임계값을 잡기 **전에** 정의를 고쳐야 임계값이 의미를 갖는다
- 결과: 편차 153배 → **6배**(469k~2.8M), 카드 표시 46.5M → **1,335,026**. `week.tokens` 0.33M~4.40M로 날짜 비교가 가능해졌다. 32 tests pass, `tsc` 빌드 통과. 백만 단위는 유지돼 "오늘 133만 토큰"의 재미는 보존

## 2026-08-20 (4) — Day 4·5 EOD 기록 (Obsidian 완료 / Notion 대기)

- 무엇을: Day 4(8/18)·Day 5(8/20) 두 날치 EOD를 한 세션에 처리. ① Obsidian TIL **신규 6건** + 기존 1건 보강 ② Notion「개발 로그:scored」등록분(진행 요약 2건 + 트러블슈팅 7건) 원고 작성 ③ TODO·history 갱신
- 어떻게: 볼트가 여러 프로젝트 공용이라 기존 47개 노트와 **중복 주제를 먼저 배제**했다 — `Node 26 네이티브 TS 테스트`·`한글 웹폰트 unicode-range`·`대비비는 계산으로`는 이미 다른 프로젝트(Stackd) 사례로 존재해 새로 쓰지 않고, `--primary` 딥 그린 건만 대비비 노트에 **추가 사례로 이어 붙였다**(사용자가 고른 색에도 같은 검사를 적용한다는 각도). 그 결과 Day 4는 신규 1건만 남았고 Day 5에 5건이 몰렸다 — 세팅·TDD 위주 날과 실데이터를 만난 날의 지식 밀도 차이가 그대로 드러난 셈. 신규 6건: ① Claude Code 세션 재개 = `.jsonl` 통째 복제 + **중복 제거 전에 세션 판정** ② `cache_read`는 작업량이 아니라 세션 길이를 잰다(편차 배수로 지표 검산) ③ StrictMode 이중 실행 × "읽으면 사라지는 값" ④ 헤드리스 브라우저 최소 뷰포트가 반응형 검증을 속인다 ⑤ 사용자 디스크에서 파일을 지우는 기능의 안전선(정확 매치 + `lstat` 일반 파일만) ⑥ bin 진입점을 얇게 두면 CLI E2E에 프로세스가 필요 없다. Notion은 MCP가 제거된 상태(상시 규칙대로 구현 세션 off)라 `claude mcp add`로 local 스코프에 재등록 — **MCP는 세션 시작 시에만 연결**되므로 이번 세션에선 못 쓴다. 원고를 스크래치패드에 완성해두고 등록은 다음 세션으로 넘김
- 왜: PLAN 기록 시스템은 EOD 격일 다운시프트를 허용하되 **트러블슈팅 로그만은 당일 필수**인데, Day 4는 EOD 자체를 건너뛰었고 Day 5는 밤에 몰렸다. 이틀치를 묶어 처리하되 트러블슈팅 7건은 사실관계가 흐려지기 전에 전부 복원했다. 소요시간은 커밋 타임스탬프 기반 추정치로 표기 — 실측 기록이 없어 정확한 값을 아는 척하지 않는다
- 결과: Obsidian `dev-notes/` 6개 파일 신규 + 1개 보강. Notion 원고 96줄(진행 요약 2 + TS-01~07) 대기. **잔여: Notion 등록(다음 세션 — MCP 재연결 후)**. 이번 EOD에서 가장 값이 큰 기록은 TS-03·TS-04 — 둘 다 "스펙대로 구현했는데 숫자가 틀린" 유형이라 테스트로는 안 잡히고 실데이터를 눈으로 봐야 드러났다

## 2026-08-22 — 일정 재조정 5회차: Day 6 착수일 8/23으로 이동 (지렛대 0)

- 무엇을: `docs/PLAN.md` 일정 매핑을 `Day N≥5 = 8/15+N` → **`Day N≥6 = 8/17+N`**로 개정(Day 6 이후 +2일 시프트). `TODO.md` 상단 매핑 동기화 + Day 6 섹션 신설. 조정 이력 5회차 기록
- 어떻게: PLAN 운용 원칙 4번대로 **make-plan Step 7(재조정)** 경유. 프로토콜상 누적 3회째부터 조정 전 **전제 재검토 3문항**이 선행 게이트라 사용자 답을 먼저 받았다 — ① 스코프(SCR 6 + CLI)는 현실적, 공백은 일회성 ② 킬 크라이테리아 조기 발동 불필요, 판정일 9/22 유지 ③ 구조적 변경으로 일정의 캘린더 등록 검토. 압축 지렛대는 순서 고정이라 **지렛대 0(버퍼 사용)**에서 종료 — 버퍼 9/4~9/7 4일에서 2일 차감으로 전액 흡수됐고 1~4번은 손댈 필요가 없었다. 작업 중 **조정 이력 2줄을 처음 읽을 때 놓쳐 차수를 3회로 오기재**했다가 되돌렸다: 실제 누적은 4회였고 9/4~9/7은 "요일 보정 갭"이 아니라 4차 조정에서 **버퍼로 명시 귀속**된 잔량이었다 — 갭이면 지렛대 미적용이지만 버퍼면 지렛대 0 소모라서, 이 구분이 다음 조정의 시작 지점을 바꾼다
- 왜: 8/21~8/22 이틀간 커밋이 0건이었다. 1~4차 조정은 전부 외부 일정(AI Summit)·범위 확정 사유였던 반면 **이번은 최초의 진척 미달 사유**라, 캘린더만 밀고 넘어가면 원인이 기록에서 사라진다. 런칭일(9/8 화)과 판정일(9/22 화)은 화~목 규칙을 그대로 충족해 건드리지 않았다 — 기간이 밀렸다고 판정 기준을 후하게 바꾸지 않는다는 확장 프로토콜 원칙
- 결과: 실작업일 21·스코프(SCR 6)·킬 크라이테리아 **전부 불변**, 런칭 9/8(화)·판정일 9/22(화) 불변. **버퍼 잔량 = Day 13~14 내 버퍼 + 9/6~9/7 2일 + Day 21 1일** (4일 → 2일). 주요 마일스톤 이동: Day 9 컷 판정 8/24 → **8/26(수)**, Day 11 테스터 섭외 마감 → **8/28(금)**, Day 13 게릴라 테스트 → **8/30(일)**, Day 14 3축 리뷰 → **8/31(월)**. 지렛대 2~3 미적용이므로 MVP 기획서 §4 가설·§15 비기능 수치는 재확인 대상 아님(제품 정의 불변). **다음 조정은 남은 버퍼 2일 소진 시 지렛대 1(출시 준비 이월 항목 축소)부터 시작된다**

## 2026-08-23 — Day 6: 랜딩(SCR-001)·처리 방식(SCR-006) 완성 + 공유 오버레이(SCR-005) 착수

- 무엇을: 미착수 화면 3개를 브랜치 `feat/day6-screens`에서 구현했다. ① **SCR-001 랜딩** — create-next-app 기본 템플릿을 전면 교체, EL-LAND-001~006 전부(헤드라인·npx 코드 블록+복사·샘플 성적표·나에게 보내기·서버전송0 배지·GitHub) ② **SCR-006 `/how`** — EL-HOW-001~006, 무업로드 원리 도식·전송 범위 표·GA4 고지·매일 뽑기·판정 변경 고지 ③ **SCR-005 공유 오버레이 착수** — 카드 미리보기·링크 복사(REQ-SHARE-002)·하이라이트 토글(BR-004)·OG 범위 고지. 부수: GA4 gtag 배선(EVT 5종), 404·`error.tsx`(ERR-APP-001)·`robots.ts`·`sitemap.ts`·정적 OG 이미지
- 어떻게: 구간 활성 스킬(frontend-design·ponytail)대로, 새 의존성 0으로 끝냈다. **오버레이는 shadcn Dialog/Drawer 대신 네이티브 `<dialog>`** — `showModal()`이 포커스 트랩·Esc·백드롭·바깥 inert를 전부 주고, 모바일 바텀시트↔데스크톱 중앙 모달 전환은 `mt-auto md:m-auto` 두 클래스면 된다. **토스트도 sonner 대신 CTA 옆 `aria-live` 인라인 피드백**(`ponytail:` 주석으로 상향 조건 명시 — 알림 종류가 늘면 도입). 로직은 DOM 없이 테스트하려 주입식 순수 함수로 분리(`lib/share.ts`) — TC-LAND-001-01·02, TC-LAND-002-01, TC-SHARE-002-01·02 신설로 **12 tests pass**. `<dialog>` 닫기는 X·Esc·백드롭·뒤로가기를 전부 `history.back()` 한 경로로 모아 히스토리 항목이 새지 않게 했다(§6). 검수는 390/1280 × 라이트/다크 실렌더
- 왜: 남은 구현일이 Day 6·8 이틀뿐(Day 7은 콘텐츠 고정, Day 9 저녁 컷 판정)이라, 화면당 예산이 반나절이다. shadcn Dialog+Drawer+Switch 3종을 붙이는 시간이면 화면 하나가 통째로 밀린다 — 네이티브가 접근성까지 더 잘 준다면 그게 지름길이자 정답이다. 랜딩을 1순위로 잡은 건 공유 유입의 착지점이면서 유일하게 손도 안 댄 화면이었기 때문
- 결과: `next build` 전 라우트 정적 프리렌더(7개), lint·tsc 클린, 12 tests pass. 실렌더에서 잡아 고친 것 4건 — ① 랜딩 샘플에 **"유형 미정 / 등급 Day 7"** 내부 일정 문구가 그대로 노출(StatCard의 개발용 자리표시가 랜딩까지 따라옴 → 판정 값이 없으면 블록 자체를 그리지 않게 변경, Day 7에 양쪽이 동시에 채워진다) ② `/how` 가로 ASCII 도식이 모바일에서 절반 잘림 → 세로 흐름으로 교체(신뢰 장치가 주 유입 기기에서 안 읽히면 의미가 없다) ③ 다크모드에서 네이티브 체크박스가 흰 박스로 튐 → `color-scheme` 한 줄로 폼 컨트롤 전체를 테마에 편입 ④ `<Link>`로 렌더하는 Button 3곳 전부 Base UI `nativeButton={false}` 누락 경고 — 한 곳만 고치면 나머지 둘이 남는 유형이라 호출부를 전수 수정. **잔여: REQ-SHARE-001 카드 PNG(클라이언트 캔버스 렌더러)·REQ-SHARE-003 동적 OG·SCR-004 대시보드 → Day 8. GA4 측정 ID(사용자)·iOS Safari 실기기(사용자)**

## 2026-08-23 (2) — EVT-RES-001·003 누락 발견·보강 (킬 크라이테리아 분자)

- 무엇을: `/report` 진입 이벤트 **EVT-RES-001 `result_reached`**(파라미터 `entry`·`has_highlights`)와 **EVT-RES-003 `result_failed`**(`error_code`)를 `report-client.tsx`에 추가했다. 진입 출처 캡처를 위해 `takeHash()`를 `takeEntry()`로 확장 — 해시와 `?from=cli` 유무를 한 번에 집어온다
- 어떻게: 사용자가 `.env.local`에 GA4 측정 ID를 넣은 뒤 실발화를 브라우저에서 확인하는 과정에서 드러났다. 랜딩·공유 이벤트 4종은 `dataLayer`에 찍히는데 `/report` 진입 이벤트만 없었다. 캡처 지점이 중요한데, **BR-004가 진입 즉시 `?from=cli`를 지우기 때문에 `history.replaceState` 이전에 읽어야 한다** — 기존 해시 캡처와 같은 이유·같은 자리라 `captured` 모듈 변수를 `{hash, entry}` 객체로 넓혔다(StrictMode 이중 실행 방어도 그대로 승계). 검증: `entry=cli` / `entry=link` / 손상 해시 3경로를 실제 로드로 확인
- 왜: EVT-RES-001은 킬 크라이테리아 1번 지표 **"/report 도달률 20%"의 분자**다. 이게 없으면 판정일(9/22)에 분자가 0으로 나와 무조건 피벗 판정이 난다 — 제품이 아니라 계측이 실패한 건데 그걸 구분할 방법이 없어진다. Day 15~16 DebugView 검수 때 발견했으면 2주치 데이터가 이미 비어 있었을 것
- 결과: 7종 실발화 확인 — `npx_command_copied` / `command_self_sent{method:clipboard}` / `result_reached{entry:cli|link, has_highlights}` / `result_failed{error_code:ERR-HASH-001}` / `share_opened` / `link_copied{highlight_included:false|true}`. 링크 복사 실검증에서 BR-004도 함께 확인됨 — 토글 OFF 해시 416자(하이라이트 제거) → ON 518자(포함), 주소창은 두 경우 모두 `/report` 그대로. 잔여 EVT 3종은 각 기능 구현 시점(Day 8·Day 10~12)에 배선

## 2026-08-23 (3) — Day 6 EOD 기록 (Obsidian 완료 / Notion Day 4·5·6 일괄 등록)

- 무엇을: Day 6 한 날치 EOD. ① Obsidian TIL **신규 5건** + 기존 1건 보강 ② Notion「개발 로그:scored」Day 6 분 원고(진행 요약 1건 + 트러블슈팅 TS-08~15) 작성 ③ TODO(Day 5 Notion 잔여·Day 6 EOD·사용자 항목·Day 7 인계)·history 갱신
- 어떻게: 기존 54개 노트와 중복 주제를 먼저 배제 — `NEXT_PUBLIC_*` 빌드타임 인라인·내부 트래픽 필터 2단계·StrictMode 캡처 위치는 이미 있어서 새로 쓰지 않고, Vercel 환경변수 등록 후 빌드 부재 건만 `프로덕션에 안 보인다` 노트에 **반대 순서 사례**("커밋은 있고 빌드가 없는")로 이어 붙였다. 신규 5건: ① 네이티브 `<dialog>` 하나가 모달 라이브러리 3종을 대체한다(닫기 경로 `history.back()` 단일화) ② 킬 크라이테리아의 분자 이벤트는 구현 당일 실발화로 검증한다(스펙≠코드≠dataLayer≠204) ③ 재사용 컴포넌트의 개발용 자리표시는 프로덕션까지 샌다 ④ `color-scheme` 한 줄이 네이티브 폼 컨트롤을 테마에 편입한다 ⑤ satori OG는 줄바꿈을 접고 oklch를 모른다. Notion은 세션 시작 시 OAuth 승인이 필요해 원고를 `_notion-원고-scored-Day6.md`로 먼저 백업했고, 사용자가 브라우저 승인 후 `/plugin`으로 재연결하자 **같은 세션에서 등록 가능**했다(MCP 재연결이 세션 재시작 없이도 됨 — Day 4·5 EOD 때의 "세션 시작 시에만 연결" 메모는 인증 플로우에는 해당 없음). Day 4·5분 9건 + Day 6분 9건 = 18건을 기존 페이지 형식(`**Day N** (날짜)` + 문제/원인/해결/소요시간)으로 일괄 생성
- 왜: PLAN 기록 시스템 — 트러블슈팅 로그는 당일 필수. 오늘 8건(TS-08~15) 중 절반이 "검증 도구·환경이 제품 버그로 위장한" 유형(포트 폴백·satori 비브라우저·환경변수 빌드 시점)이고, 가장 비싼 건 TS-14(도달률 분자 누락)였다 — 이틀 뒤 Day 9 컷 판정·3주 뒤 판정일에 직접 닿는 항목이라 기억이 선명할 때 원인·캡처 위치까지 적었다
- 결과: Obsidian `dev-notes/` 5개 신규 + 1개 보강 (54 → 59 노트). **Notion「개발 로그:scored」18건 등록 완료**(과정요약 3 + 트러블슈팅 15, Day 4·5 이월분 해소). TODO 잔여: iOS Safari 실기기(사용자, 같은 Wi-Fi) · Day 7 콘텐츠 고정(8/24)

## 2026-08-24 — Day 7 콘텐츠 고정일: 등급 임계표·유형 임계·문구 풀 확정 (OQ-001 핵심 + OQ-002 초안)

- 무엇을: ① 제작자 30일 로그(활동일 21일)를 CLI `analyze.ts`와 동일 정의로 일별 집계해 8지표 백분위 산출 ② BR-009 등급 임계표(3지표 앵커 커브 + S~C 경계)를 08 §계산 규칙에 신설 ③ BR-010 유형 임계 미세조정(retry 0.3→0.7, avgLen 200→1500, oneLiner 0.7→0.9, marathon 600→480) ④ 유형명 7종 현행 확정 + 유형 코드(07 `t`) + 문구 풀 CPY-TYPE-001~007(유형당 2개, 결정적 선택 규칙) 확정. PRD 7개 문서(08·11·07·21·00·SCR-003·CLI-001) 반영
- 어떻게: make-prd 경유(운용 원칙 4). 백분위 스크립트는 CLI 소스(`parseLine`·`boundaryDay` 등)를 직접 import해 정의 불일치를 원천 차단 — cache_read 제외·uuid 중복 제거·05시 경계 전부 동일. 임계 후보는 3안(avgLen 500/1000/1500)을 21일 실데이터에 시뮬레이션해 유형 분포를 보여주고 사용자가 C안(1500)을 선택. 등급 커브도 동일 방식으로 분포(C2·B5·B+2·A7·A+2·S3)를 검증한 뒤 승인받았다. 문구 선택은 `day` 숫자합 % 풀 크기 — TC-RPT-001-01의 "동일 입력 = 동일 결과"를 랜덤 없이 충족
- 왜: 구 임계는 감으로 잡은 값이라 실로그에 대면 avgLen≥200이 p25, retry≥0.3·oneLiner≥0.7이 p50 수준 — **장문 설교자가 21일 중 13일을 독식하고 폴백 밸런스 코더는 0회, 마라톤 러너(600분)는 30일 중 발동 0회**였다. "매일 바뀌는 유형"이 재미 장치의 핵심인데 매일 같은 유형이 나오면 장치가 죽는다. 임계를 p75~p95선으로 정렬하자 7유형 전부 등장. 단 제작자 로그는 붙여넣기 편향(avgLen 최대 7,109자)이 있어 Day 13 게릴라 로그 보정을 전제로 확정
- 결과: OQ-002 초안 확정(잔여: Day 13 보정), OQ-001 핵심 확정(잔여: 불용어 목록 — 후보 제시됨, 사용자 검토 중). 웹 판정 모듈 구현(BR-008 `/tdd` 필수)은 Day 8. 미결 2건: Q5 랜딩 샘플 1장 vs 2장, Q6 불용어 승인. 30일 상위 단어 실측에서 the:3121·and:1815가 1·2위 — 불용어 없이는 하이라이트 1위가 "the"가 된다는 근거 확보

## 2026-08-24 (2) — Day 7 마감: 불용어 56개·랜딩 샘플 2장 확정 (OQ 전건 해소)

- 무엇을: 문답 2라운드 승인분 반영. ① CLI-001 §4에 불용어 목록 56개(영어 기능어 35 + 한국어 접속 8·지시 5·관형 8)와 처리 규칙(소문자 비교·양끝 구두점 트리밍) 기입 ② 랜딩 샘플을 2장으로 확정 — SCR-001 EL-LAND-003·§16 개정(md+ 2장/모바일 1장) + 두 번째 페이로드 `SAMPLE_MARATHON`(마라톤 러너·A 72점) 데이터 제작 ③ OQ-001·006 완료 처리 → **미해결질문 전건 해소**(OQ-002 Day 13 보정만 잔여)
- 어떻게: make-prd 경유. 불용어는 30일 실측 상위 60 단어에서 골랐다 — 상위 10이 전부 영어 기능어(`the` 3,121회)라 목록 없이는 하이라이트 1위가 "the". `다시`·`해줘`·`왜`는 재미 어휘라 의도적으로 뺐다. 두 번째 샘플 값은 08 §계산 규칙을 역산해 설계 — activeMinutes 486(마라톤 비율 1.01로 유일 임계 충족), 점수 (69+48+100)/3=72 → A. 기존 SAMPLE도 재검산해 에러늪·B+(56점) 확인. 모바일 1장 제한은 카드 2장이 첫 화면에서 CTA를 밀어내는 비용 때문
- 왜: 샘플 2장은 사용자 결정("둘 다 보여줄 수 없나") — 유형·등급이 매일 바뀐다는 재방문 훅을 랜딩에서 증명하는 부수 효과가 있다. 콘텐츠 고정일 원칙대로 데이터·문서까지만 채우고 레이아웃 구현·CLI 코드 반영은 Day 8로 넘겼다
- 결과: tsc 클린. Day 7 산출물 전체 = 등급 임계표 + 유형 임계·코드 + 문구 풀 14개 + 불용어 56개 + 샘플 2장. Day 8 인계: 판정 모듈(tdd)·StatCard 켜기·랜딩 2장·CLI 불용어·SCR-004·PNG·동적 OG — Day 9(8/26) 저녁 컷 판정 전 마지막 구현일

## 2026-08-24 (3) — Day 7 EOD 기록 (Obsidian·Notion 당일 완료)

- 무엇을: Day 7 한 날치 EOD. ① Obsidian TIL **신규 6건** ② Notion「개발 로그:scored」Day 7 분 7건 등록(과정요약 1 + 트러블슈팅 TS-16~21) + 원고 백업 `_notion-원고-scored-Day7.md` ③ TODO(Day 7 EOD 항목)·history 갱신
- 어떻게: 콘텐츠 고정일이라 구현 트러블 대신 **판정·데이터 설계 실수 6건**을 TS로 남겼다 — TS-16 감 임계가 실로그 p25~p50 수준이라 장문 13/21일 독식(백분위 앵커로 재정렬), TS-17 불용어 없인 하이라이트 1위가 "the"(3,121회), TS-18 백분위 스크립트가 CLI 소스 직접 import로 정의 불일치 원천 차단, TS-19 문구 랜덤이 동일 입력=동일 결과와 충돌(day 숫자합 결정적 선택), TS-20 0값 엣지 문구는 판정 구조가 이미 차단(추가 0개), TS-21 샘플 값은 채점 규칙 역산 설계. TIL 6건은 TS와 1:1 대응. Notion은 기존 페이지 형식(`**Day N**` + 문제/원인/해결/소요시간/교훈) 그대로 7건 일괄 생성 — Day 6과 달리 이월분 없이 당일 완료
- 왜: PLAN 기록 시스템 — 트러블슈팅 로그는 당일 필수. 오늘 6건의 공통 줄기는 "직관·랜덤·재구현 대신 실데이터·결정성·단일 정의"라, Day 8 판정 모듈 구현(tdd)과 Day 13 게릴라 로그 보정에서 그대로 재사용되는 판단 기준이다
- 결과: Obsidian `dev-notes/` 신규 7파일(TIL 6 + 원고 1, 69 → 76). **Notion「개발 로그:scored」누적 25건**(과정요약 4 + 트러블슈팅 21). TODO 잔여: 게릴라 테스터 3~5명 섭외(사용자, ~8/28, iPhone 1 포함) · Day 8(8/25) 순수 구현일

## 2026-08-25 — Day 8: 웹 판정 모듈·대시보드·카드 PNG·동적 OG (전 항목 구현)

- 무엇을: Day 7 인계 6항목 전부 — ① 웹 판정 모듈(BR-009 등급 커브·BR-010 유형·CPY-TYPE 문구, `src/lib/judge.ts`) + StatCard 유형·등급 블록 점등 ② 랜딩 샘플 2장 레이아웃(md+ 2장/모바일 1장) ③ CLI 불용어 56개·양끝 트리밍(CLI-001 §4) ④ SCR-004 대시보드(타임라인·주간 추이·히트맵·기본 스탯·도구·프롬프트 스타일·로컬 리포트 안내) ⑤ REQ-SHARE-001 카드 PNG 저장 ⑥ REQ-SHARE-003 동적 OG(`/api/og` + `/report` generateMetadata) + EVT-DASH-001·SHARE-002 배선
- 어떻게: 판정·집계·검증 로직 전부 `/tdd`(BR-008) — 웹 49·CLI 33 테스트 green. 카드 PNG는 화면의 StatCard DOM을 `html-to-image`로 래스터화(REQ-RPT-002 "값 복제 금지" — 수동 캔버스 재구성 대신 실제 컴포넌트 공유). OG 쿼리는 shareUrl이 유일 생성 지점(BR-006), 서버는 parseOg 검증 통과값만 재인코딩(패스스루 금지). 대시보드 차트는 라이브러리 없이 SVG/CSS. 검증: dev 서버 + chrome-devtools로 390/768/1280·라이트/다크·실로그 페이로드 E2E(밸런스 코더·B 28점 실판정), GA collect 바디에서 result_reached·dashboard_viewed·share_opened·card_saved(method=download) 실발화 확인, PNG 산출물 육안 확인
- 왜: Day 9(8/26) 저녁 스코프 컷 판정 전 마지막 순수 구현일 — 컷 1순위 SCR-004를 마지막 순서로 배치해 미완 시 깨끗이 잘리게 했으나 전 항목 완료로 컷 불필요 상태 진입
- 결과: 발견·수정 3건 — ⓐ satori는 자식 2개 div에 display:flex 명시 요구(단일 문자열 병합으로 회피) ⓑ 데스크톱 Chrome도 `canShare` true → OS 공유시트가 떠서 AC-1 위반, `pointer: coarse`로 모바일만 공유시트 분기 ⓒ 전부 0인 시간대 데이터가 빈 차트로 그려짐(AC-2 "빈 껍데기 금지") → 합계 0 가드. 의존성 +1(html-to-image, 웹 전용 — CLI는 여전히 0). 사용자 결정 반영: 게릴라 테스터 섭외 대신 본인 직접 테스트 — Day 9 컷 판정 때 make-plan 경유로 PLAN 반영 예정(실패 조건 9 관련 스코프 변경)

## 2026-08-25 (2) — Day 8 EOD 기록 (Obsidian·Notion 당일 완료)

- 무엇을: Day 8 한 날치 EOD. ① Obsidian TIL **신규 4건** + 기존 satori 노트 보강 1건 ② Notion「개발 로그:scored」Day 8분 6건 등록(과정요약 1 + 트러블슈팅 TS-22~26) + 원고 백업 `_notion-원고-scored-Day8.md` ③ TODO(Day 8 EOD 항목)·history 갱신
- 어떻게: 순수 구현일이라 구현 중 발견·수정 3건 + 설계 결정 1건 + 프로덕션 진단 1건을 TS로 남겼다 — TS-22 satori flex 규칙 재발(자식 div 대신 문자열 병합으로 표면 제거), TS-23 canShare는 기능 감지지 기기 감지가 아니다(`pointer: coarse` 분기), TS-24 전부 0인 배열은 존재 검사를 통과한다(합계>0 의미 검사 후 위젯 생략), TS-25 카드 PNG는 값 복제 대신 실 DOM 래스터화(REQ-RPT-002가 설계를 결정), TS-26 카톡 스크래퍼의 초장문 URL 거부(서버 전수 정상 → 대조 실험으로 확정, 이슈 #1). TIL 4건은 TS-23~26과 1:1 대응, TS-22는 기존 satori 노트 ④에 회피 패턴 보강. Notion은 기존 페이지 형식(`**Day N**` + 문제/원인/해결/소요시간/교훈) 그대로 6건 일괄 생성 — 당일 완료
- 왜: PLAN 기록 시스템 — 트러블슈팅 로그는 당일 필수. 오늘 5건의 공통 줄기는 "기능 감지≠기기 감지, 존재≠의미, 화면과 산출물의 이중화 금지, 한계는 서버가 아니라 채널에" — Day 9 컷 판정(이슈 #1 대응 방향)과 Day 10~12 축 패스에서 재사용되는 판단 기준이다
- 결과: Obsidian `dev-notes/` 신규 5파일(TIL 4 + 원고 1) + 보강 1. **Notion「개발 로그:scored」누적 31건**(과정요약 5 + 트러블슈팅 26). TODO 잔여: 사용자 육안 확인 2건(StatCard 첫 렌더·`npx scored` 체험) · Day 9(8/26 수) 저녁 스코프 컷 판정(안건 5건)

## 2026-08-26 — Day 9: 이슈 #1 근본 원인 규명(길이 아님, `#`) + 유입 CTA EL-RPT-008 신설 + 한국어 줄바꿈 전역 수정

- 무엇을: Day 9는 PLAN상 저녁 스코프 컷 판정일. 판정에 앞서 안건 ③(이슈 #1)을 실측으로 규명했고, 그 과정에서 더 시급한 유입 결함을 발견해 먼저 처리했다. ① 이슈 #1 원인 정정 — 카카오톡 미리보기 미표시의 원인은 URL 길이가 아니라 `#` 프래그먼트 ② 공유 수신자용 유입 CTA **EL-RPT-008 신설**(SCR-003 v1.1.0→v1.2.0) + 구현 ③ 한국어 줄바꿈 전역 규칙(`break-keep`+`break-words`) 적용 및 DESIGN.md 기록
- 어떻게: ① 이슈에 적힌 "초장문 URL 미스크랩"을 길이 단계별 대조 실험으로 반증했다 — 81자(해시無) ✅ / **102자(해시有) ❌** / 604자 ❌ / 1,400자 ❌. 21자 차이로 길이 임계가 뒤집힐 수 없고, 이슈 본문에 이미 기록돼 있던 "~100자 해시 없는 URL은 정상"이 같은 길이대 대조군이 되어 변수가 `#` 하나로 분리됐다. ② 대응 방향은 sequential-thinking 9단계로 판정 — C(유입 CTA) 즉시 / A'(카드 PNG 워터마크) 기각 / B(해시 없는 공유 링크) Day 10~12 이연. ③ EL-RPT-008은 make-prd 경유로 PRD 먼저 개정(EL-RPT-007을 `entry=cli` 조건부로 바꿔 007/008을 배타로 만들고, REQ-RPT-003 + AC 4종 + TC 4건 추가) 후 `report-client.tsx` 구현. 측정은 **신규 EVT 없이** `/?from=report` 랜딩 page_view 세그먼트로 처리해 이벤트 스펙을 늘리지 않았다. ④ 줄바꿈은 요소별 대응 대신 `globals.css` body에 전역 적용 — 같은 누락의 재발을 구조적으로 막기 위해
- 왜: 킬 크라이테리아 도달률의 분모가 `users(page_view, '/')`인데, `/report`의 ready 분기에는 랜딩으로 갈 출구가 아예 없었다("처음으로"는 error 분기 전용). MVP는 "유입 = 공유"라고 선언했지만 **공유받은 사람이 표본 150 게이트에 잡히지 않는 구조**였던 것 — OG 미리보기 수정보다 상위 문제라 순서를 바꿨다. 미리보기가 안 뜨는 채널에서도 링크를 연 사람은 최소한 랜딩에 도달한다
- 결과: 이슈 #1은 제목·본문·코멘트 전부 정정하고 **OPEN 유지**(원인만 규명, 대응은 판정 ② 대기). 선택지 2번(공유 전용 슬림 페이로드)은 **기각** — 해시 20자로도 실패했으므로 크기 축소 접근이 원리적으로 무효. 검증: 웹 49·CLI 33 tests green, `next build` exit 0, 프로덕션 CSS에 `word-break:keep-all`·`overflow-wrap:break-word` 반영 확인, CTA 양쪽 분기 로컬 육안 확인. TC-RPT-003-01~04은 컴포넌트 렌더 테스트 인프라가 없어 **자동화 N** — Day 15~16 E2E 도입 시 1순위 후보. **8/25 EOD에 남긴 Notion TS-26("카톡 스크래퍼의 초장문 URL 거부")은 오늘 반증됐으므로 Day 9 EOD 세션에서 정정 필요.** 잔여 판정 2건(테스터 전환·실기기 문구)은 make-plan 경유라 Day 10 시작 시로 이월

## 2026-08-26 (2) — Day 9 컷 판정 종결 + PLAN 개정 2건 (make-plan 경유)

- 무엇을: Day 9의 본체인 스코프 컷 판정 4건을 종결했다. ① **컷 대상 없음**(Day 8·9 전 항목 완료) ② **이슈 #1 B안(해시 없는 공유 링크) Day 10~12 이연 확정** ③ 게릴라 테스터 3~5명 → **본인 직접 테스트 전환**을 PLAN에 반영 ④ **"주 2회 실기기/iOS Safari" 규칙 개정**
- 어떻게: ③④는 PLAN 수정이라 make-plan 경유(운용 원칙 4번). ③은 Day 11 섭외 마감 폐지 + Day 13을 "검증 세션"으로 개정하되, **실패 조건 9의 방어력이 약해진다는 사실을 문서에 그대로 남겼다** — 본인 테스트는 재확인이지 검증이 아니고, 만든 사람은 자기 화면을 처음 보는 척할 수 없다. 대신 "외부 눈 1명"을 권고로 두고, 0명일 경우 실패 조건 9를 **발동 상태로 간주**해 Day 21 초기 유저 인터뷰를 런칭 직후로 앞당기는 보전 경로를 명시했다. ④는 iPhone·Xcode 미보유로 착수 시점부터 불이행이던 규칙을 실행 가능한 절차(맥 Safari 응답형 상시 + Day 16까지 실기기 수단 확보 + `navigator.share` 등 실기기 전용 항목의 폴백 선확보)로 교체. 문서 전역의 "게릴라" 참조 6곳을 "검증 세션"으로 정합화했다
- 왜: 지킬 수 없는 규칙을 문서에 남겨두면 그 규칙 하나가 아니라 **다른 규칙의 구속력까지 같이 무너진다.** "주 2회 실기기"는 3주 내내 한 번도 지켜진 적이 없는 조항이었다. 마찬가지로 테스터 축소는 사용자 결정이지만, 그 대가(외부 검증 소실)를 문서에서 지워버리면 Day 20에 리스크가 몰린 사실 자체가 안 보이게 된다 — 축소는 기록하되 안전장치는 남기는 쪽을 택했다
- 결과: PLAN 개정 이력은 **조정 횟수에 산입하지 않음**(기간·배분·스코프·킬 크라이테리아 전부 불변, 지렛대 미적용, 버퍼 잔량 9/6~9/7 2일 + Day 21 1일 불변) — 누적 조정 5회 유지. Day 10 인계에 신규 마감 2건: **Day 11까지 외부 눈 1명 확보 시도**, **Day 16 밤까지 실기기 수단 확보**

## 2026-08-26 (3) — Day 9 EOD 기록 (Obsidian·Notion 당일 완료)

- 무엇을: Day 9 한 날치 EOD. ① Obsidian TIL **신규 2건 + 기존 노트 보강 1건**(정정 1건은 낮 세션에 선행 완료) ② Notion「개발 로그:scored」**TS-26 정정 + 신규 4건 등록**(과정요약 ⑥ + TS-27~29) + 원고 백업 `_notion-원고-scored-Day9.md` ③ TODO(Day 9 EOD 항목·인계)·history 갱신
- 어떻게: 인계 메모는 신규 TIL 3건을 예정했지만 **③(`word-break:keep-all`)은 보강으로 내렸다** — 기존 `TIL-한글-웹-조판은-라틴과-다른-규칙이-최소-3개-있다`(8/18, stackd)의 규칙 ①이 바로 `keep-all`이라, 같은 주제로 파일을 하나 더 만들면 검색 시 어느 쪽이 최신인지 알 수 없어진다. 그 노트에 "덧붙임 (2026-08-26 · scored)" 절을 붙여 **오늘 새로 안 것만** 얹었다: `keep-all`은 공백 단위로만 끊으므로 공백 없는 긴 문자열이 넘친다 → `overflow-wrap:break-word`는 장식이 아니라 짝이고, 둘은 경쟁이 아니라 역할 분담이다. 신규 2건은 각각 TS-27·TS-28과 1:1 대응 — ① 임계값 가설은 **경계 근처 두 점**으로만 반증된다(멀리 떨어진 두 점은 무엇도 확정 못 한다) ② 깔때기 **분모**에 도달하는 UI 경로가 없으면 지표가 아니라 제품이 안 보인다. Notion TS-26은 **삭제하지 않고 정정**했다(제목의 "범인은 URL 길이" → "범인은 URL 길이가 아니라 #", 원인 문단에 취소선 + Day 9 실측표, 해결 문단에 "선택지 2번 기각" 후속) — 오판 자체가 기록 가치라 흔적을 지우면 왜 그때 그렇게 판단했는지가 사라진다
- 왜: PLAN 기록 시스템 — 트러블슈팅 로그는 당일 필수. 오늘 3건의 공통 줄기는 **"내가 어제 확정이라고 적은 것을 오늘 의심한다"**다. TS-27은 대조군 1개짜리 결론을 확정이라 적은 대가(대응안 하나가 통째로 무효)를, TS-28은 분자만 태깅하고 분모의 도달 경로는 안 본 대가(공유 수신자가 표본 게이트에 아예 안 잡힘)를, TS-29는 자매 프로젝트에서 정한 규칙이 자동으로 따라오지 않는다는 사실을 남겼다. 셋 다 테스트로는 안 잡히고 **기록을 다시 읽어야 드러나는** 유형이라 EOD가 유일한 검출 지점이다
- 결과: Obsidian `dev-notes/` 신규 3파일(TIL 2 + 원고 1) + 보강 1. **Notion「개발 로그:scored」누적 35건**(과정요약 6 + 트러블슈팅 29). 등록 중 함정 1건 발견 — Notion 페이지 **제목이 인라인 마크다운으로 파싱돼 단어가 유실**된다(`[바이브 성적표] ⭐ … — …` → `[바이브 구조`). 대괄호를 `\[…\]`로 이스케이프해 `update_properties`로 재설정하면 정상이며, **생성 응답의 제목은 신뢰하지 말고 `notion-fetch`로 재확인**해야 한다(4건 중 3건이 깨진 채 생성됐다). TODO 잔여: Day 10(8/27) `npm publish scored` 0.1.0 + `npx scored` 완주 확인 / Day 11까지 외부 눈 1명 / Day 16 밤까지 실기기 수단 / Day 10~12 축 패스(EVT-RES-002 · B안)

## 2026-08-27 Day 10: 중점 품질 축 패스 1일차 — 모션 설계 확정 + 명세 공백 3건 종결

- 무엇을: PLAN상 Day 10~12는 중점 품질 축(재미 장치의 손맛)에 3일을 투자하는 구간이고, Day 10은 그 설계일이다. ① **ANIMATION.md 빈칸 해소**(Day 0부터 비어 있던 이징·duration 토큰 + 연출 타임라인 예산 3종) ② `globals.css` 모션 토큰 정착 ③ **make-prd 경유 PRD 개정 3건** ④ **G9 수정**(EVT-SHARE-002 `method` 파라미터가 항상 `download`) ⑤ 신규 이슈 #2 등록 ⑥ **이슈 #1 B안 기각**(사용자 결정) ⑦ npm publish 사전 검증.

- 어떻게: PLAN 지정 스킬 3종(find-animation-opportunities · animation-vocabulary · sequential-thinking)을 적용했다. 먼저 탐색으로 실제 상태를 확인한 결과가 계획보다 나빴다 — **공개 연출(재미 장치 v1의 유일한 실체)이 통째로 미구현**이고, `report-client.tsx` State 유니온에 `revealing`이 없으며, `EVT-RES-002`는 `src/` 전체에 0건이었다. 현재는 decode가 수 ms라 로딩 문구가 스치지도 않고 성적표 + 대시보드 전체가 한 프레임에 팝인한다.

  **의존성은 새로 들이지 않기로 했다.** SCR-002 §13이 "연출(GSAP)"이라 적었지만, 필요한 모션 4종 중 CSS로 안 되는 건 숫자 카운트업 하나뿐이고 그건 rAF 루프 한 개다. `tw-animate-css`가 이미 임포트돼 있어 GSAP ~23KB는 LCP 2.5초 예산 대비 정당화되지 않는다. 이 결정을 근거와 함께 ANIMATION.md에 고정해 Day 11~12에 재론하지 않게 했다.

  **연출 자체에 게이트를 적용했다.** 3~4초는 UI 300ms 예산을 넘지만 그 예산은 '조작에 대한 응답'에 걸리는 것이고 이건 콘텐츠 공개 시퀀스다. `/report`는 CLI 실행당 1회(안내가 "내일 또")라 **Rare 티어** — delight budget이 허용되는 유일한 구간이다. 대신 초과의 조건으로 스킵 1클릭·1키(Esc 포함)를 명세에 못 박았다. 반대로 **대시보드 위젯 애니메이션은 기각**했다 — 사용자가 읽는 정보 밀집 UI에서 데이터가 스스로 움직이면 판독을 방해한다.

  타임라인은 산술로 검산했다. 풀 버전 `600 + (5×90+700) + 100 + 800 + 380 = 3030ms`(상한 4000 이하, 여유 620), `entry=link` 축약판 `250+700+300=1250ms`(명세값 정확 일치), 부분 모드는 `judge()` 미호출로 릴·pop이 **데이터로** 0이 된다(분기 코드가 아니다). 총합을 하드코딩하지 않은 이유는 하이라이트 없는 재진입 시 행이 6→5줄로 줄어 예산이 자동 감산돼야 하기 때문이다.

  **연출을 카드 바깥 스테이지에 두기로 한 건 두 제약이 같은 곳을 가리켜서다** — `stat-card.tsx`는 share-sheet의 `toBlob` PNG 캡처 대상이라 서브트리에 opacity/transform이 남으면 저장 이미지에 중간 프레임이 박히고, 게다가 서버 컴포넌트다. 또한 `position: fixed` 오버레이 대신 h1 아래 문서 흐름(`min-h-[60svh]`)에 두면 z-index·스크롤 락·포커스 트랩·backdrop 4개가 전부 불필요해진다.

  PRD 개정 3건은 값이 아니라 명세 사안이라 make-prd를 경유했다(운용 원칙 4번). ① SCR-002 AC-4·AC-5에서 `skipped` 값이 미규정이었다 — reduced-motion과 1.5초 축약판은 **연출을 건너뛴 주체가 사용자가 아니므로** `false`로 확정 ② CPY-RES-003 "오늘의 등급" 신설(릴 라벨이 없으면 숫자 다음에 뭐가 나오는지 알 수 없다) ③ **EVT-SHARE-002 `method` enum이 실제와 어긋나 있었다** — 명세는 `download|clipboard`인데 코드에 clipboard 경로가 아예 없고 실제 두 경로는 `download|share_sheet`다. 명세를 실제에 맞춰 정정한 뒤 코드를 고쳤다(G9).

- 왜: 값과 명세 공백을 오늘 확정하지 않으면 Day 11~12 구현이 그 자리에서 멈춘다. 특히 `skipped` 값은 구현자가 임의로 정하면 GA4 깔때기 해석이 달라지는 종류의 공백이다. G9는 이미 배포돼 지표를 오염시키는 중이라 미룰 이유가 없었다.

- 작업 결과: 웹 49 · CLI 33 테스트 통과, `tsc --noEmit` · `next build` exit 0. 빌드 산출 CSS에서 `--ease-out-quart` · `--ease-out-back` · `keyframes pop` 실재 확인. ANIMATION.md 14줄 → 122줄. 이슈 #2 신규 등록(새로고침·직접 진입 시 정상 링크가 "손상됐어요"로 표시 — `strippedUrl`이 페이로드 해시까지 지우고 Empty 상태가 미구현이라 `result_failed` 지표까지 오염), 이슈 #1은 B안 기각 코멘트 후 OPEN 유지.

  **이슈 #1 B안 기각 근거**: Day 9 기록의 전제 "유실은 최다 문장 1줄"이 실측으로 틀렸다. 대시보드 5위젯이 전부 미표시되고, 유형 문구 풀 14개 중 6개가 `fun.*`를 참조하는데 선택이 `day` 숫자합 % 2로 결정적이라 해당 날짜엔 `{streak}` 리터럴이 화면에 노출되며, 부분 모드는 링크 생성 자체가 불가하다. 페이로드를 쿼리로 옮기면 유실이 0이지만 **쿼리는 서버 로그에 남아** 무업로드 원칙을 깬다(14가 "유일한 예외 = OG 쿼리"로 못 박음). 주 채널은 GeekNews·디스콰이엇·X이고 카톡은 부차 경로라 Day 18~19 채널 점검으로 이관했다.

  **npm publish 사전 검증**: 무인증 확인 가능 범위 전부 통과 — `bin` 대상 존재·shebang 보존, 0644 권한은 npm `bin-links`가 install 시 0755로 chmod하므로 블로커 아님, `files:["dist"]` 20파일 정상(루트 `.gitignore`의 `/cli/dist`는 패키지 루트 밖이라 무영향), LICENSE·README 자동 포함, 런타임 의존성 0, `prepublishOnly`가 build+test 강제. `npm view scored` → 404로 **이름 가용 확인**. 잔여 블로커는 인증 하나 — `npm whoami`가 E401이라 Day 5의 로그인이 만료됐다.

## 2026-08-27 (Day 10 추가) — npm 패키지명 정정: `scored` → `@jong-yeon/scored`

- **무엇을**: 2026-08-15에 확정했던 npm 패키지명 `scored`가 발행 불가로 판명되어 `@jong-yeon/scored`로 정정하고, 실행 명령 `npx scored`를 참조하던 코드·콘텐츠 11파일과 PRD 5문서를 전부 맞췄다.
- **어떻게**: `npm publish` 시도에서 `403 Package name too similar to existing packages: store, store2`를 받았다. 레지스트리상 `scored`는 비어 있었지만(`npm view scored` → 404) npm의 유사이름 정책은 등록 여부와 별개로 작동한다. 대안은 스코프명(이미 발행됨)과 무스코프 `scored-cli` 둘이었는데 문구 개정 작업량이 동일해 추가 인증 왕복·재거부 위험이 없는 스코프명을 채택했다(사용자 결정). 코드는 `npx scored` → `npx @jong-yeon/scored` 일괄 치환(11파일), PRD는 make-prd 경유로 값을 SSOT(11 카피사전)에만 두고 화면 문서 4곳의 **값 복제를 CPY-ID 참조로 교체**해 기존 SSOT 위반까지 함께 해소했다. PLAN·MVP는 make-plan 경유(개정 이력 1건, 조정 횟수 미산입).
- **왜**: 랜딩 복사 CTA(CPY-LAND-002)가 곧 이 명령 문자열이고, 그게 깔때기 1→2단계이자 킬 크라이테리아 1번(도달률)의 첫 관문이다. 명령이 404면 지표가 아니라 제품이 끊긴다. 과거 기록(`history.md`·`grill-log.md`·TODO의 체크 완료 항목)은 고치지 않고 남겼다 — 그때의 판단을 사후 수정하면 기록이 거짓이 된다. 대신 뒤집힌 결정에는 정정 포인터만 덧붙였다.
- **작업 결과**: 코드·콘텐츠 잔여 참조 0, PRD 잔여 0, 코드↔SSOT 5쌍 일치 검증. 웹 49 · CLI 33 tests 통과, `tsc --noEmit` · `next build` exit 0. 발행된 `0.1.0`은 구 문구(`사용법: npx scored`, `alias sc="npx scored"`)가 박힌 채 불변이고 npm은 버전 덮어쓰기를 허용하지 않으므로 **`0.1.1` 재발행이 잔여 완료 조건**이다. `license: "MIT"` 필드도 발행 과정에서 누락됐던 것을 함께 복원했다(LICENSE 파일은 tarball에 포함돼 있었으나 필드가 없어 npm 페이지에 미표기).

## 2026-08-27 (Day 10 추가 2) — OQ-005 종결 · 외부 눈 확보 폐지

- **무엇을**: ① GA4 측정 ID(OQ-005)를 실측 검증해 종결했다. ② 외부 눈 1명 확보를 폐지하고 본인 직접 검증으로 확정했다(사용자 결정).
- **어떻게**: ① `NEXT_PUBLIC_GA_ID=G-R6RZ92N1P7`이 `.env.local`·`layout.tsx:37`·프로덕션 HTML(`gtag('config','G-R6RZ92N1P7')`) 세 곳에서 일치함을 확인하고, **대조 실험으로 속성 실재를 검증**했다 — 존재하지 않는 ID(`G-XXXXXXXXXX`)로 gtag.js를 받으면 417KB에 ID가 요청 에코 1회만 나오는 반면, 실제 ID는 500KB에 2회 나온다. 차이 83KB가 Google이 프로비저닝된 속성에만 내려주는 전용 설정 페이로드다. ② PLAN 개정 이력 1건(make-plan) + Day 13 검증 세션 항목 + 실패 조건 9 본문 3곳을 고쳤다.
- **왜**: ① "측정 ID가 등록됐다"와 "그 ID가 살아 있는 속성이다"는 다른 명제다. 값이 문자열로 박혀 있는 것만 확인하면 오타 하나로 판정일(9/22)에 데이터가 0인 걸 그날 알게 된다. 대조군을 세운 이유가 그것이다. ② 안전장치 축소라 대가를 기록으로 남겨야 했다. PLAN이 스스로 적어둔 대로 **본인 테스트는 재확인이지 검증이 아니다** — 만든 사람은 이미 답을 알아 어디서 막히는지가 안 보인다. 3~5명 → 1명 → 0명으로 내려온 이상 실패 조건 9는 발동 상태로 확정되며, 보전 장치인 "Day 21 인터뷰 런칭 직후 앞당김"을 조건부에서 **확정 항목으로 전환**했다. 리스크가 사라진 게 아니라 런칭 이후로 이동했고, 이동한 리스크는 판정일 전에 회수해야 한다.
- **작업 결과**: OQ 7건 중 6건 완료(잔여 OQ-002 — Day 13 로그 보정). Day 10 Claude 담당 잔여는 EOD 기록 세션(Obsidian·Notion) 1건. 사용자 담당 잔여는 모션 감성 판정(Day 11~12 후)·실기기 수단(Day 16 밤)·런칭 직후 인터뷰(신설 확정 항목) 3건.

## 2026-08-27 (Day 10 추가 3) — GSAP 기각 철회: 검증 전에 재론 금지를 걸지 않는다

- **무엇을**: 같은 날 오후에 확정했던 "GSAP 미도입 · Day 11~12 재론 금지"를 **당일 철회했다**(사용자 결정). 기각을 기록한 4곳 중 3곳을 개정하고, 과거 기록 1곳은 정정 포인터만 남겼다.
- **어떻게**: `ANIMATION.md` §의존성을 "새 라이브러리 0 확정"에서 **"착수는 CSS + rAF, 라이브러리는 열어둔다"**로 교체하고 **도입 검토 조건 3개**를 명시했다 — ① CSS 키프레임으로 등급 릴 정지의 손맛이 안 나온다 ② 시퀀스 오케스트레이션(중단·스킵 되감기·구간 재생)이 rAF 수기 관리로 흐트러진다 ③ 60fps 검수에서 프레임이 떨어진다. 도입 시에도 LCP 2.5초 예산은 협상 대상이 아니며 `/report` 전용이므로 dynamic import로 초기 번들과 분리한다(공유 카드 `html-to-image`와 같은 방식). `TODO.md` Day 10 항목에는 `↩︎ 정정` 표기, `PLAN.md`는 make-plan 경유로 **개정 이력 4차 추가**(기존 Day 10 판정 ② 줄은 지우지 않음). `SCR-002 §13`의 "연출(GSAP)" 표기는 **개정을 취소해 원문 그대로 유효**하다 — 애초에 파일을 고치지 않았으므로 PLAN의 "미도입으로 개정" 서술만 무효가 됐고, 결과적으로 PRD↔코드 정합이 복구됐다.
- **왜**: 기각의 근거는 산술 검산(풀 3030ms · 축약판 1250ms)과 "필요한 모션 4종 중 CSS 불가는 카운트업 하나"라는 기능 목록 대조였다. 둘 다 **책상 위에서 나온 결론**이고, 실제 손맛과 60fps는 아직 한 줄도 구현해보지 않았다. 검증되지 않은 전제 위에 **"재론 금지"**를 걸면, Day 11~12에 CSS가 부족하다는 게 드러나도 **문서가 그 판단을 막는다.** 어제 확정이라 적은 것이 오늘 반증된 사례를 이미 두 건(이슈 #1 원인, npm 패키지명) 겪은 날이라 더욱 그렇다. 재론 금지는 **결정을 지키는 장치가 아니라 재검토 비용을 0으로 만드는 장치**여야 하는데, 검증 전에 걸면 반대로 작동한다. 기각 대신 **판정 조건을 문서화**해두면 Day 11~12에 "지금이 그 조건인가"만 보면 되므로 재론 비용은 그대로 낮게 유지된다.
- **작업 결과**: 4파일 개정(`ANIMATION.md`·`TODO.md`·`docs/PLAN.md`·`docs/history.md`). 코드 변경 0 — 아직 연출 구현 전이라 되돌릴 코드가 없다. PLAN 개정은 **조정 횟수 미산입**(기간·배분·스코프·킬 크라이테리아 전부 불변, 누적 조정 5회 유지). Day 4~9 구간의 "GSAP 코드 작성 금지"는 구현일 분리 규칙이라 **그대로 유효**하다.

## 2026-08-27 (Day 10 추가 4) — Day 10 EOD 기록 (Obsidian·Notion 당일 완료)

- **무엇을**: Day 10 한 날치 EOD. ① Notion「개발 로그:scored」**신규 6건 등록**(과정요약 ⑦ + TS-30~34) ② Obsidian **신규 TIL 5건 + 기존 노트 보강 1건** ③ 원고 백업 `_notion-원고-scored-Day10.md` ④ TODO 갱신.
- **어떻게**: 트러블슈팅 5건은 전부 오늘 실제로 막혔던 것과 1:1이다 — TS-30 npm 유사이름 403 / TS-31 보안키 2FA 발행 / TS-32 EVT-SHARE-002 enum 불일치 / TS-33 이슈 #2 빈 상태 미구현(**상태 `작성중`** — 원인만 규명, 수정은 Day 11) / TS-34 gtag.js 200 대조 검증. TIL은 TS와 1:1로 뽑되 **TS-30만 신규가 아니라 보강**으로 내렸다 — 기존 `TIL-레포-npm-도메인-이름은-세트로-움직인다`(8/15)의 코드블록에 `npm view <name> (404 = 미등록)`이 **확인 절차로 박혀 있고 오늘 그게 불충분함이 드러났다.** 새 파일을 만들면 틀린 확인법이 원본에 남아 다음에 또 그걸 읽는다. 5번째 신규 TIL(`검증-전에-재론-금지를-걸면-문서가-재검토를-막는다`)은 GSAP 기각 철회에서 나왔다. 반대로 **두 건은 의도적으로 안 썼다**: 래스터화 서브트리 연출 금지는 설계 근거일 뿐 실측이 없고, "CSS로 못 하는 것의 개수로 라이브러리를 정한다"는 기각 철회로 명제 자체가 미검증이 됐다 — 둘 다 Day 11~12 구현 후로 이월.
- **왜**: PLAN 기록 시스템 — 트러블슈팅 로그는 당일 필수. 오늘 5건의 공통 줄기는 **"확인했다고 적은 것이 실제로는 확인이 아니었다"**다. `npm view` 404는 등록 여부만 봤고(TS-30), `--otp` 재시도는 존재하지 않는 값을 반복해 넣은 것이었고(TS-31), enum은 문서만 읽고 코드 분기를 안 셌고(TS-32), 빈 상태는 유니온에 자리를 안 만들어 에러가 대신 먹었고(TS-33), gtag 200은 요청 수신만 증명했다(TS-34). 전부 **대조군·개수 대조·실제 쓰기**가 빠진 형태의 검증이다.
- **작업 결과**: Notion 누적 35 → **41건**(과정요약 7 + 트러블슈팅 34). Obsidian `dev-notes/` 신규 5파일 + 보강 1 + 원고 1, 위키링크 15개 전건 실재 확인(끊긴 링크 0). **Day 9의 제목 파싱 함정은 재발하지 않았다** — `\[…\]` 이스케이프로 생성한 6건 전부 정상이었고, 다만 **생성 API 응답과 SQL 쿼리 출력의 제목은 여전히 신뢰 불가**(중간 압축으로 단어가 빠져 보임)라 `notion-fetch` 개별 재확인이 유일한 검증 수단이었다. **8/25 등록분 6건도 개별 fetch로 전수 확인한 결과 전부 정상**이다 — 세션 중 "5건이 손상됐다"고 두 번 보고했으나 **둘 다 오독**이었다(SQL 조회 출력이 압축되며 잘린 제목을 실제 손상으로 읽음). 8/26 EOD의 정정 작업이 유효했다는 뜻이고, 복구 대상은 없다. Day 10 잔여 0.

## 2026-08-28 (Day 11) — 공개 연출 본체 구현 + 이슈 #2 근본 수정

- **무엇을**: 재미 장치 v1(BR-007)의 본체인 `/report` 공개 연출을 구현하고, 같은 커밋에서 이슈 #2(정상 링크가 "손상됐어요"로 표시)를 고쳤다. 신규 3파일(`src/lib/reveal.ts`·`src/lib/rows.ts`·`src/components/report/reveal-stage.tsx`), 개정 5파일, PRD 2문서.
- **어떻게**: ① **타임라인을 데이터로 표현했다.** `plan(entry, partial, rows)`가 모드 4종(full·link·partial·link+partial)을 `BUDGET` 테이블에서 뽑아 구간 시각을 계산한다 — 부분 모드에서 릴·pop이 사라지는 것은 분기 코드가 아니라 `reelSteps: 0`이라는 값이고, 행이 6줄에서 5줄로 줄면 총합이 3030→2940ms로 **자동 감산**된다(총합 하드코딩 금지). ② **`/tdd`를 등급 릴에 적용했다.** `reelIndex`의 시작점 역산 `((final-(steps-1))%len+len)%len`이 음수 모듈로로 새면 릴이 빈 칸을 돌거나 엉뚱한 등급에 선다 — 6등급 전부에 대해 마지막 스텝 착지를 테스트로 고정하고 RED를 확인한 뒤 구현했다. ③ **지표 행을 SSOT로 추출했다**(`rows.ts`). 카드는 `text(to)`, 연출은 `text(진행값)`을 쓰므로 카운트업이 끝나는 순간의 문자열이 카드와 **같은 함수**에서 나온다. ④ 이슈 #2는 호출부에서 빈 해시를 거르는 대신 **`decodeHash`의 계약**에 `error: null`(부재)을 추가해 손상과 분리했다. ⑤ 문구 공백은 make-prd 경유로 CPY-RES-004를 신설하고 SCR-002를 v1.3.0으로 올렸다.
- **왜**: ① 연출은 **시간이 곧 명세**인 기능이라 숫자가 코드 여기저기 흩어지면 검산이 불가능해진다. ANIMATION.md의 표를 그대로 옮긴 테이블 하나가 SSOT여야 "3~4초 상한"(SCR-002 AC-1)과 "1.5초 축약판"(AC-5)을 나중에도 대조할 수 있다. ② 릴 착지는 **재미 장치의 유일한 클라이맥스**다. 여기가 틀리면 서비스가 사용자에게 거짓 등급을 보여주고, 그건 눈으로 검증할 수 없다(16스텝이 800ms에 지나간다) — 랜덤·판정 로직에 `/tdd`가 필수인 이유가 정확히 이것이다. ③ 카드와 연출이 표기를 따로 계산하면 연출 마지막 프레임과 카드 첫 프레임에서 숫자가 튄다. ④ **호출부 분기는 증상 처치다.** 빈 해시가 "손상"이라고 말하는 주체는 `decodeHash`이므로 거기서 고쳐야 `result_failed` 오염(EVT-RES-003)이 원천 차단된다 — 지금은 호출부가 하나지만 계약이 거짓이면 두 번째 호출자가 같은 함정을 밟는다. ⑤ EL-RES-004는 PRD에 요소로 존재했는데 문구가 없었다. 값 없는 요소는 구현되지 않고, 실제로 8개월치 코드에서 이 상태만 빠져 있었다.
- **작업 결과**: 웹 **64 tests 통과**(reveal 14 신설 + payload TC-RES-001-05 신설), `tsc --noEmit`·`eslint`·`next build` 전부 exit 0. **의존성 추가 0** — ANIMATION.md의 GSAP 도입 검토 조건 3개(릴 손맛·오케스트레이션 붕괴·60fps 미달) 중 발동한 것이 없다. 브라우저 100ms 샘플링 실측으로 전 구간이 명세와 일치함을 확인했다: 빌드업 0~600 → 카운트업 700~1600(6지표 최종값 정확 착지) → 비트 1800(opacity 1.00→0.83→0.35) → 릴 회전 2100~2600 → **판정 등급 B+ 착지** → 3030 종료. 릴 폭은 6등급 전부 102.3px로 흔들림 0, 콘솔 에러 0. **미완 2건은 Day 12 이월**: 60fps 트레이스와 나머지 모드(스킵·`entry=link`·부분 모드·reduced-motion) 화면 캡처 — 세션 중 chrome-devtools MCP 서버가 브라우저 프로세스 정리와 함께 종료돼 재연결이 안 됐다. PLAN상 60fps 루프는 원래 Day 12 배분이라 일정 영향은 없다. **모션 감성 판정(사용자 육안 승인)은 미완**이며 축 패스의 완료 조건으로 남아 있다.

## 2026-08-28 (Day 11 추가) — 연출 검증 완료: 60fps 통과 · LCP 초과 발견(이슈 #3)

- **무엇을**: 세션 중 chrome-devtools MCP가 끊겨 Day 12로 이월했던 검증 6종을 **CLI로 전환해 전부 수행**했다 — 60fps 실측 · 스킵/Esc · `entry=link` 축약판 · 부분 모드 · reduced-motion · EVT-RES-002 실발화. 그 과정에서 **LCP 예산 초과를 발견**해 이슈 #3으로 등록했다.
- **어떻게**: MCP 서버 없이 `npx -y -p chrome-devtools-mcp@latest chrome-devtools <cmd>`로 같은 툴을 호출했다(`navigate_page --initScript`·`evaluate_script`·`take_screenshot` 전부 동작). 계측은 페이지에 심는 방식으로 통일했다 — rAF 델타 배열로 프레임 간격을, `PerformanceObserver(largest-contentful-paint)`로 LCP 요소 변천을, `setInterval` 100ms 샘플링으로 상태 전이를 각각 잡았다. 이벤트는 gtag 스텁이 GA 스크립트에 덮여 실패해 **`dataLayer` 직접 관찰**로 바꿨다. 측정은 dev와 **프로덕션 빌드(`next start`) 양쪽**에서 했다.
- **왜**: 60fps는 GSAP 도입 검토 조건 ③이라 **숫자로 닫아야 재론이 끝난다**. 그리고 "연출이 LCP를 막지 않는지"는 Day 10 인계에 적어둔 선확인 항목이었는데, 실제로 재보니 **막고 있었다** — 적어두지 않았으면 Day 17 기술 점검에서 처음 봤을 것이고 그때는 연출 설계를 되돌릴 시간이 없다. 이월 항목을 다음 날로 미루지 않고 수단을 바꿔 당일 끝낸 이유가 이것이다.
- **작업 결과**:
  - **60fps 통과** — 프로덕션 214프레임 중앙값 16.7ms · 최대 16.8ms · **17ms 초과 0 · 드롭 0**(dev 213프레임 동일). rAF 하나로 카운트업 6줄 + 릴 16스텝을 돌려도 프레임 예산에 여유가 있다. **GSAP 도입 검토 조건 ③ 미발동**, ①②도 미발동이라 의존성 추가 근거는 현재 0.
  - **전 모드 명세 일치** — `entry=link` 1250ms 정확 종료(카운트업 생략·릴만) · 부분 모드 1420ms(5행 = 500+80×4+600, 릴 미렌더) · reduced-motion은 **스테이지를 한 번도 마운트하지 않고** 102ms에 카드 · 스킵/Esc 모두 즉시 종료.
  - **EVT-RES-002 실발화 확인** — `result_reached{entry:"link"}` → `reveal_completed{skipped:false}` 순서로 dataLayer에 1회씩. 중복 0.
  - ⚠️ **LCP 초과 (이슈 #3)** — `entry=cli` **3484ms**로 예산 2.5초 초과. LCP 요소가 h1(84ms)에서 **카드 h2(3484ms)로 갱신**되어 연출 길이에 종속된다. 등급 릴은 `text-7xl`이지만 폭이 102px로 고정돼 면적이 h2보다 작아 후보가 되지 못한다. **`entry=link` 1600ms · reduced-motion 248ms는 통과** — 즉 초과하는 것은 CLI 자동 오픈 경로 하나이고 **외부 유입(공유 링크 수신자)은 예산 안**이다. ANIMATION.md가 정당화해 둔 것은 "UI 300ms 응답 예산"이지 LCP가 아니므로 별도 결론이 필요하다. 대안 3개(A 예외 명시 / B 빌드업 문구를 display 크기로 키워 LCP를 600ms 이전으로 당김 / C 연출 단축)를 이슈에 적었고, **B는 현재 `text-sm` 회색이라 존재감이 없는 빌드업 연출 자체를 개선하는 방향**이라 유력하다. Day 12 `improve-animations` 대상.
  - **부수 발견**: 데스크톱에서 빌드업 문구·지표 행은 좌측, 릴·버튼은 중앙이라 세로 축이 어긋난다 — PRD §16의 "md+ 중앙 스테이지(최대 폭 제한)"가 미구현. Day 12 인계.

## 2026-08-28 (Day 11 추가 2) — LCP 초과 당일 해결 + 육안 검증이 잡은 결함 2건

- **무엇을**: 이슈 #3(LCP 3484ms)을 등록 당일 해결하고, 그 과정의 스크린샷에서 **데이터로는 안 보이던 결함 2건**을 찾아 함께 고쳤다. `reveal-stage.tsx`·`reveal.ts`·테스트 2건·ANIMATION.md.
- **어떻게**: LCP 대안 3개 중 **B(빌드업 문구 확대)**를 골랐다. 근거는 `PerformanceEntry.size` 실측이다 — LCP는 요소 박스가 아니라 **글자가 실제 차지하는 영역**으로 크기를 재고, 카드 h2가 17,516인데 빌드업이 `text-sm`일 때 808이라 h2가 LCP를 가져갔다. `text-4xl md:text-5xl`(10,279)로는 **여전히 부족**했고 `text-6xl md:text-7xl`(22,188)에서야 h2를 넘겼다. 이 스케일은 DESIGN.md가 전면 히어로용으로 정의한 display 스케일이라 새 규격을 만든 것이 아니다. 그다음 스크린샷을 찍어보니 ① **지표 행이 0ms부터 보였다** — `animate-in`은 `animation-fill-mode`가 `none`이라 delay 동안 초기 상태가 적용되지 않는다 ② **릴이 시작 전 빈칸이었다** — `reelStep`에 하한 clamp가 없어 릴 구간 전 음수 경과가 음수 스텝이 되고 `GRADE_REEL[음수]`가 `undefined`였다. ②는 테스트 2건을 먼저 실패시킨 뒤 고쳤다. clamp를 넣자 이번엔 릴이 초기값 `S`로 1850ms 전부터 보여서, 릴 영역에 `reelStart` 시점 200ms 페이드인을 더했다(회전과 겹쳐 총합 불변).
- **왜**: ① LCP 2.5초는 CLAUDE.md에 박힌 규칙이라 **협상 대상이 아니고**, B는 명세를 하나도 바꾸지 않는다 — 타임라인·문구(CPY-RES-001)·릴 전부 그대로고 타이포 스케일 하나만 움직인다. A(예산 예외 명시)와 C(연출 단축)는 각각 프로젝트 규칙 완화와 재미 축 축소라 사용자 결정이 필요했는데, B가 통해서 **둘 다 불필요해졌다**. ② 두 결함은 **100ms 샘플링 데이터에는 그대로 찍혀 있었다** — `reel=`(빈 문자열)이 1850ms 전까지 반복됐고 행 값이 0으로 찍혔다. 그런데 숫자만 보고는 "아직 시작 전이니 당연하다"고 읽었다. 스크린샷을 열어서야 그게 **화면에 보이는 상태**라는 걸 알았다. 계측은 "무엇이 언제"를 알려주지만 "그게 보이느냐"는 알려주지 않는다. ③ 릴 페이드인은 명세에 없던 구간이지만, clamp 수정의 부작용(초기 등급 노출)을 덮으려면 필요했다. **등급을 미리 흘리면 3초를 쌓은 기대감이 무너진다** — 재미 장치의 유일한 클라이맥스가 릴 정지라는 전제에서 나온 판단이다.
- **작업 결과**: **LCP 3484ms → 52ms**(`entry=link` 48 · 부분 모드 48 · reduced-motion 248). 60fps 유지(216프레임 중앙값 16.7ms, 17ms 초과 1건 = 릴 페이드인 순간의 단발, 드롭 아님). 웹 **66 tests**(reveal 16), `tsc`·`eslint`·`next build` exit 0. 실측 타임라인이 명세와 일치: 0~600 **빌드업만** → 702 행 스태거 진입 → 1801 비트 → 1901 릴 등장 → 2701~3000 **B+ 고정** → 3030 종료. 이슈 #3 close. ANIMATION.md에 §LCP 절을 신설해 **"스테이지에 카드 h2보다 큰 텍스트가 하나는 있어야 한다"**를 규칙으로 고정했다 — 연출 길이나 카드 타이포가 바뀌면 재검산해야 하는 조건이라 근거 표(size 실측값)를 함께 남겼다.

## 2026-08-28 (Day 11 추가 3) — 모션 감성 판정 통과: 릴 감속 곡선이 라이브러리를 대신했다

- **무엇을**: 사용자 육안 판정을 받고 **릴 회전을 800ms·easeOutQuad → 1100ms·easeOutCubic**으로 조정했다. 그 결과 GSAP 도입 검토 조건 3개가 전부 미발동으로 확정됐다. 라이트모드 대비도 실측했다.
- **어떻게**: 첫인상은 통과("지금 좋아")했으나 릴에서 **"등급 측정이 너무 빨리 지나간다"**가 나왔다. 원인은 길이가 아니라 **감속 곡선**이었다 — easeOutQuad(제곱)에서 마지막 등급 체류가 `800 - 800×(1-√(1/16)) = 200ms`뿐이라, 등급이 정해지는 과정이 인지되기 전에 pop으로 넘어갔다. 세제곱으로 바꾸면 뒤쪽 스텝이 길어져 같은 조건에서 체류가 `1100 - 1100×(1-∛(1/16)) = 437ms`가 된다. 길이도 1100ms로 올려 총합 3330ms(명세 상한 4000ms 이내, 여유 670ms). 축약판(`link` 700ms)은 AC-5의 "1.5초"에 묶여 있어 **길이는 그대로 두고 곡선만 공유**했다. 라이트모드는 별도 구현이 필요 없었다 — `:root`가 라이트 기본이고 `@media (prefers-color-scheme: dark)`가 덮는 구조라 이미 동작한다(DESIGN.md 2026-08-18 결정). 다만 **라이트에서 실제로 본 적이 없어** 대비를 래스터 픽셀로 실측했다.
- **왜**: ① **부족했던 것이 라이브러리가 아니라 곡선이었다**는 점이 중요하다. "릴 손맛이 안 나온다"는 GSAP 도입 검토 조건 ①인데, 실제로 컷이 나온 순간 필요했던 건 gsap 23KB가 아니라 **`** 2`를 `** 3`으로 바꾸는 것**이었다. 조건을 "손맛이 안 나면 도입"으로 적어뒀다면 여기서 라이브러리를 넣었을 것이다. ② 슬롯머신의 손맛은 회전 속도가 아니라 **"곧 멈춘다"는 예고**에서 나온다 — 마지막 스텝이 짧으면 결과가 갑자기 튀어나온 것처럼 보이고, 3초를 쌓은 기대감이 착지 없이 끝난다. ③ 대비를 잰 이유는 오늘 빌드업 문구를 LCP 때문에 display 스케일로 키웠기 때문이다. `text-muted-foreground`를 72px로 쓰면 흰 배경에서 흐려질 수 있는데, 실측 4.79:1로 **일반 텍스트 기준(4.5:1)까지 통과**했다.
- **작업 결과**: 모션 감성 판정 **통과** — 축 패스의 완료 조건이 닫혔다. **GSAP 도입 검토 조건 3개 전부 미발동**: ① 조정 후 육안 통과 ② 4경로(스킵·Esc·자연 종료·reduced-motion)가 `finish()` 하나로 수렴하고 정리 대상은 rAF 핸들 1개 ③ 216프레임 중앙값 16.7ms·드롭 0. **재론 금지는 걸지 않았다** — Day 12 `review-animations`에서 새 근거가 나오면 다시 본다(2026-08-27 교훈). 라이트모드 대비 실측: 빌드업 4.79:1 · 등급 릴/지표 행 19.76:1 통과, **비트 디밍(opacity 0.35) 구간만 2.34:1로 미달**이다 — 라이트·다크 공통이고 `aria-hidden`이며 같은 값이 1.2초 뒤 카드에서 19.76:1로 다시 나오지만, 디밍을 0.5로 완화할지는 연출 강도와 맞바꾸는 판단이라 **Day 17 접근성 스팟 패스로 이관**했다. 웹 66 tests·`tsc`·`eslint`·`build` 통과.

## 2026-08-29 (Day 11 추가 4) — 테마 토글 v1 도입: 토글을 넣었더니 CSS가 줄었다

- **무엇을**: DESIGN.md가 2026-08-18에 **v2 backlog로 미뤄둔 테마 토글을 v1으로 당겼다**(사용자 결정). 최초 구현은 시스템 → 라이트 → 다크 3단 순환이었고, **같은 날 2단(라이트 ↔ 다크)으로 확정**했다(아래 추가 6). 신규 `src/components/theme-toggle.tsx`, `globals.css` 테마 블록 재작성, `layout.tsx` 인라인 스크립트, PRD 4문서.
- **어떻게**: 통상적인 방법은 `[data-theme="dark"]` 블록에 다크 변수 31개를 **세 번째로 복제**하는 것이다(라이트 `:root` + 미디어쿼리 + 속성 선택자). 대신 **CSS `light-dark()`**를 썼다 — 값을 쌍으로 묶어 `--background: light-dark(라이트, 다크)` 형태로 **변수 블록을 1벌**로 만들고, 전환은 `color-scheme` 한 줄로 한다. 결과적으로 토글을 추가했는데 CSS가 2벌 → 1벌로 **줄었다**. 상태의 진실의 원천은 `<html data-theme>` 하나이고, React는 `useSyncExternalStore`로 그걸 구독한다 — `useState`로 따로 들면 렌더 전에 실행되는 인라인 스크립트가 쓴 값과 갈라진다. FOUC 방지는 `<head>`의 **동기 인라인 `<script>`**로 했다(`next/script`의 `afterInteractive`는 첫 페인트 뒤라 반대 테마가 한 프레임 번쩍인다). Tailwind `dark:` 변형은 `@custom-variant dark`로 미디어쿼리와 `data-theme`을 **둘 다** 보게 했다.
- **왜**: ① **"JS 없이도 콘텐츠가 보여야 한다"**(CLAUDE.md 마크업 위생)가 토글을 v2로 미룬 원래 근거였다. `light-dark()` 구조에서는 스크립트가 죽으면 `data-theme`이 비고 `color-scheme: light dark`가 시스템을 따르므로 **테마는 살아있고 토글만 사라진다** — 규칙을 어기지 않고 기능을 넣는 길이 있었다. ② `dark:` 변형을 한쪽만 보게 하면 반드시 한쪽이 깨진다: 미디어쿼리만 보면 토글이 안 먹고, `data-theme`만 보면 JS 없을 때 안 먹는다. 7곳뿐이라 컴포넌트를 직접 고칠 수도 있었지만 그건 shadcn 원본을 건드리는 것이라 업데이트 때 충돌한다. ③ 라벨에서 조사를 뺐다 — "시스템 설정**로** 전환"이 실제로 나왔다. 라벨이 "설정"(받침 O)·"밝게"(받침 X)로 섞여 있어 조사 규칙이 하나로 안 잡힌다.
- **작업 결과**: 3단 순환·`localStorage` 저장·복원·`aria-label` 실측 확인(초기 `data-theme` 없음 → light → dark → 속성 제거). **이 3단 구성은 당일 2단으로 교체됐다 — 현행 코드는 아래 추가 6이 최신이다.** FOUC 방지 스크립트가 `<body>`보다 175바이트 앞에 위치함을 서버 HTML에서 확인. 라이트 렌더 실측: 빌드업 4.79:1 · 등급 릴/지표 행 19.76:1 통과. 웹 66 tests·`tsc`·`eslint`·`build` 전부 통과. PRD 반영: **F-008**(04 기능 목록) · **EL-COM-001**(04에 §전역 UI 절 신설 — 전역 요소는 화면 문서가 소유하지 않는다) · **CPY-COM-004**(11) · 14 §브라우저 저장(`localStorage.theme` 한 개, 성적 데이터는 저장 안 함). **backlog.md에 v2 항목이 실제로 등록된 적이 없어** 제거할 항목은 없었다 — DESIGN.md만 "v2 backlog"라고 적고 backlog에는 안 옮긴 상태였고, 이번에 v1으로 당기면서 그 불일치도 함께 해소됐다.

## 2026-08-29 (Day 11 추가 5) — `next start` 재시작 누락을 코드 결함으로 오인

- **무엇을**: 테마 토글 육안 검증 중 프로덕션(`:3002`)에서 **"버튼은 있는데 아무 반응이 없다"**는 보고를 받았다. 코드 문제가 아니라 **재빌드 후 `next start`를 재시작하지 않은 운영 실수**였다.
- **어떻게**: 같은 코드가 dev 서버(`:3001`)에서는 정상 동작한다는 사실이 결정적이었다. `next start`는 **시작 시점의 빌드 매니페스트를 메모리에 들고 있어서**, 재빌드로 청크 해시가 바뀌면 서버가 안내하는 스크립트 경로와 디스크의 실제 파일이 어긋난다. 그러면 SSR HTML은 정상으로 내려오고 **클라이언트 JS만 로드에 실패**한다 — 버튼은 그려지는데 이벤트 핸들러가 붙지 않는다. 서버를 재시작하고 클릭을 다시 재보니 3단 순환이 정상이었다(아이콘 20px, `data-theme` dark → 없음 → light, 배경색 실제 전환).
- **왜**: 아이콘 크기를 20px로 고친 뒤 `npm run build`는 돌렸지만 **재시작을 빠뜨렸다.** 그 전까지는 수정할 때마다 재시작을 했는데, 마지막 변경이 "아이콘 크기 한 줄"이라 가볍게 보고 건너뛴 것이 원인이다. 증상이 하필 **"UI는 멀쩡한데 인터랙션만 죽는"** 형태라 코드 결함처럼 보였다.
- **작업 결과**: 재시작 후 정상 확인. TODO Day 12 인계에 함정으로 기록했다 — **dev에서 정상이고 프로덕션에서만 무반응이면 이걸 먼저 의심할 것**. 검증 루프의 규칙으로는 "코드 수정 → 빌드 → **재시작** → 확인"이 한 묶음이어야 하고, 변경이 작다고 마지막 단계를 빼면 안 된다.

## 2026-08-29 (Day 11 추가 6) — 아이콘 표준 확정 + 토글 2단 전환 + hydration 해소

- **무엇을**: 육안 검증 중 나온 지적 3건을 한 흐름으로 처리했다. ① **아이콘을 lucide로 통일**(`27e3fa5`) ② **토글을 3단 → 2단**(`b93a218`, 사용자 결정) ③ **hydration 에러 해소 + 전 버튼 `cursor-pointer`**(`9131a5b`).
- **어떻게**: ① 감사해 보니 `lucide-react`가 theme-toggle에만 쓰이고 나머지는 유니코드(`▾`·`↑`)였다 — 폰트에 종속돼 렌더가 기기마다 갈리고 크기·굵기를 클래스로 못 잡는다. `ChevronDown`·`ChevronUp`으로 교체하고 테마 아이콘을 `Sun`·`Moon` 계열로 통일한 뒤 DESIGN.md에 **§아이콘**을 신설했다(lucide-react만, 유니코드 금지). ② 3단의 "시스템" 칸을 뺐다 — **시스템 추종은 선택 전 기본 동작으로 이미 살아 있으므로**(`data-theme` 미설정 시 `color-scheme: light dark`) 칸을 지워도 기능이 줄지 않는다. `Theme` 유니온이 `"light" | "dark"` 두 값이 되면서 `ORDER` 상수도 사라졌다. ③ hydration 불일치의 원인은 **우리 인라인 스크립트**였다 — 첫 페인트 전에 `<html>`에 `data-theme`을 심으니 서버 HTML과 갈린다. Next.js 공식 패턴대로 `suppressHydrationWarning` + `useLayoutEffect` 재적용으로 닫았다. `cursor-pointer`는 Tailwind v4가 preflight에서 버튼 커서를 `default`로 바꿔놓기 때문에 `button.tsx` base에 한 줄 추가했다(개별 호출부가 아니라 컴포넌트에).
- **왜**: ① 유니코드 아이콘은 "임시로 하나만" 쓰기 시작하면 계속 늘어나고, 나중에 걷어낼 때 **grep으로 안 잡힌다**(문자마다 다름). 규격을 문서에 박아야 다음에 안 샌다. ② 3단은 사용자가 "시스템"의 의미를 알아야 쓸 수 있는데, 그 상태는 **아무것도 안 눌렀을 때 이미 적용돼 있다** — 칸으로 만들면 설명이 필요한 기본값이 된다. ③ dev StrictMode 리마운트가 `<html>`을 JSX가 아는 속성만 남기고 재조정하면서 스크립트가 심은 속성을 지운다 — **프로덕션에 없고 dev에서만 나는** 종류라 어느 쪽을 믿을지 알고 있어야 했다.
- **작업 결과**: 2단 순환·시스템 추종 기본값·`localStorage` 복원 실측 확인. `aria-label`은 조사 없는 형식 유지(CPY-COM-004). 유니코드 아이콘 잔재 0(리포트 화면 grep). 웹 66 tests·CLI 33 tests·`tsc`·`eslint`·`build` 전부 통과. DESIGN.md에 **§아이콘**·**§커서** 신설, §다크모드 개정. PRD CPY-COM-004 문구를 2단에 맞춰 갱신.

## 2026-08-29 (Day 11 종료) — main 머지·배포·프로덕션 검증

- **무엇을**: `feat/day11-reveal`·`feat/theme-toggle` 두 브랜치를 main에 머지하고(`f193b01`) 배포한 뒤 프로덕션에서 실측했다. `finishing-a-development-branch` 스킬 경유.
- **어떻게**: 머지 **전**이 아니라 **머지된 결과에서** 전체 검증을 다시 돌렸다(웹 66 · CLI 33 tests · `tsc` · `build`). 통과 후 push → Vercel 자동 배포. 프로덕션 검증은 chrome-devtools CLI로 LCP·등급 판정·토글·아이콘·콘솔 에러를 한 번에 쟀다.
- **왜**: 브랜치에서 초록이었다는 것은 **그 트리**가 초록이었다는 뜻이지 머지 결과가 초록이라는 뜻이 아니다. 두 브랜치가 같은 파일(`report-client.tsx`·`globals.css`)을 건드렸으므로 머지 후 재검증이 필수였다.
- **작업 결과**: 배포 반영 **20초**. 프로덕션 실측 — LCP **1716ms**(로컬 52ms 대비 실네트워크 반영, **예산 2.5초 이내**) · 등급 `B+ · 56점` 정상 · 테마 토글·lucide 아이콘 3종·`cursor: pointer` 정상 · **콘솔 에러 0**. **이슈 #2·#3 close.** Day 11 커밋 13건(머지 포함). 브랜치는 레포 관행대로 보존.

## 2026-08-29 (Day 11 EOD) — Notion 8건 + Obsidian TIL 7건

- **무엇을**: Day 11(8/28~29 이틀치) 기록을 Notion「개발 로그:scored」와 Obsidian `dev-notes/`에 등록했다.
- **어떻게**: 원고(`_notion-원고-scored-Day11.md`)를 먼저 쓰고 승인받은 뒤 등록했다. TIL은 기존 99개를 grep해 중복을 거르고 **`reelStep` 하한 clamp(JS)와 `animation-fill-mode`(CSS)를 한 노트로 합쳤다** — 메커니즘은 다르지만 "대기 구간에 무엇이 보일지를 지정하지 않았다"는 원인 구조가 같고 같은 스크린샷이 둘을 동시에 잡았다. 1건은 신규 대신 **보강**으로 내렸다: `TIL-브라우저-저장소-복원은…`이 "린트는 `eslint-disable`로 닫는다"로 끝나는데 이번에 `useSyncExternalStore`로 **disable 없이** 같은 문제를 풀었고, 새 파일을 만들면 원본에 낡은 결론이 그대로 남는다.
- **왜**: Day 10에 "작성중"으로 남긴 TS-33(이슈 #2 원인 규명)은 Day 11에 해결했으므로 **새 페이지가 아니라 그 페이지를 종결**해야 이력이 한 줄로 이어진다.
- **작업 결과**: Notion 신규 7건(과정요약 1 + 트러블슈팅 6) + TS-33 `작성중 → 완료` = **누적 48건**. Obsidian 신규 6 + 보강 1 = **105파일**. 제목 7건을 `notion-fetch`로 전수 재확인 — **Day 9의 제목 유실 함정은 재현되지 않았다**(`\[…\]` 이스케이프 유효). 부수로 **Notion MCP가 대용량 요청과 `<html>` 태그가 든 코드블록을 Cloudflare에서 차단**하는 것을 확인해, 페이지를 1~2건씩 나눠 보내고 코드블록에서 원시 HTML 태그를 뺐다. history.md 정정도 함께 — "추가 4·5"의 날짜(08-28 → 08-29)와 **3단 순환 서술이 현행 2단과 어긋나던 것**을 바로잡았다.

## 2026-08-30 (Day 12) — 모션 감사 반영: 중앙 스테이지·공유 시트 전환·smooth scroll

- **무엇을**: `improve-animations` 감사로 축 패스 잔여 3건을 확정하고 구현했다 — ① PRD §16 "md+ 중앙 스테이지" 미구현(Day 11 인계 결함) ② ANIMATION.md 채택 표에 있었으나 미구현이던 공유 시트 열림·닫힘 전환 ③ 대시보드 앵커 텔레포트. `reveal-stage.tsx`·`share-sheet.tsx`·`globals.css`·ANIMATION.md.
- **어떻게**: 감사는 연출 본체를 재론하지 않았다 — 타임라인·이징·60fps·LCP는 Day 11에 근거가 고정돼 "이미 맞다"가 판정이다. ① 스테이지에 `md:max-w-md md:mx-auto` 두 클래스 — 행·릴·버튼이 448px 중앙 칼럼 한 축에 선다. ② 키프레임이 아니라 **트랜지션**으로: 빠른 재열림에서 현재 상태부터 이어가고(재시작 없음), 네이티브 `<dialog>`의 display·top-layer는 `transition-behavior: allow-discrete` + `@starting-style`로 붙잡았다. 모바일 slide-up 400ms quart / md+ scale 0.97 fade 250ms / 닫기는 더 짧게(비대칭 — 조작 응답은 빠르게) / reduced-motion은 transform을 죽이고 페이드 200ms만. **미지원 브라우저는 자동으로 현행(즉시 표시)으로 강하해 JS 분기가 0**이다. ③ `scroll-behavior: smooth` 한 줄(no-preference 가드). 검증은 chrome-devtools **CLI**(Day 11 인계대로 MCP 미사용) — 프리즈 스크립트(rAF no-op + `getAnimations().pause()` 동시 적용)로 중간 프레임을 잡았다.
- **왜**: ① §16은 PRD 명세이고 정렬 축 위반은 DESIGN.md가 버그로 취급한다. ② 공유 시트는 축 패스 우선순위 3(공유 순간)인데 채택 표에 적힌 채 구현이 누락돼 있었다 — 감사가 아니면 못 찾는 종류다(코드에는 결함이 없고 문서와의 차이만 있다). ③은 게이트 판정("네이티브로 충분")이 가리킨 한 줄이 실제로는 없던 것. 셋 다 새 설계가 아니라 **이미 내려진 결정의 이행**이라 재론 절차가 필요 없었다.
- **작업 결과**: 웹 66 · CLI 33 tests · `tsc` · `eslint` · `build` 전부 통과. 실측 — 60fps 252프레임 중앙값 16.7ms·초과 0·드롭 0 / LCP 44ms(빌드업 P 유지, max-w가 글리프 면적을 안 건드림) / 시트 열림 120ms 시점 translateY 148px(모바일)·scale 0.9763(md+) 진행 확인, 닫기 후 display가 전환 끝까지 유지 → 250ms 뒤 none. **GSAP 도입 검토 조건 3개 미발동 유지.** 빌드업 빈 줄은 A(유지)/B(제거) 스크린샷을 준비해 사용자 판정 대기 — B는 fade-out 종료 시 전체가 ~96px 점프한다. `review-animations`는 사용자 호출 전용 스킬로 확인돼 사용자 담당으로 이관.

## 2026-08-30 (Day 12 추가) — 자동 전환 폐지: 자연 종료 후 "결과 보기" 대기 (SCR-002 v1.4.0)

- **무엇을**: 연출이 자연 종료되면 자동으로 카드를 공개하던 것을 **"결과 보기" 버튼 대기**로 바꿨다(사용자 결정 2026-08-30). make-prd 경유 — SCR-002 v1.4.0(EL-RES-005·AC 개정·TC-RES-002-02) · CPY-RES-005 신설(11) · 08 상태전이 · 15 EVT-RES-002 발화 시점. 코드는 `reveal-stage.tsx`·`report-client.tsx`.
- **어떻게**: rAF 루프가 `t >= total`에서 `finish(false)` 대신 `end()`를 부른다 — EVT 발화 + 대기 진입만 하고 전이는 버튼이 한다. 스킵 버튼(ghost)이 그 자리에서 결과 보기(primary)로 교체돼 **유일 focusable이 유지**되고(§15), Esc는 `endedRef`로 분기해 연출 중엔 스킵·대기 중엔 결과 보기와 같은 경로를 탄다(stale closure 방지용 ref). 발화 책임 분리: `onEnded`(자연 종료 시 `skipped:false`)와 `onFinish`(스킵일 때만 `skipped:true`) — 클릭 시 중복 발화 0.
- **왜**: ① **EVT-RES-002를 클릭 시점으로 옮기지 않았다** — 이벤트의 SSOT 의미가 "공개 연출 종료"(재미 장치 발동)이고, 클릭 발화로 바꾸면 대기 중 이탈자가 완주 지표에서 빠져 Day 11 이전 데이터와 비교가 깨진다. 전이만 미루고 지표는 그대로 둔다. ② reduced-motion은 현행 유지 — 스테이지 자체가 미마운트라 버튼을 둘 곳이 없고, 접근성 경로에 클릭 관문을 추가하면 마찰만 는다. ③ 대기는 전 모드 공통(축약판·부분 모드 포함) — 모드별 분기를 만들지 않는다.
- **작업 결과**: 웹 66 tests·`tsc`·`eslint`·`build` 통과. 실측 4경로 — 자연 종료 후 카드 미공개·결과 보기 표시·EVT `skipped:false` **1회** / 클릭 → 카드 공개·중복 발화 0 / 연출 중 스킵 → 즉시 카드·`skipped:true` 1회 / 대기 중 Esc → 카드·중복 발화 0 (TC-RES-002-02 충족). LCP 무영향 — 클릭은 최초 인터랙션이라 LCP는 그전(빌드업 44ms)에 확정된다. `.dev-shots/day12-wait-state.png`.

## 2026-08-30 (Day 12 판정) — 모션 육안 판정 통과 + 빌드업 빈 줄 A안 확정

- **무엇을**: 축 패스의 완료 조건인 **모션 최종 육안 판정을 통과**했고, Day 11부터 열려 있던 **빌드업 빈 줄 유지/제거를 A(유지)로 확정**했다(사용자 결정). 코드 변경 없음 — 문서 고정만.
- **어떻게**: 판정 대상은 3건이었다 — ① §16 중앙 스테이지 정렬 ② 공유 시트 열림·닫힘 ③ 새로 넣은 결과 보기 대기. 전부 정상. 빈 줄은 A/B 스크린샷을 나란히 놓고 골랐다.
- **왜**: B(빈 줄 제거)는 `fade-out`에 `display:none`을 더해 자리를 접는 방식인데, **지표 행이 순차로 올라오는 도중 전체가 ~96px 점프**한다. 연출 중 레이아웃 이동은 카운트업의 착지점을 흔들어 읽기를 방해한다 — 빈 줄 하나가 그 점프보다 싸다. 이 판단 근거를 ANIMATION.md에 남겨 다음에 "빈 줄이 왜 있지"로 되돌아오지 않게 했다.
- **작업 결과**: **축 패스(Day 10~12) 완료 조건 충족.** Day 12 커밋 6건. 남은 것은 `/review-animations`(사용자 호출 전용 스킬)와 main 머지·배포.

## 2026-08-30 (Day 12 리뷰) — review-animations: 백드롭 desync 1건 검출·수정

- **무엇을**: PLAN 지정 스킬 `review-animations`로 Day 12 diff를 검수했다. 실결함 **1건(F1 백드롭 duration desync)**을 찾아 고쳤고, 권고 2건은 사용자 선택으로 남겼다.
- **어떻게**: 코드만 보고 단정하지 않고 `getComputedStyle(el, "::backdrop")`으로 실측했다. 데스크톱(1280px)에서 **다이얼로그 열기 250ms인데 백드롭은 400ms**, 닫기는 200ms vs 250ms였다. 원인은 `::backdrop` 규칙이 `@media (min-width: 48rem)`·`prefers-reduced-motion` 블록 **밖에만** 있어 두 미디어 오버라이드가 본체에만 걸린 것. 두 블록에 백드롭 duration을 추가해 묶었다. 수정 후 재실측 250/250·200/200.
- **왜**: 열기 400 / 닫기 250의 비대칭은 **의도**지만 본체 250 / 백드롭 400은 **누락**이다. 한 동작이 두 속도로 갈라지면 시트가 착지한 뒤 배경만 150ms 더 어두워져 "무거운 잔상"으로 읽힌다. 육안으로는 놓치기 쉬운 크기라 계산된 값을 직접 읽어야 잡힌다 — Day 11의 "계측은 무엇이 언제를 알려주지만 보이느냐는 알려주지 않는다"의 반대 사례다(이번엔 눈이 못 잡고 숫자가 잡았다).
- **작업 결과**: 웹 66 tests·`tsc`·`eslint`·`build` 통과. 리뷰 통과 항목 — 키프레임 대신 **트랜지션 + `@starting-style` + `allow-discrete`**(재열림 retarget) · `transform`/`opacity`만 · `scale(0.97)`(never `scale(0)`) · 비대칭 타이밍 · 중앙 모달 origin 면제 · `ease-in` 0건 · reduced-motion이 이동만 죽이고 페이드 유지 · 빈도 적합(연출 Rare·시트 Occasional). 잔여 권고 2건은 미적용: ① 결과 보기 버튼의 순수 페이드에 slide 추가(같은 컴포넌트에 fade-only 선례가 있어 위반은 아님) ② `scroll-behavior: smooth` 전역 스코프 축소.

## 2026-08-30 (Day 12 리뷰 반영) — 권고 2건 적용: 버튼 물리성 + 스크롤 스코프

- **무엇을**: `review-animations`가 선택으로 남긴 권고 2건을 반영했다(사용자 결정). ① `결과 보기` 버튼에 `slide-in-from-bottom-2` 추가 ② `scroll-behavior: smooth`의 스코프를 `html` → `html:has(:target)`로 축소.
- **어떻게**: ① 지표 행이 이미 쓰던 `fade-in-0 slide-in-from-bottom-2` + `ease-out` 조합을 그대로 맞췄다 — 새 규격을 만들지 않았다. ② 스코프를 좁히는 방법이 문제였다. CSS만으로 "이 링크를 눌렀을 때만"을 표현할 수는 없지만, **`:has(:target)`는 해시 대상이 실제로 존재할 때만 매칭**되므로 앵커 이동에만 걸린다. JS 스크롤(`scrollIntoView`)로 바꾸면 스코프는 정확해지지만 게이트 판정("네이티브로 충분")과 ANIMATION.md의 "JS 스크롤 금지"를 동시에 어긴다 — CSS 안에서 푸는 길이 있었다. 적용 전 `grep`으로 **프로그래매틱 스크롤이 0건**임을 먼저 확인했다(부작용 표면이 앵커 + 스크롤 복원 둘뿐임을 확정).
- **왜**: ① 순수 페이드는 "아무것도 없던 자리에서 생겨남"이라 물리성이 없다. 릴에 fade-only 선례가 있지만 그건 **초기 등급 은폐**라는 목적이 있고, 버튼은 새 어포던스의 등장이라 성격이 다르다. ② `html`에 그냥 걸면 **뒤로가기 스크롤 복원까지 애니메이션된다** — 의도는 앵커 1곳이었다.
- **작업 결과**: 웹 66 tests·`tsc`·`eslint`·`build` 통과. 실측 — `:target` 없음 → `scrollBehavior: auto`(즉시), `#dashboard` 활성 → `smooth` / 버튼 `enter` 200ms `ease-out` + slide 적용. `:has()` 미지원은 즉시 점프로 강하. **review-animations 지적 전건 종결.**

## 2026-08-30 (Day 12 종료) — main 머지·배포·프로덕션 검증

- **무엇을**: `feat/day12-motion-polish`(커밋 9건)를 main에 머지하고(`f1fa310`) 배포한 뒤 프로덕션에서 실측했다. `finishing-a-development-branch` 스킬 경유.
- **어떻게**: 머지 **전**과 **머지된 결과**에서 각각 전체 검증을 돌렸다(웹 66 · CLI 33 tests · `tsc` · `eslint` · `build`). 프로덕션 검증은 배포된 CSS 번들을 직접 받아 규칙 존재를 먼저 확인하고(`:has(:target)` 스코프 · 백드롭 오버라이드 3벌 · `allow-discrete`), 그다음 chrome-devtools CLI로 실동작을 쟀다. 프로덕션 경로가 `/_next/static/immutable/chunks/`라 로컬(`static/chunks`)과 달라 첫 폴링이 빈 값을 20회 반복했다 — 경로 가정을 실제 HTML로 확인하고 나서야 잡혔다.
- **왜**: 브랜치가 초록이었다는 것은 **그 트리**가 초록이라는 뜻이다(Day 11 교훈). 그리고 CSS는 빌드 파이프라인(lightningcss)이 `@starting-style`·`:has()`·중첩을 어떻게 내보내는지가 로컬 확인만으로는 안 끝난다 — **배포된 바이트를 읽어야** 규칙이 살아남았는지 안다.
- **작업 결과**: **프로덕션 실측 전건 통과** — 대기 상태(카드 미공개 + 결과 보기 버튼 + `slide-in-from-bottom-2` 적용) · EVT-RES-002 `skipped:false` **1회** · 클릭 → 카드 공개(`에러의 늪 탐험가`) 중복 발화 0 · 백드롭 동기(데스크톱 0.2s/0.2s) · `scroll-behavior` 대기 중 `auto` · **LCP 476ms**(빌드업 P, 예산 2.5초 이내) · **콘솔 에러 0**. Day 12 커밋 9건 + 머지 1건. 브랜치는 레포 관행대로 보존.

## 2026-08-30 (Day 12 EOD) — Notion 4건·Obsidian TIL 5건 등록

- **무엇을**: `eod` 스킬로 Day 12를 외부 기록에 남겼다. Notion「개발 로그:scored」**4건**(과정요약 1 + 트러블슈팅 TS-41~43) · Obsidian `dev-notes/` **5건**(신규 3 + 기존 보강 2). 원고는 `_notion-원고-scored-Day12.md`.
- **어떻게**: 원고 → 사용자 승인 → 등록 순서. TIL은 기존 119개를 먼저 grep해 중복을 피했다 — `@starting-style`·`allow-discrete`는 새 파일 대신 `TIL-네이티브-dialog-…`에, 프로덕션 자산 경로는 `TIL-프로덕션에-안-보인다는…`에 절로 붙였다(링크 그래프가 갈라지지 않게). 신규 3건은 ① `::backdrop`이 본체의 미디어 오버라이드를 못 받는다 ② `scroll-behavior: smooth`가 뒤로가기 복원까지 먹는다 ③ 지표 발화 시점을 UI 전이 따라 옮기면 과거 데이터와 비교가 깨진다.
- **왜**: TS-41~43은 셋 다 **다음 프로젝트에서도 재발하는 종류**다(의사요소 캐스케이드·CSS 스코프 과잉·배포 자산 경로 가정). Notion은 "이 프로젝트에서 무슨 일이 있었나", Obsidian은 "다음에도 쓸 지식인가"로 갈라 담았다.
- **작업 결과**: Notion 4건 등록 후 **제목 유실 재발** — `\[바이브 성적표\]` 이스케이프는 통했으나 em dash(`—`)와 일부 단어(`html에`, 백틱 안 `next`)가 마크다운 파싱에서 사라졌다. 저장값을 `notion-query-data-sources`로 재확인해 2건을 평문 제목으로 정정(백틱·em dash 제거). **Day 9 함정의 새 변종** — 이스케이프만으로는 부족하고 **등록 후 저장값 재확인이 필수**다. Obsidian 5건 위키링크 10개 전건 해석 확인.

## 2026-08-31 (Day 14) — 3축 리뷰 + 코드 성숙도 체크 통과, 발견 전건 반영

- **무엇을**: PLAN Day 13~14의 Claude 몫을 수행했다 — **3축 리뷰**(정확성 / 복잡도 / 스펙 준수, 병렬 서브에이전트) + **코드 성숙도 체크리스트**(보안 4종 포함) + **BR-009/010 재보정용 30일 로그 마이닝**. 발견 사항은 `feat/day14-review` 브랜치에서 전건 반영(15파일, +37/−133).
- **어떻게**: 축 1(정확성)은 치명/주의 0건·참고 1건(`encodeHash` `.catch()` 비대칭 → 대칭 적용). 축 2(복잡도)는 shadcn 스톡의 죽은 variant(button 6종·badge 3종·card 서브컴포넌트 4개)·미사용 CSS 변수 36줄(sidebar·chart·popover·accent·radius-2xl/3xl)·등급 목록 3중 정의를 지적 → 삭제하고 `judge.ts`에 `GRADE_ORDER` SSOT를 세워 reveal·og가 파생. 축 3(스펙 준수)은 실결함 1건 — **ERR-CLIP-001 폴백이 "아래 텍스트를 복사하라"면서 URL을 안 그렸다** → `fallbackUrl` 노출 구현. SCR-003 §16 md+ 지표 2열 미적용도 적용. dev 전용 `?primary=` 분기는 디버깅 규칙대로 제거. 마이닝은 `analyze`가 `now` 이후를 거르지 않아 ts 커트오프 필터를 씌워 일별 재현했다.
- **왜**: `Options` export 제거 권고는 **기각** — d.ts 선언 방출이 깨진다(exported 시그니처가 참조). 사용자 몫 판단 3건은 코드로 건드리지 않고 이관: ① 등급·유형 임계 보정(30일 실측에서 S·A+ 0회로 나와 드리프트로 보고 — ⚠️ **이 판정은 오측이었다. 같은 날 재측정으로 뒤집힘, 아래 Day 14 후속 항목 참조**) ② SCR-005 엣지 3 ↔ 07 API명세의 스펙 내부 모순(PRD 수정은 make-prd 경유 규칙) ③ `px-5` 등 DESIGN 스페이싱 표 밖 값의 체계적 사용(h-11 버튼 짝).
- **작업 결과**: 성숙도 체크 전항 통과 — any 0·`!` 6곳(전건 인덱스 보장/ref 패턴) / 중복·네이밍(rank·top 통일) / 커스텀 브레이크포인트 0 / ARCHITECTURE.md 실구조 갱신 / **보안 4종**: 번들 시크릿 0(NEXT_PUBLIC_GA_ID뿐)·DB 없음·`/api/og` 서버 검증 확인·`npm audit` 0건 + git 이력 시크릿 스캔 클린 / 재미 로직 테스트 통과. 최종 검증: 웹 66·CLI 33 tests·`tsc`(웹·CLI)·`eslint`·`build` 전부 통과. 커밋·머지는 사용자 확인 대기.

## 2026-08-31 (Day 14 후속) — OQ-002 종결: 등급 커브 재측정, 임계 무변경 확정

- **무엇을**: PRD 21 OQ-002의 잔여 항목("Day 13 게릴라 로그 보정", 담당 Claude)을 종결했다. 앞선 마이닝이 낸 "S·A+ 0회 = 커브 드리프트" 판정을 **재측정으로 뒤집고**, BR-009 앵커·등급 컷과 BR-010 임계를 **무변경으로 확정**했다.
- **어떻게**: 오측의 원인은 표본이 아니라 **컷오프 방식**이었다. 과거 일자를 재현하려고 컷오프를 `Date.now() - i일`로 잡았는데, 이러면 모든 과거일이 **실행 시각에서 잘린다** — 15:31에 돌리면 매일 저녁 활동이 통째로 빠져 프롬프트·토큰·활동시간이 동시에 깎이고, 세 축 평균인 점수가 상단부터 무너진다. 컷오프를 **대상일+1의 05:00 경계**(BR-011)로 바꿔 완결된 하루만 세도록 고쳤다. 같은 로그, 같은 코드인데 결과가 갈렸다.
- **왜**: 게릴라 테스터 폐지로 외부 로그가 0이라 원래 계획한 외부 표본 보정은 불가능하다. 그래서 차선으로 **확정(8/24) 이후 구간**인 8/3~8/30을 새로 돌려 분포 유지 여부만 확인하는 방식을 택했다. 임계를 건드리려면 근거가 필요한데, 재측정 결과가 확정 시 분포와 사실상 같아 **바꿀 근거가 없다** — 근거 없는 조정은 보정이 아니라 흔들기다.
- **작업 결과**: 활동일 27일(BR-002 미달 3일 제외 → 판정 24일) — 등급 **C1·B3·B+5·A8·A+3·S4**(확정 시 21일 기준 C2·B5·B+2·A7·A+2·S3), 유형 **밸런스11·장문4·한줄3·마라톤2·에러늪2·새벽1·다시요1**로 **7유형 전부 등장**. 전 등급 등장·쏠림 없음 → 코드 변경 0, PRD 08 §계산 규칙에 재측정 근거·방법 함정·잔여 리스크를 기록하고 21 OQ-002를 완료 처리. **잔여 리스크(수용)**: 표본이 여전히 제작자 1인이고 확정 구간과 기간이 겹쳐 독립 표본이 아니다. avgLen의 붙여넣기 편향은 미검증으로 남으며, BR-001이 판정일 이후 커브 변경을 막으므로 **런칭 직후 유저 인터뷰가 유일한 검증 창구**다.

## 2026-08-31 (Day 14 후속 2) — 출시기준 DoD 17항 실사, 피드백 채널 backlog행

- **무엇을**: ① 문의·피드백 채널을 `backlog.md`로 이관(사용자 결정) ② `docs/prd/19_출시기준.md`의 **체크박스 15항을 실사**해 근거와 함께 채웠다 — 런칭 8일 전인데 게이트 목록이 한 칸도 안 채워져 있었다.
- **어떻게**: 항목별로 "무엇을 보면 참인지"를 정하고 그것만 확인했다. 테스트로 되는 것(판정 로직·BR-004)은 TC 번호로, 배포 산출물로 되는 것(서버 렌더·OG·위생 파일)은 **프로덕션 HTML·PNG를 직접 받아서**, 런타임이 필요한 것은 실제로 터뜨려서. 특히 에러 바운더리는 `decodeHash`가 `v`만 검증한다는 점을 이용해 **`fun` 누락 + `prompts≥10` 해시**를 만들어 프로덕션에서 강제 예외를 냈다(`prompts<10`이면 BR-002 부분 모드로 빠져 judge가 호출되지 않아 안 터진다).
- **왜**: 체크박스를 근거 없이 채우면 목록이 있으나 마나다. 반대로 비워두면 **무엇이 진짜 구멍인지 모른 채 남은 5일을 쓴다**. 실사의 목적은 통과율이 아니라 잔여 항목의 성격(코드 결함인가, 수단 부족인가)을 가르는 것이었다.
- **작업 결과**: **11/15 완료, 코드 결함 0건.** 잔여 4건은 전부 수단·세션 대기 — CLI 수동 완주(Day 13 세션 미실시) / GA4 DebugView(Day 15~16) / LCP 재측정(Day 17, Day 12 실측 476ms로 여유) / **iOS Safari 순회(수단 미확보, 마감 Day 16 밤)**. 실사 중 확인된 것: 강제 예외에서도 `role=alert` + 안내 + 버튼 2개가 렌더돼 흰 화면이 아니었고, **그 경로에서도 해시가 주소창에서 제거**돼 BR-004가 에러 경로까지 지켜졌다. OG는 불량 파라미터에 기본 OG로 폴백(SCR-005 엣지 3). ⚠️ **CLI Windows 실행은 코드 리뷰(`open.ts` win32 `Start-Process`, `os.homedir()`)로 대체 — 실행 미검증 상태로 런칭한다**는 점을 DoD에 명시했다.

## 2026-08-31 (Day 15 선행) — Sentry 오류 수집 배선 + URL 스크럽 실증

- **무엇을**: PLAN Day 15~16 필수 항목인 오류 수집을 붙였다. `@sentry/nextjs@10.72.0` 설치 → `instrumentation-client.ts`(클라이언트)·`instrumentation.ts`(서버·엣지) 배선, **BR-004 URL 스크럽**, `/how` 고지, PRD 14·SCR-006 반영, `.env.example` 신설. DSN·알림 채널 설정은 사용자 몫으로 남겼다.
- **어떻게**: GA4와 같은 **게이팅 패턴** — `NEXT_PUBLIC_SENTRY_DSN`이 없으면 `init` 자체를 안 한다(실측: `window.__SENTRY__` 없음, 요청 0건). 프라이버시가 핵심이라 세 겹으로 막았다: ① 이벤트 `request.url`과 브레드크럼(`url`·`to`·`from`)을 `scrubUrl`로 통과시켜 해시·`?from` 제거 ② 세션 리플레이 0 (성적표 화면을 통째로 녹화하므로 도입 금지) ③ `tracesSampleRate: 0`·`sendDefaultPii: false`. 스크럽은 새로 짜지 않고 이미 있던 `strippedUrl`을 재사용했다.
- **왜**: `/report#<해시>`에는 하이라이트 원문이 실릴 수 있다. 오류 수집이 URL을 그대로 보내면 **BR-004가 무력화되고 `/how`의 "컴퓨터를 떠나지 않습니다"가 거짓말이 된다.** 그래서 배선과 고지를 같은 커밋에 묶었다 — 코드만 붙이고 문서를 안 고치면 화면이 사용자를 속이게 된다.
- **작업 결과**: **실측 2건이 설계를 바꿨다.**
  - ① **렌더 오류가 Sentry에 안 갔다.** React 오류 바운더리가 예외를 삼켜 `window.onerror`에 뜨지 않는다 — DoD가 신경 쓰는 바로 그 경로가 비어 있었다. `error.tsx`에 `captureException`을 넣어 해결. 로컬 수집기로 엔벨로프를 받아 확인: 수정 전 `session`만 도착 → 수정 후 `event`·`TypeError` 도착.
  - ② **URL 스크럽 실증.** 크래시가 주소창 정리보다 먼저 나 **해시가 URL에 남은 최악의 경우**를 재현한 뒤 전송 바이트를 검사했다 — `url` 필드는 `http://localhost:3111/report`(해시 제거), 브레드크럼 `to`는 `/report`, 해시 전체·앞 20자·`#`+40자 패턴 **전부 미검출**.
  - ③ **구조 회귀 1건 자체 수정.** `scrub.ts`가 `payload.ts`를 import해 엣지 런타임이 `DecompressionStream` 경고를 냈다 → 의존 방향을 뒤집어 `strippedUrl`을 `scrub.ts`(의존 0)로 옮기고 `payload.ts`가 아닌 쪽이 참조하게 했다. 경고 소멸.
  - ⚠️ **비용**: 랜딩 초기 JS **196.6 → 262.5 KB gzip (+65.9 KB, +33%)**. `bundleSizeOptimizations`(excludeTracing 등)는 Turbopack에서 **효과 0**이라 제거했다. Day 17 LCP 재측정의 감시 항목 — 예산 초과 시 동적 import로 임계 경로에서 뺀다.
  - **실 DSN 확인 (2026-08-31 밤)**: 사용자가 Vercel·`.env.local`에 DSN·`SENTRY_ORG`·`SENTRY_PROJECT`·`SENTRY_AUTH_TOKEN`을 설정 → 로컬 프로덕션 빌드로 실오류 1건 발생 → `ingest.us.sentry.io` **200 × 10건 수신**, 소스맵 85개 생성. `disableLogger`도 **deprecated + Turbopack 미지원** 경고가 떠 제거했다(`bundleSizeOptimizations`와 같은 이유 — 동작 안 하는 설정은 남기지 않는다). 저장된 이벤트의 URL 필드를 Sentry API로 재확인하려 했으나 **403** — 토큰이 소스맵 업로드 권한만 가진 최소 권한이라 정상이다. 스크럽 증명은 로컬 수집기 바이트 검사로 갈음하고, UI 확인은 사용자 몫으로 남겼다.
  - 검증: 웹 69(신규 3)·CLI 33 tests·`tsc`·`eslint`·`build` 통과.

## 2026-08-31 (Day 15 선행 종료) — Sentry main 머지·배포·프로덕션 검증

- **무엇을**: `feat/day15-sentry`(커밋 4건)를 main에 머지(`c66b21c`)하고 배포한 뒤 프로덕션에서 실측했다. 사용자 승인 후 진행 — 랜딩 JS +33%와 `/how` 공개 문구 변경이 걸려 있어 앞선 문서 커밋들과 달리 확인을 받고 머지했다.
- **어떻게**: 머지된 트리에서 전체 검증을 다시 돌리고(웹 69·CLI 33 tests·`tsc`·`eslint`·`build`), 배포 후에는 `/how` 고지 문구가 실제 HTML에 나오는지 폴링한 뒤(4회차 반영) 배포된 청크를 직접 받아 DSN 주입을 확인했다. 프로덕션 자산 경로가 `/_next/static/immutable/chunks/`라 로컬(`static/chunks`) 패턴으로 잡으면 0건이 나온다 — Day 12에 겪은 함정이 그대로 재발해 패턴을 고치고 나서야 잡혔다.
- **왜**: 배선이 로컬에서 도는 것과 배포된 번들에 DSN이 실려 도는 것은 다른 문제다. 특히 `NEXT_PUBLIC_*`는 **빌드 시점에 값이 박히므로**, Vercel 환경변수가 프로덕션 빌드에 실제로 들어갔는지는 배포된 청크를 열어봐야 안다.
- **작업 결과**: **프로덕션 실측 전건 통과** — `/how` 고지 4개 문구 노출 · 배포 청크(`0ftwzhnx72t68.js`, 126KB)에 **DSN 주입 확인** · 강제 예외 → `role=alert` + ERR-APP-001 안내 렌더(흰 화면 아님) · `window.__SENTRY__` 활성 · **`ingest.us.sentry.io` 200 × 7건** · 주소창 URL 24자(`https://scored.kr/report` — 해시 제거). 랜딩 초기 JS는 프로덕션 실측 **269 KB gzip**(로컬 측정 262.5 KB와 정합). Day 17 LCP 재측정의 감시 항목으로 남긴다.
- **잔여**: ① 서버·엣지 `onRequestError` 경로는 실오류를 강제하지 못해 **런타임 미검증** ② Sentry 알림 채널 도달 확인은 사용자 몫(DoD 미완) ③ 테스트 이슈 정리 필요 — 안 지우면 런칭 후 실오류와 섞인다.

## 2026-09-01 (Day 15) — Vercel 환경변수 민감도 정리 + 운영 함정 기록

- **무엇을**: 사용자 지적으로 Vercel 환경변수 민감도를 점검해 정리했다. `SENTRY_AUTH_TOKEN`을 **Sensitive + Production 전용**으로 재등록(사용자 직접 입력), `NEXT_PUBLIC_SENTRY_DSN`을 Production 전용으로 축소. 정책과 함정을 PRD 14 §8·"운영 함정"과 `.env.example`에 기록했다.
- **어떻게**: `vercel env ls`로 실상태를 먼저 봤더니 **민감도 플래그가 정확히 뒤집혀 있었다** — 진짜 시크릿인 `SENTRY_AUTH_TOKEN`은 Non-sensitive(대시보드 평문 조회·`env pull` 가능), 브라우저 번들에 박히는 공개값 `NEXT_PUBLIC_GA_ID`는 Sensitive. git 이력·추적 파일에서 토큰 패턴을 스캔해 유출 0건을 확인한 뒤 순서를 잡았다.
- **왜**: 시크릿을 "되읽을 수 없게" 만드는 것이 목적인데, 잠긴 쪽이 공개값이고 열린 쪽이 시크릿이면 정책이 반대로 작동한다. DSN을 Production 전용으로 좁힌 것은 별개 이유 — 프리뷰 배포 오류가 프로덕션 이슈와 섞이면 런칭 후 신호를 못 읽는다.
- **작업 결과**: 최종 상태 — `SENTRY_AUTH_TOKEN` Sensitive/Production · `NEXT_PUBLIC_SENTRY_DSN` Non-sensitive/Production · `SENTRY_ORG`·`SENTRY_PROJECT` Non-sensitive · `NEXT_PUBLIC_GA_ID`는 Sensitive 유지(무의미하나 무해, 재등록 실패 시 GA4가 끊겨 런칭 8일 전에 건드릴 이유가 약함).
  - ⚠️ **사고 1건**: `vercel env rm NEXT_PUBLIC_SENTRY_DSN preview`가 Preview 타깃만이 아니라 **변수를 통째로 지웠다.** 도움말이 `env remove name [environment]`라 환경별 삭제로 읽힌 탓 — 같은 일이 사용자 실행분까지 합쳐 하루 2회 발생했다. 즉시 재등록해 복구했고 결과 상태는 목표와 동일. **`NEXT_PUBLIC_*`는 빌드 시점에 박히므로 기존 배포는 멀쩡하고 다음 배포부터 조용히 꺼진다** — 즉시 티가 안 나는 종류의 사고라 기록해 둔다.
  - **자기 정정 1건**: 로컬 `.env.local`의 `SENTRY_AUTH_TOKEN` 제거를 권했으나, 확인해 보니 `@sentry/cli` postinstall이 차단돼 **바이너리가 없어 로컬 업로드가 애초에 불가능**했다. "로컬 빌드가 릴리스를 어지럽힌다"는 근거가 성립하지 않아 철회하고, 위생 차원의 선택 사항으로 낮췄다.
  - 시크릿 입력 경로: Vercel Claude Code 플러그인이 CLI를 비대화형으로 만들어 `--value`를 요구한다 → 히스토리·대화 기록에 평문이 남으므로 **대시보드나 별도 터미널에서만** 입력한다.

## 2026-09-01 (Day 15) — 장애 대비 3종 + E2E 1개 + GA4 EVT 9종 실발화 검증

- **무엇을**: PLAN Day 15~16 중 내가 할 수 있는 항목을 처리했다. ① `RUNBOOK.md` 신설(롤백 절차·백업 정책 N/A 근거·장애 시 행동 3줄) ② Playwright 도입 + **핵심 플로우 E2E 1개** ③ **GA4 EVT 9종 실발화를 네트워크 레벨로 검증** ④ Day 17 사전 훑기 ⑤ `docs/agents/issue-tracker.md` 낡은 전제 정정.
- **어떻게**:
  - **장애 대비**: 문서를 쓰기 전에 수단부터 확인했다 — `vercel rollback` 존재·배포 이력 13건 보존·도메인 `scored.kr`·npm `0.1.1`. 웹과 CLI의 롤백 성격이 다르다는 점을 갈랐다: 웹은 별칭 이동(수십 초), **npm은 되돌리기가 없어 `deprecate` + 패치 재발행이 곧 롤백**이다(버전 덮어쓰기 불가·72시간 후 unpublish 불가).
  - **E2E**: 해시 픽스처를 하드코딩하지 않고 `encodeHash(SAMPLE)`로 **실제 URL을 생성**해 `/report?from=cli#…` 진입 → 연출 → `결과 보기` → 카드까지 통과시키고, 마지막에 **주소창에 `#`·`from=cli`가 없음**을 단언했다(BR-004). 브라우저는 chromium 1종 — 호환은 Day 17 실기기 순회 몫이다.
  - **GA4**: DebugView 육안 대신 `/g/collect` 히트를 가로채 `en`(이벤트명)·`ep.*`(파라미터)를 직접 뜯었다. 육안보다 강한 증거이고, 15 §5의 "성적 데이터 전송 금지"를 바이트로 확인할 수 있다.
- **왜**: 장애 대비는 런칭 당일이 아니라 **런칭 전에 수단이 있는지**를 확인하는 작업이다. GA4는 킬 크라이테리아 판정의 유일한 정량 근거인데(외부 눈 0명 결정으로 정성 근거가 없다), 이벤트가 안 나가면 9/22에 판정할 데이터 자체가 없다.
- **작업 결과**: **EVT 9종 + 파라미터 전 변형 발화 확인.** `entry` cli/link · `has_highlights` T/F · `skipped` T/F · `error_code` ERR-HASH-001/002 · `method` clipboard/share_sheet·download/share_sheet · `highlight_included` T/F. 전송된 `ep.*` 값에 등급·유형·점수·하이라이트 **없음**(14 §5 준수). DoD **11/16 → 12/16**.
  - **오판 1건 자체 정정**: 중간에 "복사 후 즉시 이탈하면 이벤트가 유실된다"는 결론을 냈으나 틀렸다. GA4는 ~5초 배치 지연을 두고 이탈 시 `pagehide`에서 비운다 — 페이지를 살려둔 채 `pagehide`만 발생시키자 같은 이벤트가 POST로 잡혔다. **Playwright가 teardown 중 요청을 리포트하지 못한 캡처 한계**였다. 마찬가지로 "랜딩 2종 미발화"도 프로브가 한 테스트에서 `pagehide`를 반복 발생시켜 생긴 오염이었고, 페이지당 1테스트로 격리하자 전건 잡혔다. **관측 도구의 한계를 제품 결함으로 읽을 뻔했다.**
  - **Day 17 사전 훑기**: 프로덕션 `robots.txt`·`sitemap.xml` 200 · `html lang="ko"` · 랜딩/`/how` title·description·OG·Twitter 카드 전부 정상. **구멍 2개** — `canonical` 링크 없음, **JSON-LD 없음**(PLAN에만 있고 PRD 미명세 → 추가 시 make-prd 경유 필요). 둘 다 Day 17 몫으로 남긴다.
  - ⚠️ **GA4 속성 오염**: 검증 과정에서 실 측정 ID로 테스트 이벤트 수십 건이 들어갔다(2026-09-01). 런칭 전이라 판정 구간(9/8~9/22) 밖이지만, 지표를 볼 때 이 날짜를 제외해야 한다.
  - 검증: 웹 69·CLI 33 tests · E2E 1 · `tsc` · `eslint` 통과.


## 2026-09-01 (Day 15) — canonical·JSON-LD (Day 17 갭 2건 선처리)

- **무엇을**: Day 15 사전 훑기에서 나온 구멍 2개 — `canonical` 링크 없음, JSON-LD 없음 — 을 메웠다. 둘 다 PLAN Day 17 항목인데 **PRD에 명세가 없어** 구현 근거가 비어 있던 상태였다. `04_정보구조`에 **메타데이터·색인 규약**을 단일 원본으로 신설하고(make-prd 경유), 4라우트 canonical + 랜딩 JSON-LD를 구현했다.
- **어떻게**: 값은 04 한 곳에만 두고 화면 문서 §13(SCR-001·002·006)은 참조만 하게 했다. 랜딩 `description`은 `layout.tsx`에서 `SITE_DESCRIPTION`으로 export해 메타 태그와 JSON-LD가 같은 문자열을 쓴다 — 복제하면 문구 변경 때 갈라진다. JSON-LD는 `next/script`가 아니라 네이티브 `<script type="application/ld+json">`(Next.js 가이드: 구조화 데이터는 실행 코드가 아니다).
- **왜**: 판단이 필요했던 것은 `/report`다. 해시에 개인 활동 데이터가 실리는 화면이라 ① canonical은 **쿼리·해시를 뺀 경로만** 가리키게 하고(`?from=cli`·`?from=report`는 GA4 세그먼트용일 뿐 별개 문서가 아니다 — 쿼리별로 색인되면 크롤 예산이 갈리고 유입 판정도 흐려진다) ② **JSON-LD를 금지**했다. 성적 값을 구조화 데이터로 내보내면 14 §5(성적 데이터 전송 금지)를 우회하는 경로가 된다. `noindex`는 **넣지 않기로** 했다 — 색인 가치는 없지만 사이트맵 제외로 충분하고, SNS 크롤러가 `robots` 메타를 어떻게 다루는지 보장할 수 없다. 공유 미리보기(F-005)가 v1 유입의 주 경로라 잃을 위험이 더 크다.
- **작업 결과**: 로컬 프로덕션 빌드 실측 — `/`·`/?from=report` → `https://scored.kr` · `/how` → `/how` · `/report`·`/report?from=cli` → `/report` (**쿼리 제거 확인**), 랜딩 JSON-LD 서버 렌더(JS 없이 노출), `/report` JSON-LD **0건**.
  - **표기 정정 1건**: PRD에 `https://scored.kr/`로 적었으나 Next `metadataBase` 정규화가 슬래시를 떼 `https://scored.kr`로 렌더된다 → JSON-LD `url`과 PRD 표기를 실제 출력에 맞췄다. 문서와 구현이 한 글자라도 다르면 Day 17 검수 때 무엇이 정답인지 알 수 없다.
  - **E2E 5건 추가**(`e2e/seo.spec.ts`) — 메타데이터는 조용히 사라지는 종류라 육안 검수만으로는 회귀를 못 잡는다. canonical 4라우트 + JSON-LD 유무 2화면.
  - 검증: 웹 69 · CLI 33 tests · **E2E 6** · `tsc` · `eslint` · `build` 통과. DoD 위생 항목에 근거 기입.
  - **프로덕션 실측** (머지 `cf72a9e` 배포 후): 5개 URL canonical 전건 일치 · 랜딩 JSON-LD 노출 · `/report` 0건 · 동적 OG 200 PNG(회귀 없음).

## 2026-09-02 (Day 16) — Day 17 사전 감사: LCP 통과 + 다크 모드 대비 결함 1건 수정

- **무엇을**: Day 17 기술 점검 항목(Lighthouse·LCP·접근성 스팟)을 하루 앞당겨 돌렸다. LCP는 예산 통과, 접근성에서 **다크 모드 대비 미달 1건**이 나와 토큰을 분리해 고쳤다. `llms.txt`는 backlog행.
- **어떻게**:
  - **LCP**: 데스크톱 무스로틀 223ms, **모바일 390px·Slow 4G·CPU 4x에서 705ms** (예산 2.5초의 28%), CLS 0.00. Sentry 도입으로 랜딩 JS가 +33%(269KB gzip) 늘어난 뒤의 수치라 감시 항목을 해소했다.
  - **접근성**: 랜딩·`/how`·`/report`(카드 상태) 3화면을 모바일 Lighthouse로 감사. `/how`가 **다크에서만 96** — GitHub 링크가 `text-primary`로 다크 배경 위 **3.24:1**(SC 1.4.3 4.5:1 미달). 라이트는 100이라 **다크 전용 결함**이었다.
- **왜**: 원인은 링크 하나가 아니라 **토큰의 용도 충돌**이었다. `--primary`가 텍스트 색과 배경색 양쪽에 쓰이는데 요구가 정반대다 — 실측: 다크 L을 0.48→0.60으로 올리면 텍스트는 5.34로 통과하지만 **버튼 흰 글자가 3.48로 깨진다**. 한 토큰으로 동시 만족이 불가능하다는 게 수치로 나와 **텍스트 전용 `--primary-text`를 신설**했다(다크 `oklch(0.65 0.14 150)` — 배경 위 6.45:1, 카드 위 5.71:1). `text-primary` 사용처는 3곳(`/how` 링크 · 복사 완료 알림 · 공유 완료 알림)이고 **셋 다 같은 결함을 공유**했다 — 알림 2곳은 버튼을 눌러야 나타나서 Lighthouse가 못 잡는다. 링크만 고쳤으면 나머지 2곳은 조용히 남았다.
  - DESIGN.md의 기존 `--primary` 대비 기록(라이트 5.22 / 다크 5.73)이 **버튼 조합 기준이었고 텍스트 용도는 검증된 적이 없었다**는 점을 함께 적었다. 그게 이 결함이 8/20부터 미검출로 남은 이유다.
- **작업 결과**: `/how` 다크 접근성 **96 → 100**, `color-contrast` 감사 통과(score 1). 랜딩·`/report` 다크도 접근성 100. 실측 재확인 — 링크·알림 색이 실제 렌더에서 **6.45:1**.
  - **오판 방지 1건**: `/report` snapshot 감사가 `meta-description` 없음으로 떴으나, DOM과 프로덕션 HTML 양쪽에 **실제로 존재**했다. snapshot 모드가 navigation 아티팩트를 수집하지 않아 생긴 도구 한계다(GA4 때와 같은 패턴).
  - **backlog행**: `llms.txt`(Agentic Browsing 67점 유일 실패) — v1 스코프 동결 대상.
  - 검증: 웹 69 · CLI 33 tests · `tsc` · `eslint` · `build` 통과.
  - **프로덕션 실측** (머지 `9f82f5a` 배포 후): `/how` 다크 접근성 **100** · LCP **708ms**(모바일 Slow 4G·CPU 4x)·CLS 0.00. DoD **12/16 → 13/16**(LCP 항목 해소).

## 2026-09-02 (Day 16) — PLAN 마일스톤 실사 동기화 + Day 라벨 정정

- **무엇을**: 실제 진척과 `docs/PLAN.md` 마일스톤 체크박스가 어긋난 것을 맞췄고, `docs/history.md`의 Day 라벨 오기 2건을 고쳤다. 롤백 리허설(RUNBOOK §4)은 착수했으나 **실행 단계에서 차단**돼 미실시로 남았다.
- **어떻게**:
  - **PLAN 체크박스**: `Day 9`(전 화면 정적 완성·컷 판정)와 `Day 12`(축 패스·지식 총정리)가 `[ ]`로 남아 있었다. 각각 2026-08-26 컷 판정(컷 대상 없음)과 2026-08-29~30 모션 감성 판정 통과 + Obsidian 5건·Notion 4건 등록으로 **이미 완료된 항목**이라 근거를 달아 `[x]` 전환. `Day 16`·`Day 17`은 미완이라 체크하지 않고 **진척 서브라인**만 달았다 — 무엇이 끝났고 무엇이 누구 몫으로 남았는지가 한 줄에 보여야 남은 6일에 착오가 없다.
  - **Day 라벨**: 일정 매핑이 `Day N≥6 = 8/17+N`이므로 9/1 = Day 15, 9/2 = Day 16이다. 9/1 엔트리 3건 중 2건이 `(Day 16)`으로 적혀 있었다(자정 넘겨 작업하다 붙인 오기). `(Day 15)`로 정정.
- **왜**: 라벨은 장식이 아니라 **마감 계산의 입력값**이다 — iOS Safari 실기기 수단 확보 마감이 "Day 16 밤"으로 적혀 있는데 Day 16이 9/1이면 마감이 이미 지난 것이고 9/2면 오늘 밤이다. 한 글자 차이로 마감 판정이 뒤집힌다. 체크박스도 같은 이유다: `Day 9`·`Day 12`가 미완으로 보이면 런칭 6일 전 잔여 목록이 실제보다 부풀어 우선순위 판단이 틀어진다.
- **작업 결과**: PLAN 마일스톤 실사 일치 — 잔여 미완은 **`Day 13`(검증 세션, 사용자 몫) · `Day 16` · `Day 17` · `Day 19~21`**뿐이다. history 라벨 정정 2건.
  - ⚠️ **롤백 리허설 미실시 유지**: `vercel rollback`이 실행 환경의 자동 승인 분류기에 차단됐다. 우회하지 않았다. 사전 준비는 끝난 상태 — 현재 프로덕션 별칭 `dpl_5cYrdXTLeTPCqRWACv2hZFEiMNWn`(`scored-7pc2ygcid`, `cefeedb`), 직전 배포 `scored-c3w8q08zh`(`9f82f5a`), baseline `scored.kr` 200 확인 완료. 직전 배포와의 차이는 **문서 커밋 1개뿐**이라 화면 변화 0인 최적의 리허설 창이다. 권한 허용 또는 사용자 직접 실행 필요.

## 2026-09-02 (Day 16 후속) — 롤백 리허설 실시

- **무엇을**: 바로 위 엔트리에서 "미실시 유지"로 남겼던 `RUNBOOK.md §4` 롤백 리허설을 실시했다. 실행 환경의 승인 분류기가 `vercel rollback`을 막아 **사용자가 직접 4개 명령을 실행**했고, 결과 상태는 내가 실측 검증했다.
- **어떻게**: 직전 배포(`scored-c3w8q08zh`, 커밋 `9f82f5a`)로 롤백 → `scored.kr` 200 → 현재 배포(`scored-7pc2ygcid`, 커밋 `cefeedb`)로 롤포워드 → 200. 종료 후 `vercel inspect https://scored.kr`으로 별칭이 원래 배포 `dpl_5cYrdXTLeTPCqRWACv2hZFEiMNWn`으로 복귀했음을 확인했다.
- **왜**: 롤백은 RUNBOOK §1 "행동 3줄"의 2번이자 장애 대응의 **유일한 1차 수단**이다. 문서에 절차만 적어두고 한 번도 돌려보지 않으면, 처음 실행하는 순간이 하필 장애 한복판이 된다. 두 배포의 차이가 문서 커밋 1개뿐이라 **화면 변화 0**인 지금이 절차만 따로 검증할 수 있는 창이었다.
- **작업 결과**: 4단계 전부 200, 별칭 복귀 확인. `RUNBOOK.md §4` `[ ] 미실시` → **`[x] 실시 완료`**(단계별 대상·결과 표 첨부). PLAN Day 16 진척 서브라인에서 롤백 리허설 항목 해소 — **Day 16 잔여는 Sentry 알림 채널·GA4 DebugView 육안 2건이며 둘 다 사용자 계정 설정**이다. 이로써 "장애 대비 3종"(롤백 / 백업 정책 N/A / 행동 3줄)이 문서·실증 양쪽에서 닫혔다.

## 2026-09-02 (Day 16) — Sentry 알림 채널 도달 확인 + URL 스크럽 바이트 실증

- **무엇을**: DoD 잔여였던 **Sentry 알림 채널 도달 확인**을 닫았다. 프로덕션에서 강제 예외 → 이슈 생성 → **알림 메일 수신**까지 완주. 원인 규명 과정에서 부수로 **URL 스크럽 UI 확인**(8/31 잔여)까지 전송 바이트로 종결했다.
- **어떻게**:
  - 강제 예외 URL을 새로 생성(`fun` 누락 + `prompts=15`, `deflate-raw`+base64url) → `TypeError ... 'nightRatio'`.
  - 알림이 안 와서 **원인을 세 번에 걸쳐 좁혔다.** ① "이미 본 이슈라 `first seen` 미발동" — 확인 불가 ② "다른 시그니처로 새 이슈 만들기"(`week` 누락) — **실패, 예외가 안 났다**(대시보드가 `week` 누락을 견딘다). ③ 추측을 멈추고 **DevTools로 엔벨로프 요청 본문을 직접 떴다** — 여기서 즉시 규명.
  - 실측값: `level: error` · `mechanism.handled: true`(`error.tsx`의 `captureException`) · 응답 `200 {"id":"d464adbec..."}` — **접수는 처음부터 정상이었다**. 결정적 필드는 `"environment": "vercel-production"`.
  - 알림 규칙의 environment 필터를 `All Environments`로 바꾸자 메일이 도착했다.
  - **부수 발견(검증 절차 함정)**: 이미 `/report`가 열린 탭에 같은 해시 URL을 입력하면 **아무 일도 안 일어난다**. 앱이 첫 방문에서 해시를 지웠으므로(BR-004) 경로가 같고 프래그먼트만 다른 **동일 문서 내비게이션**이 되어 마운트가 없다. 증거는 주소창에 해시가 **남아 있다**는 것 — 해시 읽는 코드가 안 돌았다는 뜻이다. 새 탭 또는 새로고침이면 정상 발동.
- **왜**: `Send Test Notification`은 규칙의 **액션**만 실행하고 **조건**을 건너뛴다. 그래서 "테스트 메일은 오는데 실제 알림은 안 온다"가 성립했고, 이 비대칭이 원인을 액션(채널)이 아니라 조건 쪽으로 확정해줬다. environment가 `vercel-production`인 것은 Vercel 배포에서만 나타나므로, **8/31 로컬 프로덕션 빌드 검증(`production`)으로는 절대 발견할 수 없는 결함**이었다 — 로컬 검증이 프로덕션 검증을 대체하지 못하는 사례.
- **작업 결과**: DoD **13/16 → 14/16**. 잔여 2건(CLI 수동 완주·iOS Safari)은 전부 수단·세션 대기이고 코드 결함 0건.
  - **URL 스크럽 종결**: 이벤트의 `request.url = "https://scored.kr/report"` — 해시 없음. 8/31에 "로컬 수집기 바이트 검사로 갈음하고 UI 확인은 사용자 몫"으로 남겼던 항목(Sentry API 403 — 토큰이 소스맵 권한뿐)이 **실제 전송 바이트로 닫혔다**.
  - `RUNBOOK.md §1`에 environment 함정을 경고로 고정했다 — 알림만 조용하면 장애를 늦게 안다.
  - ⚠️ **잔여**: 테스트 이슈 삭제(Resolve 아님)는 사용자 몫. 안 지우면 런칭 후 실오류와 섞인다.
  - **방법론 교훈**: 추측 2회가 전부 빗나갔고, **실제 전송 바이트를 뜬 순간 1회에 규명**됐다. 관측 가능한 지점이 있으면 가설을 세우기 전에 먼저 뜬다.

## 2026-09-02 (Day 16) — iOS Safari 순회를 Mac Safari(동일 WebKit)로 대체 검증

- **무엇을**: DoD 잔여 `iOS Safari 실기기 전 화면 순회`를 실기기 없이 닫을 수 있는 데까지 닫았다. Mac Safari 26.6.2로 **엔진 기능 8종 실측 + 3라우트 전수 순회**를 수행하고, 못 덮는 항목을 미검증으로 명시했다.
- **어떻게**:
  - **먼저 위험을 코드에서 특정했다.** `playwright.config.ts`가 `chromium` 1종뿐이고 파일 주석이 직접 "브라우저 호환은 Day 17 iOS Safari 실기기 순회에서 본다"고 적어둔 상태 — **WebKit 커버리지가 0**이고 이 순회 하나가 그 전부를 담당하도록 설계돼 있었다.
  - 최대 블로커는 `src/lib/payload.ts:24`의 `DecompressionStream("deflate-raw")`. **기능 탐지도 폴백도 없다**(grep 0건). 미지원이면 `catch`로 떨어져 `ERR-HASH-001`("손상된 링크")을 띄운다 — 멀쩡한 링크를 손상됐다고 **오진하면서** 성적표가 통째로 안 열린다.
  - 위험을 4단계로 나눴다: ① `DecompressionStream`(치명) ② `navigator.share`·클립보드(iOS 제스처 정책) ③ 최신 CSS(`light-dark()`·`<dialog>`·`@starting-style`·`allow-discrete`·`:has()`) ④ 뷰포트·safe-area·`break-keep`.
  - **①③은 Mac Safari로 그대로 덮인다** — 동일 WebKit·동일 버전 계열이라 엔진 기능 지원이 전이된다. ②④는 기기 고유라 안 덮인다. 처음에 "Mac Safari로는 못 닫는다"고 뭉뚱그렸던 것을 이 구분으로 정정했다.
  - 콘솔 프로브 1회로 8종 판정 → **전부 통과**. 이어 검증용 성적표 URL을 생성하되, 지난번 `nightRatio` 크래시를 되풀이하지 않도록 **실제 `judge.ts`/`dashboard.ts`를 노드로 돌려 렌더 가능성을 먼저 확인**하고(`B+`·`에러의 늪 탐험가`·`weekDelta 11`) 넘겼다.
  - 3라우트(`/`·`/how`·`/report`) + iPhone 프리셋 세로/가로 순회 — 가로 스크롤 없음, 콘솔 에러 0, 리빌·히트맵 정상, **해시 제거 확인**(BR-004).
- **왜**: iOS에는 엔진 선택지가 없다 — Chrome·Firefox·카톡 인앱 전부 WebKit이라 여기서 깨지면 **그 기기의 모든 브라우저에서** 깨진다. v1 유입이 공유 링크 = 모바일이므로 착지점이 곧 이 엔진이다. `@starting-style`/`allow-discrete`는 `globals.css:108`에 이미 "미지원 시 즉시 표시로 강하" 설계가 적혀 있어 블로커가 아니었고, 실제 블로커는 폴백이 없는 `DecompressionStream`과 테마 토큰 전부를 지배하는 `light-dark()` 둘뿐이었다 — **이 둘만 확인하면 최악의 시나리오가 배제된다**는 판단으로 검증을 좁혔다.
- **작업 결과**: DoD **14/16 유지** — 체크박스는 켜지 않았다. 실기기 순회가 아니므로 "완료"라고 적는 것은 없는 검증을 있다고 적는 것이다.
  - 19_출시기준 iOS 항목을 **부분 검증 근거로 교체**, 잔여 표를 `수단 미확보` → `Mac Safari 부분 검증으로 대체 — 기기 정책 2건 미검증 상태로 런칭 확정`으로. `CLI Windows 실행 확인`과 동일한 처리다.
  - PLAN Day 4~9 규칙 개정 ②("실기기 수단을 Day 16까지 확보")는 **미달로 종결**을 명시했다. 다만 ③(실기기 전용 항목 폴백 선확보)이 지목했던 항목이 정확히 잔여와 일치해 — `navigator.share` transient activation·주소창/safe-area — 규칙 자체는 유효하게 작동했다.
  - Day 17 잔여는 **Search Console 등록·Vercel 플랜 판단 2건**(전부 사용자)으로 줄었다.
  - ⚠️ **미검증 잔여**: iOS 실기기에서의 공유 시트 발동·주소창 축소 대응. 런칭 후 실사용자 리포트로 확인할 수밖에 없다.

## 2026-09-02 (Day 16) — GA4 서버 측 수집 확인 (DebugView → 실시간 보고서로 갈음) · Day 16 종결

- **무엇을**: Day 16 마지막 잔여였던 `GA4 DebugView 육안`을 닫았다. 다만 **DebugView가 아니라 실시간 보고서**로 갈음했고, 그 과정에서 9/1 검증에 **증거 공백**이 있었음을 확인했다.
- **어떻게**:
  - 처음엔 "콘솔에서 `gtag('config', ID, {debug_mode:true})`를 다시 부르면 DebugView에 뜬다"고 안내하려 했다. **드리기 전에 실제로 실행해 `/g/collect` 요청을 떠보니 `_dbg=1`이 없었다** — 이벤트는 나가지만 디버그 플래그가 안 붙어 DebugView에는 영원히 안 뜬다. `layout.tsx:66` 설정에 `debug_mode`가 없기 때문이고, 재설정으로는 붙지 않는다. **틀린 절차를 넘기기 직전에 걸렀다.**
  - 대안 2개 중 **실시간 보고서**를 택했다. DebugView는 `debug_mode`가 필수라 Chrome 확장 설치가 필요한 반면, 실시간은 아무 설정 없이 바로 보이고 — 결정적으로 **"GA가 실제로 수집했나"라는 질문에 더 직접적인 답**이다. DebugView는 "내 브라우저가 디버그로 보낸 것"을, 실시간은 "서버가 받아 집계한 것"을 보여준다.
  - 사용자가 랜딩 2종 → 성적표 6종 → 실패 경로 1종 순으로 순회, **9종 전부 실시간에 표시** 확인.
- **왜 이게 형식적 확인이 아니었나**: 9/1 GA4 검증은 `/g/collect` 히트를 가로채 `en`·`ep.*`를 뜯는 방식이었다 — 즉 **"요청이 브라우저를 떠났다"까지만 증명했고 GA가 그걸 받았는지는 확인하지 않았다.** 실시간에 9종이 뜨면서 그 공백이 닫혔다. 킬 크라이테리아 판정의 유일한 정량 근거이므로(외부 눈 0명), 전송과 수집을 같은 것으로 취급할 수 없었다.
- **오관측 2건 — 둘 다 결론 내기 전에 걸러졌다**:
  - **`503`**: 확인 중 `/g/collect` 응답이 전부 `503`으로 잡혀 "이벤트가 버려지는 중"으로 보였다. 실시간에 이벤트가 뜨는 순간 **브라우저 확장이 요청을 가로채며 만든 관측 아티팩트**로 판명. 정상은 `204`다.
  - **`command_self_sent` 미발화**: 9종 중 이것만 안 떠서 재현을 시도했고 실제로 `catch` 진입(`"자동 복사가 안 됐어요"`)을 관측했다. 그러나 **자동화 컨텍스트는 `document.hasFocus() === false`라 `clipboard.writeText`가 무조건 `NotAllowedError`로 실패**한다 — 판별이 불가능한 관측이었다. 단정 대신 안내 문구로 구분하는 절차를 사용자에게 넘겼고, **실제 브라우저에서는 정상 발화**했다. 결함 아님.
  - 관측 도구 한계를 제품 결함으로 읽을 뻔한 사례가 이로써 4건(Playwright teardown 9/1 · Lighthouse snapshot 9/2 · 확장 프록시 503 · 포커스 없는 클립보드).
- **작업 결과**: **Day 16 잔여 0.** PLAN Day 16 마일스톤 체크. DoD 항목에 실시간 갈음 근거와 `_dbg=1` 실측을 기록. **14/16 유지**(GA4는 이미 체크된 항목의 증거 보강).
  - ⚠️ **자체 정정**: 위 조사 중 `command-cta.tsx`의 두 버튼이 "계측 원칙이 반대라 결함"이라고 적었으나 **틀렸다. 반대인 것이 스펙이다** — 15 §EVT 카탈로그가 EVT-LAND-001을 "복사 CTA **클릭**(성공 여부 무관)", EVT-LAND-002를 "나에게 보내기 **실행**" + `method: share_sheet | clipboard`로 나눠 정의한다. `method`는 `await share(...)`가 결정돼야 채울 수 있으므로 `track`을 앞으로 옮기면 스펙 위반이고, 시트를 띄우고 안 고른 사용자까지 "보냈다"로 세어 모바일 우회 경로 지표가 부풀려진다. **`track`을 앞으로 옮기는 수정은 하지 말 것.**
  - **backlog 후보 (미조치)**: 남는 것은 `onSend`의 `catch`에서 아무 이벤트도 안 나가는 것뿐이다. 클립보드 쓰기까지 실패하면 시도가 통째로 사라져 실패가 분모에서도 빠진다. 필요한 조치는 `catch`에 `method: "failed"` 한 줄(+ 15 §EVT 카탈로그의 `method` 값 추가). **미조치 근거**: 데스크톱 미재현이고 iOS 발생 여부는 미검증이라, 존재가 확인되지 않은 문제에 지표 스키마를 늘리는 셈이다. 실사용 데이터에서 `npx_command_copied` 대비 `command_self_sent`가 비정상적으로 낮으면 그때 근거가 생긴다.

## 2026-09-02 (Day 17 선처리) — Search Console 도메인 속성 등록 + 사이트맵 제출

- **무엇을**: Day 17 잔여였던 Search Console 등록을 마쳤다. **도메인 속성**으로 등록하고 `sitemap.xml`을 제출했다.
- **어떻게**: DNS가 가비아(`ns.gabia.co.kr`)이고 TXT 레코드가 0건인 상태를 먼저 확인한 뒤, 가비아 DNS 설정에 TXT를 추가하는 절차로 안내했다(사용자 수행 — 구글 계정 로그인은 내가 하지 않는 영역). 완료 후 **Google 공개 DNS(8.8.8.8)로 직접 조회해 전파를 확인**했다: `google-site-verification=SDq6eWINCfYXQL2r-RSH7VW_doGG0QgABn9UXD5Z4yQ`. 사이트맵은 `200 application/xml`, URL 2건(`/`·`/how`).
- **왜 도메인 속성인가**: URL 접두어 속성은 `https://scored.kr`과 `https://www.scored.kr`, `http`까지 각각 별개 속성으로 잡혀 나중에 중복 등록이 필요해진다. 도메인 속성은 DNS 한 번으로 전부 덮는다. 검증도 DNS TXT라 **배포와 무관**하다 — HTML meta 방식이었다면 검증 태그가 코드에 남고 롤백 시 소유권이 풀릴 수 있다.
- **작업 결과**: **Day 17 잔여는 `Vercel 플랜 판단` 하나**로 줄었다. `/report`가 사이트맵에서 빠져 있는 것은 9/1 결정대로 의도적 제외이며(해시에 개인 활동 데이터가 실린다), Search Console에서 누락으로 보여도 추가하지 않는다는 점을 기록에 남긴다.
  - 참고: 색인 반영에는 수일이 걸린다. 런칭일(9/8) 기준 검색 유입은 기대하지 않으며, 검색 채널은 애초에 v1 유입 전략(공유·입소문)의 축이 아니다.

## 2026-09-02 (Day 16 잔여) — 롤백이 남긴 자동 도메인 할당 OFF 상태 복구

- **무엇을**: 오전 롤백 리허설 이후 `scored.kr`이 **6시간 전 배포에 고정**돼 있던 것을 발견하고 `vercel promote`로 복구했다. RUNBOOK §2에 함정을 고정했다.
- **어떻게**: 사용자가 Vercel 대시보드의 `To undo the rollback promote to production or re-enable auto-assigning custom domains` 배너를 보고 물어왔다. 실측 결과 `vercel inspect https://scored.kr` → `scored-7pc2ygcid`(6시간 전)인데 `vercel ls --prod`에는 그 이후 **`● Ready` 프로덕션 배포가 5건**(5분·22분·49분·1시간·2시간 전) 쌓여 있었다. 전부 도메인을 못 받은 상태. `vercel promote <최신배포URL>`로 도메인 이전 + 자동 할당 재활성화, `vercel inspect`로 `scored-6vw2rbsa8` 복귀 확인.
- **왜 위험한가**: **빌드가 성공하고 배포 상태도 `Ready`로 뜬다.** 실패 신호가 어디에도 없고 도메인만 조용히 옛 배포를 가리킨다. GitHub·Vercel 로그만 보면 정상 배포로 보인다. 오늘 Sentry의 `vercel-production` 함정과 **정확히 같은 종류** — 성공한 것처럼 보이는데 실제로는 안 되는 상태다.
- **작업 결과**: 실피해 **0건**. 고정된 배포 이후 12커밋이 전부 문서였고 `src/`·`cli/` 변경이 0건이라 사용자가 보는 화면은 최신과 동일했다(`git log cefeedb..HEAD -- src/ cli/`로 확인).
  - RUNBOOK §2 롤백 절차에 **`vercel promote` 복구 단계를 명령 블록 안에 넣고**, 🚨 경고와 점검법(`vercel inspect`의 배포 id가 `vercel ls --prod` 맨 위와 같은지)을 추가했다.
  - **리허설의 값이 여기서 나왔다.** 롤백을 한 번도 안 돌려봤다면 이 상태를 **런칭 당일(9/8)에 처음 만났을 것**이다. 그때는 "코드는 머지됐는데 사이트가 안 바뀐다"를 원인 모른 채 디버깅하게 된다. 리허설의 목적은 절차가 되는지 확인하는 것이었는데, 실제로는 **절차가 남기는 부작용**을 찾아냈다.
