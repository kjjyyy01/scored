# 19 출시기준 (DoD)

> 실사 2026-08-31 (Day 14). 표기: `[x]` 검증 완료 · `[ ]` 미완. 근거 없이 체크하지 않는다.

- [x] SCR-001·002·003·004·005 (P0 코어 — 대시보드 포함) 전 상태 구현 — 반응형·모바일 퍼스트 완료 <br>↳ Day 14 실측: md+ 지표 2열(`328px 328px`) / md 미만 1열·가로 스크롤 없음
- [ ] CLI (F-007) npm publish + CPY-LAND-002 명령 실행 → `/report?from=cli` 자동 오픈 + `~/.scored/{day}.html` 로컬 대화 리포트 두 번째 탭 오픈 E2E 수동 확인 (macOS·Windows PowerShell 경로) <br>↳ npm `@jong-yeon/scored@0.1.1` 배포 완료. **macOS 수동 완주 = Day 13 검증 세션(미실시) / Windows 실행 확인 수단 없음**
- [x] BR-001·002·003·009·010·011 판정 로직 테스트 통과 (`/tdd` — BR-008): 등급 커브 경계·유형 동점·05시 경계 <br>↳ 웹 66 · CLI 33 통과. 등급 경계 TC-RPT-001-05 8건 · 05시 경계 TC-CLI-001-12/12b
- [x] BR-004 하이라이트 주소창 제거 + 명시 토글 동작 확인 (프라이버시 게이트) <br>↳ TC-RES-001-04 · TC-SHARE-002-01/02. 강제 예외 시에도 해시가 주소창에서 제거됨(실측)
- [x] EVT 전 이벤트 GA4 실발화 확인 (Day 15~16) <br>↳ 2026-09-01 `/g/collect` 히트 직접 검사 — **9종 전부 + 파라미터 전 변형** 발화(`entry` cli/link · `has_highlights` T/F · `skipped` T/F · `error_code` 001/002 · `method` clipboard/share_sheet·download/share_sheet · `highlight_included` T/F). 성적·하이라이트 전송 0 (14 §5). **DebugView 육안 확인은 당신 몫**
- [x] Sentry 오류 수집 + **알림 채널 도달 확인** (PLAN Day 15~16 필수) <br>↳ **2026-09-02 프로덕션 도달 확인 완료**: 강제 예외 → 엔벨로프 200 + event id 반환 → 이슈 생성 → **알림 메일 수신**. 막힌 원인은 알림 규칙의 environment 필터 — 배포 이벤트는 `production`이 아니라 **`vercel-production`**으로 들어간다(전송 바이트 실측). URL 스크럽도 같은 바이트에서 종결: `request.url = https://scored.kr/report` (해시 없음, BR-004)
- [x] 오늘의 유형 6종+폴백 이름·문구 풀 + 등급 임계값(내 30일 로그 캘리브레이션) 실콘텐츠 반영 (Day 7 산출물 — 더미 문구 런칭 금지) <br>↳ 더미 0건. 임계는 2026-08-31 재측정으로 무변경 확정 (OQ-002 종결)
- [x] 공개 연출 순서(채점 중 → 카운트업 → 등급 릴 → 카드) 3~4초·스킵·링크 유입 1.5초 확인 + `prefers-reduced-motion` 페이드 <br>↳ Day 12 프로덕션 실측 (대기 상태 + `결과 보기` 클릭 → 카드 공개)
- [x] OG 태그 — 정적 + 동적(`/api/og`) 모두 필수 (2026-08-16 컷 제외) — 공유 미리보기 육안 확인 <br>↳ 프로덕션 육안 확인: 정적 1200×630 / 동적 200 PNG. 불량 파라미터는 기본 OG 폴백(SCR-005 엣지 3)
- [x] JS 없이 랜딩·`/how` 콘텐츠 노출 + `/report` 안내 문구 노출 (서버 렌더링 위생) <br>↳ 프로덕션 HTML 직접 확인 — 3화면 모두 `<h1>` 서버 렌더
- [x] `/report` 전역 에러 바운더리(`error.tsx`) — 판정·렌더 예외 시 흰 화면 금지, ERR-APP-001 안내로 폴백 (더미 페이로드로 강제 예외 1회 확인) <br>↳ `fun` 누락 + `prompts≥10` 해시로 프로덕션에서 강제 예외 → `role=alert` + 안내 + 버튼 2개 렌더, 흰 화면 아님
- [x] CLI(F-007) CLI-001 §9 테스트 통과 + Windows 경로(PowerShell) 코드 리뷰 확인 <br>↳ CLI 33 통과. `open.ts` win32 분기 `Start-Process`, 홈은 `os.homedir()` — 코드 리뷰 완료 (실행 확인은 위 항목)
- [x] LCP 2.5초 예산 통과 (Day 17) <br>↳ **2026-09-02 프로덕션 재측정**: 모바일 390px·Slow 4G·CPU 4x **708ms** (예산의 28%) · 데스크톱 무스로틀 223ms · CLS 0.00. Sentry로 랜딩 JS +33%(269KB gzip) 늘어난 뒤의 수치 — 감시 항목 해소
- [x] 시크릿 스캔 0건 + 클라이언트 번들 시크릿 노출 무 (보안 4종 중 해당 항목) <br>↳ Day 14: git 이력 스캔 클린, 번들 노출은 `NEXT_PUBLIC_GA_ID`뿐, `npm audit` 0건
- [ ] iOS Safari 실기기 전 화면 순회 (공유 유입 착지점) <br>↳ **수단 미확보** (Xcode 미설치·실기기 미섭외). 마감 Day 16 밤
- [x] 404·favicon·robots.txt·사이트맵 + canonical·JSON-LD (위생 항목) <br>↳ `not-found.tsx`·`favicon.ico`·`robots.ts`·`sitemap.ts` 존재. **2026-09-01 추가**: canonical 4라우트(쿼리 제거 실측)·랜딩 `SoftwareApplication` JSON-LD·`/report` JSON-LD 0건 — 04 §메타데이터·색인 규약, E2E 5건이 회귀를 잡는다

## 실사 요약 (2026-09-01 갱신)

**14/16 완료** (Day 14 실사 11/16 → GA4 실발화 → LCP 재측정 → Sentry 알림 도달 해소). 잔여 2건은 전부 **수단·세션 대기**이고 코드 결함은 0건이다.

> Day 17 점검 항목은 하루 앞당겨(9/2) 통과했다 — canonical·JSON-LD 구현, Lighthouse 접근성·SEO·Best Practices **전 화면 100**(다크 대비 결함 1건 수정 후), LCP 예산 통과. 남은 Day 17 몫은 **Search Console 등록·Vercel 플랜 판단·iOS Safari 순회**(전부 당신 몫)다.

| 잔여 | 성격 | 해소 경로 |
|---|---|---|
| CLI 수동 완주 (macOS) | 세션 미실시 | Day 13 검증 세션 (당신 몫) |
| CLI Windows 실행 확인 | 수단 없음 | 코드 리뷰로 대체 — **실행 미검증 상태로 런칭 확정** |
| iOS Safari 순회 | 수단 미확보 | **마감 Day 16 밤 = 9/2** |
